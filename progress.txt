## Codebase Patterns
- Team Analysis Summary UI lives primarily in `components/organisms/AnalysisPanel/AnalysisPanel.tsx`; keep client components display-only and do not recompute optimizer scoring in the UI.
- Recommended lineup diagnostics should flow from `/api/generate-team` into `TeamManager.generatedTeam`, then render in `AnalysisPanel`; do not render recommendation details inside generated-team cards.
- Conditional `AnalysisPanel` accordion sections must derive keyboard focus order from rendered sections so Arrow/Home/End navigation does not focus hidden section refs.
- Accordion headers must use native buttons with `aria-expanded`, `aria-controls`, visible focus styles, and region panels labeled by the corresponding trigger.
- Recommended lineup cards should stay concise: show `Lead`, `Switch`, and `Closer` role terms, avoid repeating those labels inside values, omit lineup score and covered-threat lists, and keep weaknesses as semantic `ul`/`li` lists with one readable weakness label per item.
- `/api/generate-team` should resolve display-facing threat and lineup ids to readable species names before returning them so client components stay display-only.
- Keep core optimizer/domain models species-id based. Resolve species names at API/UI adapter boundaries rather than by importing Pokemon data into client analysis components.
- `lib/genetic/fitness/rosterScoring.ts` uses roster metrics internally for `scoreBreakdown` and fitness. Do not delete `buildRosterMetrics` or internal `PlayPokemonRosterScoreResult.metrics` unless scoring is intentionally refactored.
- `Chromosome` results should not expose top-level `rosterMetrics` or `benchUtility`; keep roster metrics inside `scorePlayPokemonRoster(...)` internals unless a current API/UI consumer requires a new explicit adapter field.
- `lib/genetic/fitness/lineupScoring.ts` uses resource path metrics for lineup/roster safety scoring. Remove shield path display fields only from API/UI output unless internals are proven unused.
- `lib/analysis/pokemonContributionAnalysis.ts` currently generates contribution rationale text. Remove this only after confirming no remaining UI/API/test consumer needs the string.
- `TeamDisplay` should not render PlayPokemon `Roster Metrics`; keep `rosterMetrics` and `benchUtility` as internal optimizer/domain diagnostics unless a current story explicitly reintroduces a display consumer.
- Optimizer score breakdown UI should consume API-provided `scoreBreakdown` through `TeamManager.generatedTeam` and render display-only explanations in `AnalysisPanel`.
- For optimizer-adjacent changes, invoke/read the `gbl-optimizer` skill and record optimizer docs read in this file before marking the story complete.
- Specialist delegation guidance: use `@api-designer` for `/api/generate-team` response contract changes, `@backend-developer` for domain deletion-boundary reviews, `@frontend-developer` for React/component/test implementation, and `@ui-designer` for accordion/card hierarchy and visual QA.
- Runtime ranking data includes `chargers`, `attackers`, and `consistency` CSVs for supported formats; ranking tests should assert these role-support categories load instead of treating them as optional missing files.

## Iteration Log
- Next iteration start: US-001 - Resolve lineup role species names in generate-team API

## 2026-06-08 13:47 MST - US-001
- Implemented API-side species-name resolution for recommended lineup `lead`, `switch`, and `closer` values while preserving existing weakness-name resolution and species-id domain output from `generateTeam`.
- Files changed: `app/api/generate-team/route.ts`, `app/api/generate-team/route.test.ts`, `lib/data/rankings.test.ts`, `progress.txt`
- Commit message: `feat: US-001 - Resolve lineup role species names in generate-team API`
- Checks:
  - `npx vitest run app/api/generate-team/route.test.ts` (pass after expected red failure)
  - `npx vitest run lib/data/rankings.test.ts` (pass after stale expectation update)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
- Implementation agents:
  - Recommended: `backend-developer`, `api-designer`, `frontend-developer`
  - Used: `backend-developer`, `api-designer`, `frontend-developer`
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`
  - Findings: fixed low stale test name; deferred low missing-file rankings coverage suggestion because it is outside US-001 and existing API MissingRankingDataError behavior was not changed; second review pass returned no actionable findings
- Decisions (why):
  - Chose API adapter mapping over client mapping because existing codebase patterns keep client analysis components display-only and already resolve lineup weaknesses in `/api/generate-team`.
  - Kept `team`, `coveredThreats`, and optimizer/domain recommendation types species-id based because US-001 only changes display-facing role values in the API response.
  - Updated `lib/data/rankings.test.ts` because the full quality gate exposed stale expectations that Chargers, Attackers, and Consistency ranking CSVs were missing even though current checked-in data includes them for supported formats.
- **Learnings for future iterations:**
  - `resolveRecommendedLineupLabels` is the route adapter seam for display-facing recommended lineup labels.
  - Route tests mock `speciesIdToSpeciesName`; add any species ids used in expected API display labels to the local mock map.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - None.
- Next iteration start:
  - US-002 - Remove Roster Metrics display and unused display-only generation
---

## 2026-06-08 14:02 MST - US-002
- Removed PlayPokemon Roster Metrics and Bench Utility display from generated-team cards, removed frontend pass-through through `TeamManager` and `ResultsPanel`, and removed unused display-facing `rosterMetrics`/`benchUtility` API response fields from `/api/generate-team` while preserving internal optimizer/domain diagnostics.
- Files changed: `app/AGENTS.md`, `app/api/generate-team/route.ts`, `app/api/generate-team/route.test.ts`, `components/AGENTS.md`, `components/organisms/ResultsPanel/ResultsPanel.tsx`, `components/organisms/TeamDisplay/TeamDisplay.tsx`, `components/organisms/TeamDisplay/TeamDisplay.test.tsx`, `components/organisms/TeamManager/TeamManager.tsx`, `components/organisms/TeamManager/TeamManager.test.tsx`, `lib/AGENTS.md`, `lib/types.ts`, `lib/genetic/algorithm.ts`, `lib/genetic/algorithm.test.ts`, `lib/genetic/fitness/index.test.ts`, `lib/genetic/fitness/recommendations.ts`, `lib/genetic/fitness/recommendations.test.ts`, `progress.txt`
- Commit message: `feat: US-002 - Remove Roster Metrics display and unused display-only generation`
- Checks:
  - `npx vitest run components/organisms/TeamDisplay/TeamDisplay.test.tsx components/organisms/TeamManager/TeamManager.test.tsx app/api/generate-team/route.test.ts` (pass)
  - `npx vitest run components/organisms/TeamDisplay/TeamDisplay.test.tsx components/organisms/TeamManager/TeamManager.test.tsx app/api/generate-team/route.test.ts lib/genetic/algorithm.test.ts lib/genetic/fitness/recommendations.test.ts lib/genetic/fitness/index.test.ts` (pass after specialist review fix)
  - `npx vitest run app/api/generate-team/route.test.ts lib/genetic/algorithm.test.ts lib/genetic/fitness/recommendations.test.ts` (pass after second specialist review fix)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
  - Browser verification with `dev-browser` at desktop and mobile viewport (pass; generated team rendered with `Team Notes` and without `Roster Metrics` or `Bench Utility`; `/api/generate-team` and `/api/team-details` returned 200; unrelated `favicon.ico` 404 observed)
- Implementation agents:
  - Recommended: `frontend-developer`, `backend-developer`, `api-designer`
  - Used: `frontend-developer`, `backend-developer`, `api-designer`; each returned guidance through the Task mechanism, with local tool limitations noted in their outputs but still provided actionable implementation advice
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `documentation-engineer`, `ui-designer`, `ux-researcher`
  - Findings: fixed medium unused `rosterMetrics`/`benchUtility` generation exposure from `generateTeam`/recommendation output; fixed medium API negative-test gap by retaining stale mocked diagnostics and asserting response omission; fixed low stale recommendation helper signature, recommendation output assertion, and progress metadata before final review pass; final pass returned no code/QA/UI/UX findings and one low progress-note clarity issue fixed before metadata staging
- Decisions (why):
  - Chose to remove `rosterMetrics` and `benchUtility` from the `/api/generate-team` response because the only discovered consumers were the removed display pass-through path and no public API schema was found.
  - Preserved `lib/genetic/fitness/rosterScoring.ts` metrics because roster metrics still support optimizer scoring, finalist diagnostics, and domain tests.
  - Removed top-level `Chromosome.rosterMetrics`, `Chromosome.benchUtility`, and recommendation-output bench utility because no remaining API/UI consumer exists after the Roster Metrics display removal.
  - Removed the now-unused `roster` parameter from `buildPlayPokemonRosterRecommendations(...)` because lineup recommendation conversion no longer depends on roster-wide bench utility output.
  - Updated nearby `AGENTS.md` guidance because existing component/API instructions still told future agents to pass roster diagnostics into generated-team cards.
  - Deferred staging `prd.json` until after final specialist review because Ralph marks story metadata passing only once implementation checks and staged review have passed.
- **Learnings for future iterations:**
  - `TeamDisplay` generated-team cards now only render team Pokemon and notes/export controls; analysis diagnostics should remain in `AnalysisPanel` or API-adapter fields with active consumers.
  - `/api/generate-team` should not expose display-only roster diagnostics without a current UI/API consumer; keep roster metrics inside scoring results such as `PlayPokemonRosterScoreResult.metrics` rather than top-level `Chromosome` output.
  - `buildPlayPokemonRosterRecommendations(...)` now returns recommended lineups only; bench utility warning generation should stay out unless a current display/API consumer is reintroduced.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - Local dev server was started for browser verification and `.ralph-dev-server.log` was left untracked and unstaged.
  - Existing `favicon.ico` 404 appears in browser console/network and was not introduced by this story.
- Next iteration start:
  - US-003 - Simplify recommended lineup card content
---

## 2026-06-08 14:25 MST - US-003
- Simplified recommended lineup cards by removing the visible lineup score and shield resource path labels, removed display-facing `resourcePathMetrics` from `RecommendedLineup` output, and preserved internal lineup scoring resource path metrics for optimizer safety calculations.
- Files changed: `app/api/generate-team/route.ts`, `app/api/generate-team/route.test.ts`, `components/AGENTS.md`, `components/organisms/AnalysisPanel/AnalysisPanel.tsx`, `components/organisms/AnalysisPanel/AnalysisPanel.test.tsx`, `lib/genetic/fitness/recommendations.ts`, `lib/genetic/fitness/recommendations.test.ts`, `lib/types.ts`, `lib/types.lineup.test.ts`, `tasks/prd-lineup-aware-team-generation.md`, `progress.txt`
- Commit message: `feat: US-003 - Simplify recommended lineup card content`
- Checks:
  - `npx vitest run components/organisms/AnalysisPanel/AnalysisPanel.test.tsx lib/genetic/fitness/recommendations.test.ts app/api/generate-team/route.test.ts lib/types.lineup.test.ts` (pass)
  - `npx tsc --noEmit` (pass after removing stale typed resource-path payload from route test defaults)
  - `npm run lint` (pass after using the `LineupResourcePathMetrics` type fixture)
  - `npm test` (pass)
  - Browser verification with `dev-browser` at desktop and mobile-width viewport (pass; recommended lineups rendered with role assignments, structure, and weaknesses; `Score`, `Balanced`, `Shield spend`, and `Shield save` were absent; `/api/pokemon-list`, `/api/generate-team`, and `/api/team-details` returned 200; existing `favicon.ico` 404 observed)
- Implementation agents:
  - Recommended: `frontend-developer`, `backend-developer`, `api-designer`
  - Used: `frontend-developer`, `backend-developer`, `api-designer`; each returned concise guidance through the Task mechanism, with local tool limitations noted in their outputs but still provided actionable implementation advice
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `documentation-engineer`, `ui-designer`, `ux-researcher`
  - Findings: fixed medium stale task PRD guidance for removed resource path output, fixed medium stale FR-35 resource path display requirement, and fixed low progress-log placeholder; blocked after third documentation review pass found the same stale resource-path display guidance class in follow-up UI findings
- Decisions (why):
  - Chose to remove `resourcePathMetrics` at the recommendation adapter/API-facing `RecommendedLineup` boundary because the remaining UI no longer consumes Balanced, Shield spend, or Shield save labels and internal scoring still needs shield-path metrics.
  - Kept `RecommendedLineup.score` in the API/domain recommendation contract because US-003 only removes score from the card display, while the value still identifies the sorted recommendation quality and existing consumers/tests use it as recommendation metadata.
  - Rebuilt `/api/generate-team` lineup label mapping without object spread so stale display-only fields cannot leak if a mock or future caller supplies extra diagnostics.
  - Updated `components/AGENTS.md` because prior guidance still instructed future agents to render resource path cards.
  - Updated `tasks/prd-lineup-aware-team-generation.md` because specialist review found older resource/shield metrics acceptance and functional requirements conflicted with the current US-003 API/display contract.
- **Learnings for future iterations:**
  - `RecommendedLineup` remains UI-ready recommendation metadata with `score` retained but `resourcePathMetrics` omitted; shield-path metrics stay on internal `LineupScoreResult` and roster scoring diagnostics.
  - `AnalysisPanel` recommended lineup cards now render only role assignments, structure, and weakness lists until a later story moves or restructures the section.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - Blocked before commit by Ralph review stabilization limit: `tasks/prd-lineup-aware-team-generation.md` follow-up UI findings still include stale resource path display label/color-band guidance for `Balanced`, `Shield spend`, and `Shield save`; next iteration should update or mark that historical bullet superseded, then rerun specialist review.
  - Existing `favicon.ico` 404 appears in browser console/network and was not introduced by this story.
  - `.ralph-dev-server.log` remains untracked and unstaged from the existing local dev server.
- Next iteration start:
  - US-003 continuation - Resolve stale resource path display follow-up guidance before committing
---

## 2026-06-08 14:37 MST - US-003
- Completed the US-003 continuation by updating stale task PRD follow-up guidance so resource path metrics remain internal scoring diagnostics unless a future current UI/API consumer explicitly reintroduces display output.
- Files changed: `app/api/generate-team/route.ts`, `app/api/generate-team/route.test.ts`, `components/AGENTS.md`, `components/organisms/AnalysisPanel/AnalysisPanel.tsx`, `components/organisms/AnalysisPanel/AnalysisPanel.test.tsx`, `lib/genetic/fitness/recommendations.ts`, `lib/genetic/fitness/recommendations.test.ts`, `lib/types.ts`, `lib/types.lineup.test.ts`, `tasks/prd-lineup-aware-team-generation.md`, `progress.txt`
- Commit message: `feat: US-003 - Simplify recommended lineup card content`
- Checks:
  - `npx vitest run components/organisms/AnalysisPanel/AnalysisPanel.test.tsx lib/genetic/fitness/recommendations.test.ts app/api/generate-team/route.test.ts lib/types.lineup.test.ts` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
  - Browser verification with `dev-browser` at desktop and mobile-width viewport (pass; recommended lineup cards show `Lead`, `Switch`, `Closer`, `Structure`, and `Weaknesses`; `Score`, `Balanced`, `Shield spend`, and `Shield save` were absent; `/api/pokemon-list`, `/api/generate-team`, and `/api/team-details` returned 200; existing 404 resource error observed)
- Implementation agents:
  - Recommended: `frontend-developer`, `backend-developer`, `api-designer`
  - Used: `frontend-developer`, `backend-developer`, `api-designer`; each returned concise guidance through the Task mechanism and confirmed the staged approach to keep shield-path scoring internal while removing display/API resource path output
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `documentation-engineer`, `ui-designer`, `ux-researcher`
  - Findings: fixed low stale US-010 resource path display guidance and low missing negative type fixture for `RecommendedLineup.resourcePathMetrics`; deferred low scoped-commit-format suggestion because Ralph explicitly requires `feat: US-003 - Simplify recommended lineup card content`; updated pending review placeholders before restaging; second review pass returned no actionable findings
- Decisions (why):
  - Chose to resolve the blocked stale task guidance rather than alter implementation code because the staged implementation already matched US-003 and current Codebase Patterns.
  - Kept resource path metrics documented as internal diagnostics so future work does not reintroduce `Balanced`, `Shield spend`, or `Shield save` display without an explicit consumer and accessibility range requirements.
  - Kept the Ralph-required unscoped commit message because the user instruction for this autonomous iteration is more specific than the repository's general scoped commit convention.
- **Learnings for future iterations:**
  - Historical task PRD follow-up findings can conflict with newer story contracts; update superseded follow-up text when specialist review identifies stale guidance.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - Existing 404 resource error appears in browser console and was not introduced by this story.
  - `.ralph-dev-server.log` remains untracked and unstaged from the existing local dev server.
- Next iteration start:
  - US-004 - Remove unused Per-Pokemon Contribution explanatory sections
---
