## Codebase Patterns
- Team Analysis Summary UI lives primarily in `components/organisms/AnalysisPanel/AnalysisPanel.tsx`; keep client components display-only and do not recompute optimizer scoring in the UI.
- Recommended lineup diagnostics should flow from `/api/generate-team` into `TeamManager.generatedTeam`, then render in `AnalysisPanel`; do not render recommendation details inside generated-team cards.
- Conditional `AnalysisPanel` accordion sections must derive keyboard focus order from rendered sections so Arrow/Home/End navigation does not focus hidden section refs.
- Accordion headers must use native buttons with `aria-expanded`, `aria-controls`, visible focus styles, and region panels labeled by the corresponding trigger.
- Recommended lineup cards should stay concise: show `Lead`, `Switch`, and `Closer` role terms, avoid repeating those labels inside values, omit lineup score and covered-threat lists, and keep weaknesses as semantic `ul`/`li` lists with one readable weakness label per item.
- Recommended lineup cards should not render `diagnosticLabel`/`Structure`; keep structure labels as internal optimizer/API diagnostics unless a current story explicitly reintroduces a display consumer.
- `/api/generate-team` should resolve display-facing threat and lineup ids to readable species names before returning them so client components stay display-only.
- Keep core optimizer/domain models species-id based. Resolve species names at API/UI adapter boundaries rather than by importing Pokemon data into client analysis components.
- `lib/genetic/fitness/rosterScoring.ts` uses roster metrics internally for `scoreBreakdown` and fitness. Do not delete `buildRosterMetrics` or internal `PlayPokemonRosterScoreResult.metrics` unless scoring is intentionally refactored.
- `Chromosome` results should not expose top-level `rosterMetrics` or `benchUtility`; keep roster metrics inside `scorePlayPokemonRoster(...)` internals unless a current API/UI consumer requires a new explicit adapter field.
- `lib/genetic/fitness/lineupScoring.ts` uses resource path metrics for lineup/roster safety scoring. Remove shield path display fields only from API/UI output unless internals are proven unused.
- `lib/analysis/pokemonContributionAnalysis.ts` returns concise per-Pokemon numeric contribution diagnostics only; do not reintroduce generated rationale text unless a current UI/API consumer explicitly needs display copy.
- `TeamDisplay` should not render PlayPokemon `Roster Metrics`; keep `rosterMetrics` and `benchUtility` as internal optimizer/domain diagnostics unless a current story explicitly reintroduces a display consumer.
- Optimizer score breakdown UI should consume API-provided `scoreBreakdown` through `TeamManager.generatedTeam` and render display-only explanations in `AnalysisPanel`.
- Summary Statistics in `AnalysisPanel` is the optimizer score-card section; do not reintroduce client-computed Overall Fitness, Threat Handling, Shield Stability, Core-Breaker Risk, Expected Ranges, or a separate Optimizer Score Breakdown accordion without a current story.
- Summary Statistics optimizer score cards should render Role immediately after Bulk as a standard card, followed by Offensive Ratio and Defensive Ratio in a dedicated two-card comparison row; do not rely on a single auto-flow grid for the ratio pair.
- Recommended Lineups belongs in the `AnalysisPanel` accordion immediately after Summary Statistics; keep it in the rendered-section list so keyboard navigation follows actual visible section order.
- Per-Pokemon Contribution headers should render Replacement Risk in a dedicated row below the Pokemon name; keep the metric boxes unchanged below the header.
- For optimizer-adjacent changes, invoke/read the `gbl-optimizer` skill and record optimizer docs read in this file before marking the story complete.
- PlayPokemon bring-6 lineup enumeration evaluates 120 ordered lead/switch/closer assignments (`6 * 5 * 4`); do not canonicalize or sort switch/closer backline pairs because role order changes scoring.
- Final generated `Chromosome.fitness` should match the attached final `scoreBreakdown.score`; PlayPokemon finalists use recomputed full roster fitness and GBL finalists use the final lineup recommendation score before multi-team sorting.
- Specialist delegation guidance: use `@api-designer` for `/api/generate-team` response contract changes, `@backend-developer` for domain deletion-boundary reviews, `@frontend-developer` for React/component/test implementation, and `@ui-designer` for accordion/card hierarchy and visual QA.
- Runtime ranking data includes `chargers`, `attackers`, and `consistency` CSVs for supported formats; ranking tests should assert these role-support categories load instead of treating them as optional missing files.

## Iteration Log
- Next iteration start: US-001 - Resolve lineup role species names in generate-team API

## 2026-06-08 13:47 MST - US-001
- Implemented API-side species-name resolution for recommended lineup `lead`, `switch`, and `closer` values while preserving existing weakness-name resolution and species-id domain output from `generateTeam`.
- Files changed: `app/api/generate-team/route.ts`, `app/api/generate-team/route.test.ts`, `lib/data/rankings.test.ts`, `progress.txt`
- Commit message: `feat: US-001 - Resolve lineup role species names in generate-team API`
- Checks:
  - `npx vitest run app/api/generate-team/route.test.ts` (pass after expected red failure)
  - `npx vitest run lib/data/rankings.test.ts` (pass after stale expectation update)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
- Implementation agents:
  - Recommended: `backend-developer`, `api-designer`, `frontend-developer`
  - Used: `backend-developer`, `api-designer`, `frontend-developer`
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`
  - Findings: fixed low stale test name; deferred low missing-file rankings coverage suggestion because it is outside US-001 and existing API MissingRankingDataError behavior was not changed; second review pass returned no actionable findings
- Decisions (why):
  - Chose API adapter mapping over client mapping because existing codebase patterns keep client analysis components display-only and already resolve lineup weaknesses in `/api/generate-team`.
  - Kept `team`, `coveredThreats`, and optimizer/domain recommendation types species-id based because US-001 only changes display-facing role values in the API response.
  - Updated `lib/data/rankings.test.ts` because the full quality gate exposed stale expectations that Chargers, Attackers, and Consistency ranking CSVs were missing even though current checked-in data includes them for supported formats.
- **Learnings for future iterations:**
  - `resolveRecommendedLineupLabels` is the route adapter seam for display-facing recommended lineup labels.
  - Route tests mock `speciesIdToSpeciesName`; add any species ids used in expected API display labels to the local mock map.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - None.
- Next iteration start:
  - US-002 - Remove Roster Metrics display and unused display-only generation
---

## 2026-06-08 14:02 MST - US-002
- Removed PlayPokemon Roster Metrics and Bench Utility display from generated-team cards, removed frontend pass-through through `TeamManager` and `ResultsPanel`, and removed unused display-facing `rosterMetrics`/`benchUtility` API response fields from `/api/generate-team` while preserving internal optimizer/domain diagnostics.
- Files changed: `app/AGENTS.md`, `app/api/generate-team/route.ts`, `app/api/generate-team/route.test.ts`, `components/AGENTS.md`, `components/organisms/ResultsPanel/ResultsPanel.tsx`, `components/organisms/TeamDisplay/TeamDisplay.tsx`, `components/organisms/TeamDisplay/TeamDisplay.test.tsx`, `components/organisms/TeamManager/TeamManager.tsx`, `components/organisms/TeamManager/TeamManager.test.tsx`, `lib/AGENTS.md`, `lib/types.ts`, `lib/genetic/algorithm.ts`, `lib/genetic/algorithm.test.ts`, `lib/genetic/fitness/index.test.ts`, `lib/genetic/fitness/recommendations.ts`, `lib/genetic/fitness/recommendations.test.ts`, `progress.txt`
- Commit message: `feat: US-002 - Remove Roster Metrics display and unused display-only generation`
- Checks:
  - `npx vitest run components/organisms/TeamDisplay/TeamDisplay.test.tsx components/organisms/TeamManager/TeamManager.test.tsx app/api/generate-team/route.test.ts` (pass)
  - `npx vitest run components/organisms/TeamDisplay/TeamDisplay.test.tsx components/organisms/TeamManager/TeamManager.test.tsx app/api/generate-team/route.test.ts lib/genetic/algorithm.test.ts lib/genetic/fitness/recommendations.test.ts lib/genetic/fitness/index.test.ts` (pass after specialist review fix)
  - `npx vitest run app/api/generate-team/route.test.ts lib/genetic/algorithm.test.ts lib/genetic/fitness/recommendations.test.ts` (pass after second specialist review fix)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
  - Browser verification with `dev-browser` at desktop and mobile viewport (pass; generated team rendered with `Team Notes` and without `Roster Metrics` or `Bench Utility`; `/api/generate-team` and `/api/team-details` returned 200; unrelated `favicon.ico` 404 observed)
- Implementation agents:
  - Recommended: `frontend-developer`, `backend-developer`, `api-designer`
  - Used: `frontend-developer`, `backend-developer`, `api-designer`; each returned guidance through the Task mechanism, with local tool limitations noted in their outputs but still provided actionable implementation advice
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `documentation-engineer`, `ui-designer`, `ux-researcher`
  - Findings: fixed medium unused `rosterMetrics`/`benchUtility` generation exposure from `generateTeam`/recommendation output; fixed medium API negative-test gap by retaining stale mocked diagnostics and asserting response omission; fixed low stale recommendation helper signature, recommendation output assertion, and progress metadata before final review pass; final pass returned no code/QA/UI/UX findings and one low progress-note clarity issue fixed before metadata staging
- Decisions (why):
  - Chose to remove `rosterMetrics` and `benchUtility` from the `/api/generate-team` response because the only discovered consumers were the removed display pass-through path and no public API schema was found.
  - Preserved `lib/genetic/fitness/rosterScoring.ts` metrics because roster metrics still support optimizer scoring, finalist diagnostics, and domain tests.
  - Removed top-level `Chromosome.rosterMetrics`, `Chromosome.benchUtility`, and recommendation-output bench utility because no remaining API/UI consumer exists after the Roster Metrics display removal.
  - Removed the now-unused `roster` parameter from `buildPlayPokemonRosterRecommendations(...)` because lineup recommendation conversion no longer depends on roster-wide bench utility output.
  - Updated nearby `AGENTS.md` guidance because existing component/API instructions still told future agents to pass roster diagnostics into generated-team cards.
  - Deferred staging `prd.json` until after final specialist review because Ralph marks story metadata passing only once implementation checks and staged review have passed.
- **Learnings for future iterations:**
  - `TeamDisplay` generated-team cards now only render team Pokemon and notes/export controls; analysis diagnostics should remain in `AnalysisPanel` or API-adapter fields with active consumers.
  - `/api/generate-team` should not expose display-only roster diagnostics without a current UI/API consumer; keep roster metrics inside scoring results such as `PlayPokemonRosterScoreResult.metrics` rather than top-level `Chromosome` output.
  - `buildPlayPokemonRosterRecommendations(...)` now returns recommended lineups only; bench utility warning generation should stay out unless a current display/API consumer is reintroduced.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - Local dev server was started for browser verification and `.ralph-dev-server.log` was left untracked and unstaged.
  - Existing `favicon.ico` 404 appears in browser console/network and was not introduced by this story.
- Next iteration start:
  - US-003 - Simplify recommended lineup card content
---

## 2026-06-08 14:25 MST - US-003
- Simplified recommended lineup cards by removing the visible lineup score and shield resource path labels, removed display-facing `resourcePathMetrics` from `RecommendedLineup` output, and preserved internal lineup scoring resource path metrics for optimizer safety calculations.
- Files changed: `app/api/generate-team/route.ts`, `app/api/generate-team/route.test.ts`, `components/AGENTS.md`, `components/organisms/AnalysisPanel/AnalysisPanel.tsx`, `components/organisms/AnalysisPanel/AnalysisPanel.test.tsx`, `lib/genetic/fitness/recommendations.ts`, `lib/genetic/fitness/recommendations.test.ts`, `lib/types.ts`, `lib/types.lineup.test.ts`, `tasks/prd-lineup-aware-team-generation.md`, `progress.txt`
- Commit message: `feat: US-003 - Simplify recommended lineup card content`
- Checks:
  - `npx vitest run components/organisms/AnalysisPanel/AnalysisPanel.test.tsx lib/genetic/fitness/recommendations.test.ts app/api/generate-team/route.test.ts lib/types.lineup.test.ts` (pass)
  - `npx tsc --noEmit` (pass after removing stale typed resource-path payload from route test defaults)
  - `npm run lint` (pass after using the `LineupResourcePathMetrics` type fixture)
  - `npm test` (pass)
  - Browser verification with `dev-browser` at desktop and mobile-width viewport (pass; recommended lineups rendered with role assignments, structure, and weaknesses; `Score`, `Balanced`, `Shield spend`, and `Shield save` were absent; `/api/pokemon-list`, `/api/generate-team`, and `/api/team-details` returned 200; existing `favicon.ico` 404 observed)
- Implementation agents:
  - Recommended: `frontend-developer`, `backend-developer`, `api-designer`
  - Used: `frontend-developer`, `backend-developer`, `api-designer`; each returned concise guidance through the Task mechanism, with local tool limitations noted in their outputs but still provided actionable implementation advice
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `documentation-engineer`, `ui-designer`, `ux-researcher`
  - Findings: fixed medium stale task PRD guidance for removed resource path output, fixed medium stale FR-35 resource path display requirement, and fixed low progress-log placeholder; blocked after third documentation review pass found the same stale resource-path display guidance class in follow-up UI findings
- Decisions (why):
  - Chose to remove `resourcePathMetrics` at the recommendation adapter/API-facing `RecommendedLineup` boundary because the remaining UI no longer consumes Balanced, Shield spend, or Shield save labels and internal scoring still needs shield-path metrics.
  - Kept `RecommendedLineup.score` in the API/domain recommendation contract because US-003 only removes score from the card display, while the value still identifies the sorted recommendation quality and existing consumers/tests use it as recommendation metadata.
  - Rebuilt `/api/generate-team` lineup label mapping without object spread so stale display-only fields cannot leak if a mock or future caller supplies extra diagnostics.
  - Updated `components/AGENTS.md` because prior guidance still instructed future agents to render resource path cards.
  - Updated `tasks/prd-lineup-aware-team-generation.md` because specialist review found older resource/shield metrics acceptance and functional requirements conflicted with the current US-003 API/display contract.
- **Learnings for future iterations:**
  - `RecommendedLineup` remains UI-ready recommendation metadata with `score` retained but `resourcePathMetrics` omitted; shield-path metrics stay on internal `LineupScoreResult` and roster scoring diagnostics.
  - `AnalysisPanel` recommended lineup cards now render only role assignments, structure, and weakness lists until a later story moves or restructures the section.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - Blocked before commit by Ralph review stabilization limit: `tasks/prd-lineup-aware-team-generation.md` follow-up UI findings still include stale resource path display label/color-band guidance for `Balanced`, `Shield spend`, and `Shield save`; next iteration should update or mark that historical bullet superseded, then rerun specialist review.
  - Existing `favicon.ico` 404 appears in browser console/network and was not introduced by this story.
  - `.ralph-dev-server.log` remains untracked and unstaged from the existing local dev server.
- Next iteration start:
  - US-003 continuation - Resolve stale resource path display follow-up guidance before committing
---

## 2026-06-08 14:37 MST - US-003
- Completed the US-003 continuation by updating stale task PRD follow-up guidance so resource path metrics remain internal scoring diagnostics unless a future current UI/API consumer explicitly reintroduces display output.
- Files changed: `app/api/generate-team/route.ts`, `app/api/generate-team/route.test.ts`, `components/AGENTS.md`, `components/organisms/AnalysisPanel/AnalysisPanel.tsx`, `components/organisms/AnalysisPanel/AnalysisPanel.test.tsx`, `lib/genetic/fitness/recommendations.ts`, `lib/genetic/fitness/recommendations.test.ts`, `lib/types.ts`, `lib/types.lineup.test.ts`, `tasks/prd-lineup-aware-team-generation.md`, `progress.txt`
- Commit message: `feat: US-003 - Simplify recommended lineup card content`
- Checks:
  - `npx vitest run components/organisms/AnalysisPanel/AnalysisPanel.test.tsx lib/genetic/fitness/recommendations.test.ts app/api/generate-team/route.test.ts lib/types.lineup.test.ts` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
  - Browser verification with `dev-browser` at desktop and mobile-width viewport (pass; recommended lineup cards show `Lead`, `Switch`, `Closer`, `Structure`, and `Weaknesses`; `Score`, `Balanced`, `Shield spend`, and `Shield save` were absent; `/api/pokemon-list`, `/api/generate-team`, and `/api/team-details` returned 200; existing 404 resource error observed)
- Implementation agents:
  - Recommended: `frontend-developer`, `backend-developer`, `api-designer`
  - Used: `frontend-developer`, `backend-developer`, `api-designer`; each returned concise guidance through the Task mechanism and confirmed the staged approach to keep shield-path scoring internal while removing display/API resource path output
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `documentation-engineer`, `ui-designer`, `ux-researcher`
  - Findings: fixed low stale US-010 resource path display guidance and low missing negative type fixture for `RecommendedLineup.resourcePathMetrics`; deferred low scoped-commit-format suggestion because Ralph explicitly requires `feat: US-003 - Simplify recommended lineup card content`; updated pending review placeholders before restaging; second review pass returned no actionable findings
- Decisions (why):
  - Chose to resolve the blocked stale task guidance rather than alter implementation code because the staged implementation already matched US-003 and current Codebase Patterns.
  - Kept resource path metrics documented as internal diagnostics so future work does not reintroduce `Balanced`, `Shield spend`, or `Shield save` display without an explicit consumer and accessibility range requirements.
  - Kept the Ralph-required unscoped commit message because the user instruction for this autonomous iteration is more specific than the repository's general scoped commit convention.
- **Learnings for future iterations:**
  - Historical task PRD follow-up findings can conflict with newer story contracts; update superseded follow-up text when specialist review identifies stale guidance.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - Existing 404 resource error appears in browser console and was not introduced by this story.
  - `.ralph-dev-server.log` remains untracked and unstaged from the existing local dev server.
- Next iteration start:
  - US-004 - Remove unused Per-Pokemon Contribution explanatory sections
---

## 2026-06-08 14:51 MST - US-004
- Removed the display-only Fitness Contribution Categories accordion section, removed the Per-Pokemon Contribution relative grading guide and rationale copy, and removed unused contribution rationale generation from the shared analysis payload while preserving numeric contribution diagnostics.
- Files changed: `app/api/generate-team/route.test.ts`, `components/AGENTS.md`, `components/organisms/AnalysisPanel/AnalysisPanel.tsx`, `components/organisms/AnalysisPanel/AnalysisPanel.test.tsx`, `lib/AGENTS.md`, `lib/analysis/pokemonContributionAnalysis.ts`, `lib/analysis/pokemonContributionAnalysis.test.ts`, `lib/types.ts`, `progress.txt`
- Commit message: `feat: US-004 - Remove unused Per-Pokemon Contribution explanatory sections`
- Checks:
  - `npx vitest run components/organisms/AnalysisPanel/AnalysisPanel.test.tsx lib/analysis/pokemonContributionAnalysis.test.ts app/api/generate-team/route.test.ts` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
  - Browser verification with `dev-browser` at desktop and mobile-width viewport (pass; generated team rendered with Per-Pokemon Contribution numeric metrics, without Fitness Contribution Categories, Relative grading guide, or rationale copy; `/api/pokemon-list`, `/api/generate-team`, and `/api/team-details` returned 200; existing `favicon.ico` 404 observed)
- Implementation agents:
  - Recommended: `frontend-developer`, `backend-developer`, `api-designer`
  - Used: `frontend-developer`, `backend-developer`, `api-designer`; each returned concise implementation guidance through the Task mechanism and recommended removing `rationale` after confirming no remaining consumer
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `documentation-engineer`, `ui-designer`, `ux-researcher`
  - Findings: fixed low QA gap by asserting retained API contribution fields; fixed low UI/code-review issue by removing unexplained relative color grading from per-Pokemon numeric metrics; second review pass returned no actionable findings
- Decisions (why):
  - Chose to remove `PokemonContributionAnalysisEntry.rationale` from the shared payload because the only active product consumer was the removed UI paragraph and repository patterns favor removing display-only diagnostics without active consumers.
  - Kept per-Pokemon numeric metrics and replacement-risk badges because they remain the concise contribution information requested by the story.
  - Removed the whole Fitness Contribution Categories accordion section rather than hiding its cards because its values were generated entirely in the client for a now-removed display-only section.
  - Removed relative color styling from per-Pokemon numeric metrics because the explanatory grading guide was intentionally removed and the remaining values should be concise raw diagnostics.
- **Learnings for future iterations:**
  - Per-Pokemon Contribution now renders only Pokemon name, replacement risk, Threats Handled, Coverage Added, and High-Pressure Relief.
  - Contribution analysis entries no longer include `rationale`; tests should assert payload fixtures omit it when changing analysis response shape.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - Existing `favicon.ico` 404 appears in browser console/network and was not introduced by this story.
  - `.ralph-dev-server.log` remains untracked and unstaged from the existing local dev server.
- Next iteration start:
  - US-005 - Convert Recommended Lineups to analysis accordion section
---

## 2026-06-08 15:04 MST - US-005
- Converted Recommended Lineups from a standalone panel into the `AnalysisPanel` accordion, placed it immediately after Summary Statistics, preserved concise lineup cards inside the expanded panel, and kept keyboard navigation derived from rendered accordion sections.
- Files changed: `components/AGENTS.md`, `components/organisms/AnalysisPanel/AnalysisPanel.tsx`, `components/organisms/AnalysisPanel/AnalysisPanel.test.tsx`, `progress.txt`, `prd.json`
- Commit message: `feat: US-005 - Convert Recommended Lineups to analysis accordion section`
- Checks:
  - `npx vitest run components/organisms/AnalysisPanel/AnalysisPanel.test.tsx` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
  - Browser verification with `dev-browser` at desktop and mobile-width viewport (pass; Recommended Lineups rendered directly after Summary Statistics as an accordion section, expanded to show role assignments/structure/weaknesses, and ArrowDown moved focus to the next rendered section; `/api/pokemon-list`, `/api/generate-team`, and `/api/team-details` returned 200; existing resource 404 observed)
- Implementation agents:
  - Recommended: `frontend-developer`, `ui-designer`
  - Used: `frontend-developer`, `ui-designer`; each returned concise implementation or review guidance through the Task mechanism, with local tool limitations noted in their outputs but still provided actionable guidance
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `documentation-engineer`, `ui-designer`, `ux-researcher`
  - Findings: fixed low test coverage/assertion brittleness and low progress traceability issues from first pass; second pass returned no actionable code, QA, UI, or UX findings; deferred low scoped-commit-format documentation suggestion because Ralph explicitly requires `feat: US-005 - Convert Recommended Lineups to analysis accordion section`
- Decisions (why):
  - Extended the existing accordion section model instead of creating separate lineup accordion state so `aria-expanded`, `aria-controls`, region labeling, focus refs, and Arrow/Home/End navigation stay consistent across analysis sections.
  - Preserved recommended lineup rendering when analysis details are unavailable by allowing the visible accordion section list to include lineups independently of Summary Statistics and Per-Pokemon Contribution.
  - Kept lineup cards concise and display-only because API output already resolves display-facing labels and the UI should not recompute optimizer diagnostics.
- **Learnings for future iterations:**
  - `AnalysisPanel` accordion visibility is now data-driven per section: Summary Statistics and Per-Pokemon Contribution require analysis/fitness, Recommended Lineups requires lineup data, and Optimizer Score Breakdown requires score breakdown data.
  - Tests that assert recommended lineup contents need to expand the Recommended Lineups accordion before querying card details.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - Existing resource 404 appears in browser console and was not introduced by this story.
  - `.ralph-dev-server.log` remains untracked and unstaged from the existing local dev server.
- Next iteration start:
  - US-006 - Reorder Optimizer Score Breakdown fields
---

## 2026-06-08 15:17 MST - US-006
- Reordered Optimizer Score Breakdown display so Offensive Ratio appears before Defensive Ratio, rendered both ratio cards in a dedicated responsive two-card row for side-by-side comparison, and kept Role after the ratio pair while preserving existing labels, values, descriptions, and quality badges.
- Files changed: `components/AGENTS.md`, `components/organisms/AnalysisPanel/AnalysisPanel.tsx`, `components/organisms/AnalysisPanel/AnalysisPanel.test.tsx`, `progress.txt`, `prd.json`
- Commit message: `feat: US-006 - Reorder Optimizer Score Breakdown fields`
- Checks:
  - `npx vitest run components/organisms/AnalysisPanel/AnalysisPanel.test.tsx` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
  - Browser verification with `dev-browser` at desktop and mobile-width viewport (pass; Optimizer Score Breakdown rendered `Offensive Ratio` and `Defensive Ratio` side-by-side at desktop width, stacked in order on mobile, with `Role` after both; `/api/pokemon-list`, `/api/generate-team`, and `/api/team-details` returned 200; existing 404 resource error observed)
- Implementation agents:
  - Recommended: `frontend-developer`, `ui-designer`
  - Used: `frontend-developer`, `ui-designer`; each returned concise implementation/design guidance through the Task mechanism, with local tool limitations noted in their outputs but still provided actionable guidance
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `ui-designer`, `ux-researcher`
  - Findings: none; all reviewers returned empty actionable findings lists, with residual risk limited to relying on the already completed checks/browser verification during review
- Decisions (why):
  - Chose a dedicated ratio-card grid row over only swapping component order because five primary cards precede the ratio pair; a single two-column auto-flow grid would place Offensive Ratio below Bulk instead of beside Defensive Ratio.
  - Kept all optimizer score values and descriptions display-only in `AnalysisPanel` because score computation and API payload construction remain outside the client component.
- **Learnings for future iterations:**
  - Optimizer Score Breakdown source order is now `Synergy`, `Coverage`, `Safety`, `Consistency`, `Bulk`, `Offensive Ratio`, `Defensive Ratio`, then `Role`.
  - To compare two score cards side-by-side after an odd number of preceding cards, render the pair in its own `sm:grid-cols-2` group instead of depending on the parent grid auto-placement.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - Existing 404 resource error appears in browser console and was not introduced by this story.
  - `.ralph-dev-server.log` remains untracked and unstaged from the existing local dev server.
- Next iteration start:
  - All current stories expected complete after final PRD metadata update.
---

## 2026-06-08 15:56 MST - US-007
- Removed the visible `Structure` row from expanded Recommended Lineups cards while preserving Lead, Switch, Closer, and semantic Weaknesses lists; kept `diagnosticLabel` generation and API/domain metadata intact.
- Files changed: `components/AGENTS.md`, `components/organisms/AnalysisPanel/AnalysisPanel.tsx`, `components/organisms/AnalysisPanel/AnalysisPanel.test.tsx`, `progress.txt`, `prd.json`
- Commit message: `feat: US-007 - Remove structure from recommended lineup cards`
- Checks:
  - `npx vitest run components/organisms/AnalysisPanel/AnalysisPanel.test.tsx` (fail before implementation for expected visible Structure text, pass after implementation)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
  - Browser verification with `dev-browser` at desktop and mobile-width viewport (pass; Recommended Lineups expanded with Lead, Switch, Closer, and Weaknesses visible and `Structure` absent; `/api/pokemon-list`, `/api/generate-team`, and `/api/team-details` returned 200; existing 404 resource error observed)
- Implementation agents:
  - Recommended: `frontend-developer`, `backend-developer`, `ui-designer`
  - Used: `frontend-developer`, `backend-developer`, `ui-designer`; each returned concise implementation, contract-audit, or hierarchy guidance through the Task mechanism, with local tool limitations noted in their outputs but still provided actionable guidance
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `documentation-engineer`, `ui-designer`, `ux-researcher`
  - Findings: fixed low progress-log review placeholder; deferred low scoped-commit-format suggestion because Ralph explicitly requires `feat: US-007 - Remove structure from recommended lineup cards`; second review pass returned no actionable findings
- Decisions (why):
  - Chose a display-only removal in `AnalysisPanel` because US-007 asks to remove the card row, and `diagnosticLabel` remains useful internal optimizer/API metadata with existing tests and contract consumers.
  - Simplified the leftover weakness container from a two-column grid to a single text block so removing Structure does not leave an empty layout column.
  - Updated nearby `components/AGENTS.md` because future lineup-card work should know that `diagnosticLabel`/`Structure` is internal unless an explicit display consumer is reintroduced.
  - Kept the Ralph-required unscoped commit message because the user instruction for this autonomous iteration is more specific than the repository's general scoped commit convention.
- **Learnings for future iterations:**
  - Recommended lineup `diagnosticLabel` is still part of the recommendation contract, but `AnalysisPanel` should not render it as a Structure row.
  - Tests that validate removed lineup-card labels should expand the Recommended Lineups accordion before asserting absence and retained role/weakness details.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - Existing 404 resource error appears in browser console/network and was not introduced by this story.
  - `.ralph-dev-server.log` remains untracked and unstaged from the existing local dev server.
- Next iteration start:
  - US-008 - Replace Summary Statistics content with optimizer score cards
---

## 2026-06-08 16:07 MST - US-008
- Replaced Summary Statistics content with API-provided optimizer score cards, removed the old client-computed summary metric cards/ranges, removed the separate Optimizer Score Breakdown accordion and stale normalized-category description, and kept Recommended Lineups immediately after Summary Statistics.
- Files changed: `components/AGENTS.md`, `components/organisms/AnalysisPanel/AnalysisPanel.tsx`, `components/organisms/AnalysisPanel/AnalysisPanel.test.tsx`, `progress.txt`, `prd.json`
- Commit message: `feat: US-008 - Replace Summary Statistics content with optimizer score cards`
- Checks:
  - `npx vitest run components/organisms/AnalysisPanel/AnalysisPanel.test.tsx` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
  - Browser verification with `dev-browser` at desktop and mobile-width viewport (pass; Summary Statistics expanded to optimizer score cards, old summary metrics and separate Optimizer Score Breakdown section were absent, Recommended Lineups remained immediately after Summary Statistics; `/api/pokemon-list`, `/api/generate-team`, and `/api/team-details` returned 200; existing `favicon.ico` 404 observed)
- Implementation agents:
  - Recommended: `frontend-developer`, `ui-designer`, `api-designer`
  - Used: `frontend-developer`, `ui-designer`, `api-designer`; each returned concise implementation, visual hierarchy, or contract guidance through the Task mechanism, with local tool limitations noted in two outputs but still provided actionable guidance
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `documentation-engineer`, `ui-designer`, `ux-researcher`
  - Findings: fixed medium missing Summary Statistics fallback when `scoreBreakdown` is unavailable, fixed medium/low progress and component guidance wording issues, and fixed low zero-score test scoping before rerunning affected checks and review
- Decisions (why):
  - Chose to move the existing optimizer score card rendering into Summary Statistics rather than duplicate it because the story requires replacement, not appending, and client components should stay display-only.
  - Chose to remove the separate Optimizer Score Breakdown accordion because Summary Statistics now owns the optimizer score-card layout and keeping both sections would duplicate the same user-facing metrics.
  - Chose a concise non-recomputed unavailable fallback when `scoreBreakdown` is missing so the Summary Statistics header remains stable without deriving optimizer values in the client.
  - Kept score values sourced from `generatedTeam.scoreBreakdown` and only formatted them for display because the UI must not recompute optimizer values.
  - Updated nearby `components/AGENTS.md` and consolidated `progress.txt` patterns because future AnalysisPanel work should know Summary Statistics is now the optimizer score-card section.
- **Learnings for future iterations:**
  - Tests that validate optimizer score-card layout should expand the Summary Statistics accordion, not an Optimizer Score Breakdown accordion.
  - `AnalysisPanel` keeps Summary Statistics visible for generated teams and shows a display-only unavailable fallback when `scoreBreakdown` is missing; Per-Pokemon Contribution still depends on `analysis` and `fitness`.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - Existing `favicon.ico` 404 appears in browser console/network and was not introduced by this story.
  - `.ralph-dev-server.log` remains untracked and unstaged from the existing local dev server.
- Next iteration start:
  - US-009 - Move Role after Bulk in optimizer score layout
---

## 2026-06-08 16:20 MST - US-009
- Moved the Role optimizer score card immediately after Bulk in the Summary Statistics optimizer score layout, made Role use the same standard score-card grid as Synergy/Coverage/Safety/Consistency/Bulk, and kept Offensive Ratio plus Defensive Ratio in a dedicated comparison row after Role.
- Files changed: `components/AGENTS.md`, `components/organisms/AnalysisPanel/AnalysisPanel.tsx`, `components/organisms/AnalysisPanel/AnalysisPanel.test.tsx`, `progress.txt`, `prd.json`
- Commit message: `feat: US-009 - Move Role after Bulk in optimizer score layout`
- Checks:
  - `npx vitest run components/organisms/AnalysisPanel/AnalysisPanel.test.tsx` (fail before implementation for expected Role placement, pass after implementation)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
  - Browser verification with `dev-browser` at desktop and mobile-width viewport (pass; Summary Statistics expanded with Role immediately after Bulk, Role matching standard card width/layout, and Offensive Ratio plus Defensive Ratio paired after Role; `/api/pokemon-list`, `/api/generate-team`, and `/api/team-details` returned 200; existing 404 resource error observed)
- Implementation agents:
  - Recommended: `frontend-developer`, `ui-designer`
  - Used: `frontend-developer`, `ui-designer`; each returned concise implementation or design guidance through the Task mechanism, with local tool limitations noted in their outputs but still provided actionable guidance
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `documentation-engineer`, `ui-designer`, `ux-researcher`
  - Findings: fixed low grid-specific test coverage gap and low progress metadata timing issue before second review pass; deferred low scoped-commit-format suggestion because Ralph explicitly requires `feat: US-009 - Move Role after Bulk in optimizer score layout`
- Decisions (why):
  - Chose to include Role in the primary optimizer score-card grid because US-009 requires Role to use the same width and layout treatment as the other standard cards.
  - Kept Offensive Ratio and Defensive Ratio in a dedicated two-card grid after Role because they remain a side-by-side comparison pair and should not depend on parent grid auto-placement.
  - Updated nearby `components/AGENTS.md` and consolidated `progress.txt` patterns because future Summary Statistics layout changes should preserve Role-after-Bulk and ratio-pair behavior.
  - Kept the Ralph-required unscoped commit message because the user instruction for this autonomous iteration is more specific than the repository's general scoped commit convention.
- **Learnings for future iterations:**
  - `AnalysisPanel` Summary Statistics optimizer score order is now `Synergy`, `Coverage`, `Safety`, `Consistency`, `Bulk`, `Role`, `Offensive Ratio`, then `Defensive Ratio`.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - Existing 404 resource error appears in browser console/network and was not introduced by this story.
  - `.ralph-dev-server.log` remains untracked and unstaged from the existing local dev server.
- Next iteration start:
  - US-010 - Place Replacement Risk below Pokemon name
---

## 2026-06-08 16:31 MST - US-010
- Moved each Per-Pokemon Contribution Replacement Risk pill into a dedicated row below the Pokemon name, preserved the existing contribution metric boxes, and added structural AnalysisPanel coverage for the new header layout.
- Files changed: `components/AGENTS.md`, `components/organisms/AnalysisPanel/AnalysisPanel.tsx`, `components/organisms/AnalysisPanel/AnalysisPanel.test.tsx`, `progress.txt`, `prd.json`
- Commit message: `feat: US-010 - Place Replacement Risk below Pokemon name`
- Checks:
  - `npx vitest run components/organisms/AnalysisPanel/AnalysisPanel.test.tsx` (fail before implementation for missing dedicated rows, pass after implementation)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
  - Browser verification with `dev-browser` at desktop and mobile-width viewport (pass; Per-Pokemon Contribution expanded with Replacement Risk rows below Pokemon names, no row overlap, and metric boxes visible; `/api/pokemon-list`, `/api/generate-team`, and `/api/team-details` returned 200; existing generic 404 resource error observed)
- Implementation agents:
  - Recommended: `frontend-developer`, `ui-designer`
  - Used: `frontend-developer`, `ui-designer`; each returned concise implementation or design guidance through the Task mechanism, with local tool limitations noted in their outputs but still provided actionable guidance
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `documentation-engineer`, `ui-designer`, `ux-researcher`
  - Findings: fixed low QA gap by asserting every contribution card has dedicated name/risk rows; fixed low pending progress review placeholders before rerunning affected checks and review
- Decisions (why):
  - Chose a simple vertical header stack over responsive flex wrapping because the story requires Replacement Risk to stay below the Pokemon name at all viewport widths rather than only after wrapping.
  - Added structural test ids for the name and risk rows so regression coverage verifies row separation without brittle Tailwind class assertions.
  - Updated nearby `components/AGENTS.md` and consolidated `progress.txt` patterns because future Per-Pokemon Contribution work should preserve the dedicated risk row.
- **Learnings for future iterations:**
  - Per-Pokemon Contribution header tests can assert the name row precedes the risk row with `compareDocumentPosition` while separately asserting metric boxes remain visible.
- Known issues / follow-ups:
  - Existing generic 404 resource error appears in browser console and was not introduced by this story.
  - `.ralph-dev-server.log` remains untracked and unstaged from the existing local dev server.
- Next iteration start:
  - All current stories expected complete after final PRD metadata update.
---

## 2026-06-10 01:23 MST - US-011
- Implemented PlayPokemon lineup enumeration for every ordered lead, switch, and closer assignment, producing 120 unique six-roster lineups while preserving roster input order and validation for duplicate, empty, and delimiter-containing species ids.
- Files changed: `lib/AGENTS.md`, `lib/genetic/algorithm.test.ts`, `lib/genetic/fitness/index.test.ts`, `lib/genetic/fitness/lineupEnumeration.ts`, `lib/genetic/fitness/lineupEnumeration.test.ts`, `lib/genetic/fitness/recommendations.ts`, `lib/genetic/fitness/rosterScoring.ts`, `lib/genetic/fitness/rosterScoring.test.ts`, `progress.txt`
- Commit message: `feat: US-011 - Enumerate ordered PlayPokemon switch and closer assignments`
- Checks:
  - `npx vitest run lib/genetic/fitness/lineupEnumeration.test.ts` (fail before implementation for expected 120-lineup behavior, pass after implementation)
  - `npx vitest run lib/genetic/fitness/rosterScoring.test.ts lib/genetic/fitness/index.test.ts lib/genetic/algorithm.test.ts lib/genetic/fitness/recommendations.test.ts` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
- Implementation agents:
  - Recommended: `backend-developer`, `gbl-optimizer`
  - Used: `backend-developer`; `gbl-optimizer` skill loaded and required optimizer docs read, with optimizer review guidance gathered through a general subagent because `gbl-optimizer` is a skill, not an available Task subagent type
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`
  - Findings: none; both reviewers returned empty actionable findings lists with residual risk limited to not rerunning the already completed checks during review
- Decisions (why):
  - Chose ordered switch/closer permutation over canonical backline sorting because PvPoke Switch and Closer rankings represent different tactical jobs, and the story explicitly requires role-order-sensitive scoring.
  - Kept lineup keys unchanged as `lead|switch|closer` because they already encode role order and remain unique once swapped switch/closer assignments are emitted.
  - Updated the roster synergy viable-lineup normalization from 60 to 120 so doubled enumeration capacity does not saturate the viable-lineup component too early.
  - Updated `lib/AGENTS.md` because previous local guidance still instructed future agents to canonicalize backline pairs and avoid all 60-lineup re-enumeration.
- **Learnings for future iterations:**
  - PlayPokemon lineup enumeration now yields 20 ordered switch/closer assignments per lead and 120 total lineups per valid six-Pokemon roster.
  - Tests that assert roster scoring counts should expect 120 evaluated lineups for PlayPokemon and should include swapped switch/closer identities when changing enumeration.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - `prd.json` was already dirty relative to `HEAD` for later failing-story metadata; leave unrelated `US-012` PRD state unstaged for the next iteration.
- Next iteration start:
  - US-012 - Return recomputed full optimizer fitness
---

## 2026-06-10 01:34 MST - US-012
- Implemented final optimizer fitness synchronization so PlayPokemon generated teams return recomputed full roster fitness, GBL generated teams return the final lineup recommendation score, and multi-team sorting uses those final returned fitness values.
- Files changed: `lib/AGENTS.md`, `lib/genetic/algorithm.ts`, `lib/genetic/algorithm.test.ts`, `progress.txt`
- Commit message: `feat: US-012 - Return recomputed full optimizer fitness`
- Checks:
  - `npx vitest run lib/genetic/algorithm.test.ts` (fail before implementation for expected stale-fitness regressions, pass after implementation)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
- Implementation agents:
  - Recommended: `backend-developer`, `api-designer`
  - Used: `backend-developer`, `api-designer`; both returned concise guidance through the Task mechanism, with local tool limitations noted in their outputs but still provided actionable implementation and contract guidance
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`, `documentation-engineer`
  - Findings: fixed low pending review placeholders and stale second-pass wording; deferred low scoped-commit-format suggestion because Ralph explicitly requires `feat: US-012 - Return recomputed full optimizer fitness`; second review pass returned no code or QA findings
- Decisions (why):
  - Chose to set PlayPokemon final `fitness` from `rosterScore.fitness` because that is the full diagnostics pass attached to the returned chromosome and its `scoreBreakdown.score`.
  - Chose to set GBL final `fitness` from `recommendedLineup.scoreBreakdown?.score ?? recommendedLineup.score` because the final recommendation path can use a different scoring context than the hot-path GA evaluation and should describe the same final displayed score.
  - Left `/api/generate-team` unchanged because it already passes through `result.fitness` and `result.scoreBreakdown` without recomputing scores.
  - Added algorithm-level regression coverage rather than route-level coverage because the API contract is a pass-through and the inconsistency originated in `lib/genetic/algorithm.ts`.
- **Learnings for future iterations:**
  - `generateMultipleTeams(...)` sorts by returned `Chromosome.fitness`, so final single-team mapping must synchronize fitness before the sort can be trusted.
  - GBL final recommendation scoring can differ from the hot-path GA score because the final recommendation path builds diagnostics after GA selection.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - None.
- Next iteration start:
  - All current stories expected complete after final PRD metadata update.
---
