## Codebase Patterns
- Team Analysis Summary UI lives primarily in `components/organisms/AnalysisPanel/AnalysisPanel.tsx`; keep client components display-only and do not recompute optimizer scoring in the UI.
- Threat Score diagnostics are exposed through `OptimizerScoreBreakdown.threatScore`; keep backend fields such as rank, team answers, and threat value available for sorting and tests, but do not show those raw details in the user-facing Threat Score list for this PRD.
- Threat Score UI should show readable team weaknesses and one team-level threat profile pill; raw Overall, Top Meta, Full Meta, Risk, Rank, and Answers numbers should not be user-facing in the Threat Score card.
- Recommended Lineups belongs in the `AnalysisPanel` accordion immediately after Summary Statistics; keep Lead, Switch, Closer, and semantic weakness lists, but do not render weak/neutral/strong/elite quality pills for this PRD.
- Summary Statistics optimizer score cards should render only simple `A`, `B`, `C`, `D`, or `F` letter grades for score components; do not render numeric optimizer component scores, plus/minus modifiers, or quality pills in the component cards.
- Optimizer generation changes must use format-specific ranking data from `lib/data/rankings.ts`; do not hardcode open Great League score thresholds as global viability bands.
- Dynamic candidate bands should be derived per format from score distribution, rank percentile, score density, score dropoffs, and bounded candidate counts.
- Use `deriveCandidateRankingBands(...)` in `lib/data/candidateRankingBands.ts` for pure ranking-band logic and `getCandidateRankingBands(...)` in `lib/data/rankings.ts` for format-scoped overall ranking data; do not hardcode PvPoke scores like 92, 90, 88, or 85 as global viability bands.
- Use `buildCandidateProfiles(...)` in `lib/data/candidateProfiles.ts` to combine dynamic band assignments with injected species, role, consistency, move typing, and simulation coverage inputs; keep candidate profiles pure and avoid file reads in generation or scoring code.
- Use `rankAnchorCompanionPairs(...)` in `lib/data/companionPairRanking.ts` to evaluate pure anchor plus companion candidate-profile pairs; keep rank and PvPoke score as priors while simulation coverage, unique anchor-loss coverage, safety, consistency, bulk, typing, and shared weakness penalties drive the pair score.
- Anchor-first generation should seed elite or preferred anchors, rank anchor plus companion pairs, expand teams by remaining weaknesses and lineup quality, then rely on canonical lineup or roster scoring for final fitness.
- Low-ranked specialists should be admitted only with unique measurable top-meta or core-weakness coverage and should not crowd out safer high-ranked generalists without matchup evidence.
- Runtime app, component, and optimizer code must not import, require, bundle, or load PvPoke vendor JavaScript; keep local PvPoke engine execution isolated to sync tooling.
- For optimizer-adjacent changes, invoke/read the `gbl-optimizer` skill and record optimizer docs read in this file before marking the story complete.
- Optimizer docs to read for this PRD: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.

## Iteration Log
- Next iteration start: US-001 - Derive dynamic candidate ranking bands per format

## 2026-06-13 02:32 MST - US-001
- Implemented a pure dynamic candidate ranking-band helper that derives elite anchors, preferred anchors, normal companions, flexible companions, and specialists from ranking score distribution, rank percentile, density, score dropoffs, and bounded candidate counts.
- Added a format-scoped `getCandidateRankingBands(...)` wrapper over existing overall ranking loaders.
- Added tests for dense open Great League-style curves, steeper naic2026-style limited-meta curves, and shifted-score fixtures that reject global fixed score thresholds.
- Files changed: `lib/data/candidateRankingBands.ts`, `lib/data/candidateRankingBands.test.ts`, `lib/data/rankings.ts`, `lib/AGENTS.md`, `progress.txt`
- Commit message: `feat: US-001 - Derive dynamic candidate ranking bands per format`
- Checks:
  - `npx vitest run lib/data/candidateRankingBands.test.ts` (pass; red first failed because `./candidateRankingBands` did not exist)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass; 62 files, 486 tests)
- Implementation agents:
  - Recommended: `gbl-optimizer`, `backend-developer`, `test-driven-development`
  - Used: `gbl-optimizer` skill and optimizer docs; `backend-developer` guidance via Task; `test-driven-development` skill and `test-automator` guidance as the available TDD implementation advisor
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`
  - Findings: fixed before commit; added small-pool feasible boundary handling and same-size steep-vs-dense test coverage so limited-meta behavior cannot pass due only to fixture size; second review pass returned no actionable findings
- Decisions (why):
  - Added the helper under `lib/data` because it derives runtime candidate bands from already-loaded ranking data and belongs near ranking loader boundaries, while remaining pure and testable.
  - Returned assignments, grouped bands, summaries, and score cutoffs so future candidate-profile and generation stories can consume labels without re-deriving thresholds or reading files directly.
  - Used distribution-relative candidate counts and large-gap detection instead of absolute PvPoke score thresholds so limited cups and open formats adapt independently.
  - Added a small-pool boundary fallback because limited ranking exports can have fewer candidates than the default minimum allocation across five bands.
  - Added a `lib/AGENTS.md` pattern because future optimizer work should reuse this helper and avoid reintroducing global fixed ranking cutoffs.
- **Learnings for future iterations:**
  - `RankedPokemon` does not include an explicit rank field; derive 1-based rank after sorting by descending `Score` while preserving input order for equal scores.
  - `lib/data/rankings.ts` is the correct format-aware boundary for overall ranking data; pure scoring or generation helpers should accept loaded rankings instead of reading CSVs.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - US-002 should build candidate profiles on top of these band assignments rather than reading ranking CSVs directly.
- Next iteration start:
  - US-002 - Build ranked anchor and companion candidate profiles
---

## 2026-06-13 02:44 MST - US-002
- Implemented a pure candidate profile builder that converts dynamic ranking-band assignments into ranked candidate profiles with PvPoke rank, score, dynamic band, optional safety/switch/consistency signals, bulk/stat product, offensive typing, defensive typing, and injected simulation coverage.
- Added fixture-based tests for open Great League-style and NAIC-style ranking curves, plus explicit missing optional signal handling that avoids invalid numeric output.
- Files changed: `lib/data/candidateProfiles.ts`, `lib/data/candidateProfiles.test.ts`, `lib/AGENTS.md`, `progress.txt`
- Commit message: `feat: US-002 - Build ranked anchor and companion candidate profiles`
- Checks:
  - `npx vitest run lib/data/candidateProfiles.test.ts` (pass; red first failed because `./candidateProfiles` did not exist)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass after fixing new test import order)
  - `npm test` (pass; 63 files, 490 tests)
- Implementation agents:
  - Recommended: `gbl-optimizer`, `backend-developer`, `test-driven-development`
  - Used: `gbl-optimizer` skill and optimizer docs; `backend-developer` guidance via Task; `test-driven-development` skill and `test-automator` guidance as the available TDD implementation advisor
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`
  - Findings: fixed before commit; returned fresh empty simulation coverage arrays per profile, flagged partial move-type resolution through `missingInputs`, computed bulk independently from stat product, and marked unavailable fallback bulk; final pass had no critical/high/medium findings
- Decisions (why):
  - Kept `buildCandidateProfiles(...)` in `lib/data` because it transforms already-loaded ranking and metadata inputs into normalized candidate data without entering optimizer scoring hot paths.
  - Used injected maps for species ids, role/consistency ranking signals, move-type resolution, and simulation coverage so profile construction remains pure and format-scoped by its caller.
  - Represented unavailable optional inputs as `{ available: false }`, `null`, empty coverage arrays, and `missingInputs` entries instead of misleading zero scores or invalid numeric values.
- **Learnings for future iterations:**
  - Candidate profile consumers should read `missingInputs` before treating optional role, consistency, or coverage data as available.
  - Offensive typing is derived from selected ranking moves through injected move-name-to-type data; defensive typing comes directly from ranking type columns.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - Low-priority review suggestions for future hardening: reject non-positive injected ranking-signal ranks and add a defensive-typing missing-input marker for malformed ranking rows.
  - US-003 should consume candidate profiles for pair ranking rather than recomputing ranking, typing, role, or coverage signals.
- Next iteration start:
  - US-003 - Rank anchor plus companion pairs
---

## 2026-06-13 03:01 MST - US-003
- Implemented a pure companion pair-ranking module that scores anchor plus companion profiles using PvPoke rank and score as priors, simulation coverage of anchor losses, unique measurable coverage, safety, consistency, bulk, offensive typing, defensive typing, missing-input penalties, and severe shared-weakness penalties.
- Added focused tests for high-ranked synergistic companions, lower-ranked specialists with unique measurable coverage, severe shared weakness demotion, dual-type resistance cancellation, threat-typed offensive coverage, neutral missing defensive typing, missing optional inputs, empty candidate lists, peer-group unique coverage, and deterministic tie-break ordering.
- Files changed: `lib/data/companionPairRanking.ts`, `lib/data/companionPairRanking.test.ts`, `lib/AGENTS.md`, `progress.txt`
- Commit message: `feat: US-003 - Rank anchor plus companion pairs`
- Checks:
  - `npx vitest run lib/data/companionPairRanking.test.ts` (pass; red first failed because `./companionPairRanking` did not exist)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass; 64 files, 498 tests)
- Implementation agents:
  - Recommended: `gbl-optimizer`, `backend-developer`, `test-driven-development`
  - Used: `gbl-optimizer` skill and optimizer docs; `backend-developer` Task guidance; `test-driven-development` skill and `test-automator` Task guidance as the available TDD implementation advisor
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`
  - Findings: fixed before commit; changed shared-weakness scoring to use the canonical type chart with dual-type resistance cancellation, added finite missing-input plus empty-candidate coverage, made same-rank unique-coverage ordering deterministic by peer grouping, changed offensive typing to score against injected anchor-loss threat typings instead of the anchor's own typing, and treated missing defensive typing as neutral instead of safest-possible
- Decisions (why):
  - Added the pair scorer under `lib/data` because it consumes normalized `CandidateProfile` values from data boundaries and prepares anchor-first generation inputs without reading ranking, simulation, or PvPoke files directly.
  - Kept PvPoke rank and score as a bounded prior instead of a tier-list gate so a lower-ranked specialist can outrank a generalist only with unique measurable anchor-loss coverage.
  - Used deterministic score and tie-break output with an explainable breakdown so later team expansion can inspect why a companion ranked highly before canonical lineup scoring.
  - Reused the canonical type-effectiveness helper instead of maintaining a second local chart so pair typing respects Pokemon GO dual-type multiplier cancellation.
  - Kept offensive typing neutral unless threat defensive typings are injected because anti-anchor move pressure is not useful pair synergy evidence.
  - Treated candidates tied on rank and score as peers for unique coverage so species-id ordering remains only a final deterministic tie-breaker, not a semantic score input.
- **Learnings for future iterations:**
  - Pair ranking should use `CandidateProfile.simulationCoverage` first for anchor-loss coverage and use typing only as a supporting signal.
  - Offensive typing pair signals require injected threat defensive typings on `importantLosses`; without threat typing, the pair scorer leaves offensive typing neutral rather than inferring coverage from the anchor's own types.
  - Missing defensive typing should be scored as neutral and left to `missingInputs` penalties; it should not look safer than fully typed profiles with real weaknesses.
  - Default anchor-loss weights preserve input order as a priority hint when no explicit threat weights are injected.
  - Optimizer docs read for this iteration: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, `docs/team-optimization/validation.md`, and `.opencode/skills/gbl-optimizer/SKILL.md`.
- Known issues / follow-ups:
  - US-004 should apply specialist gating on top of pair-ranking output so rare niche picks remain bounded and justified by unresolved top-meta or core-weakness coverage.
- Next iteration start:
  - US-004 - Gate specialist candidates behind unique coverage
---
