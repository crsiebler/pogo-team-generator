## Codebase Patterns
- Battle Frontier Master cycle point data lives in `data/battle-frontier-master-points.csv` with a `speciesId,points` header and should stay aligned with the active bundled PvPoke cup tier rules while using canonical ids from `data/pokemon.json`.
- Battle Frontier Master shadow point inheritance belongs in code, not the CSV: inherit a listed base species cost only when the shadow variant exists in `data/pokemon.json` and is ranked for `battle-frontier-master`.
- In `app/api/generate-team/route.ts`, validate Battle Frontier Master anchor legality after ranked-species and uniqueness checks but before `generateTeam(...)`, and map the first legality violation to a deterministic HTTP 400 message.
- In `lib/genetic/chromosome.ts`, enforce Battle Frontier Master initialization legality while filling each slot and in the random fallback path, then keep a final legality assertion on the completed team so later operator work can reuse the same pattern.

## Iteration Log
## 2026-04-14 17:08:04 MST - US-001
- What was implemented
- Added the checked-in Battle Frontier Master cycle point CSV and a regression test that validates its canonical ids and current-cycle contents against the bundled cup tier rules.
- Files changed: `data/battle-frontier-master-points.csv`, `lib/data/battleFrontierMasterPoints.test.ts`, `lib/AGENTS.md`, `prd.json`, `progress.txt`
- Commit: `cf64c32`
- Checks:
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
- Decisions (why):
  - Used the bundled PvPoke `battlefrontiermaster` cup definition as the source for the CSV so the checked-in data matches the active cycle without inventing a separate manual source.
  - Added a focused CSV regression test instead of production loader code because this iteration only covers storing validated cycle data, not rule evaluation.
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
  - Battle Frontier Master cycle point data should stay as a flat `speciesId,points` CSV so later rules helpers can load it without format-specific parsing.
  - Keep Battle Frontier Master point ids canonical to `data/pokemon.json`; the current cycle already matches bundled PvPoke ids.
  - Useful context (e.g., "the evaluation panel is in component X")
  - The active cycle tier list already exists in `vendor/pvpoke/src/data/gamemaster/cups/battlefrontiermaster.json`, which is a reliable comparison source for test coverage.
- Known issues / follow-ups:
  - `progress.txt` commit hash placeholder needs to match the final git commit created in this iteration.
  - US-002 still needs the runtime rules helper that loads this CSV and applies shadow inheritance.
- Next iteration start:
- Implement `US-002` by adding a cached Battle Frontier Master points helper in `lib/` that reads this CSV and resolves exact and inherited shadow costs.
---
## 2026-04-15 11:10:00 MST - US-004
- What was implemented
- Kept Battle Frontier Master random chromosome initialization legal by rejecting over-budget, extra-5-point, and extra-Mega candidates during both scored selection and fallback selection, with a final legality assertion before returning the chromosome.
- Files changed: `lib/genetic/chromosome.ts`, `lib/genetic/chromosome.test.ts`, `lib/AGENTS.md`, `prd.json`, `progress.txt`
- Commit: `<pending>`
- Checks:
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
- Decisions (why):
  - Reused `getBattleFrontierMasterTeamLegality(...)` inside `createRandomChromosome(...)` so initialization follows the same legality rules already used by the API instead of duplicating point, Mega, and 5-point logic.
  - Applied the legality filter in both the scored candidate loop and the fallback loop because the fallback path can otherwise bypass format constraints when the diversity scorer cannot find a candidate quickly.
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
  - `lib/genetic/chromosome.test.ts` is the focused unit-test entry point for initialization behavior, so Battle Frontier Master generator coverage can stay isolated there with mocked data helpers instead of loading full rankings or simulations.
  - Useful context (e.g., "the evaluation panel is in component X")
  - `createRandomChromosome(...)` now enforces Battle Frontier Master legality during initial team construction only; mutation and crossover still need equivalent guardrails in `lib/genetic/operators.ts` for `US-005`.
- Known issues / follow-ups:
  - `progress.txt` commit hash placeholder needs to match the final git commit created in this iteration.
  - `US-005` still needs mutation and crossover legality preservation so evolved Battle Frontier Master teams cannot drift into illegal states after initialization.
- Next iteration start:
  - Update `lib/genetic/operators.ts` so crossover and mutation reject or repair illegal Battle Frontier Master children using the shared legality helper.
---
## 2026-04-14 17:14:29 MST - US-002
- What was implemented
- Added a cached Battle Frontier Master rules helper that loads the checked-in points CSV, resolves exact and inherited shadow costs, detects Mega species from structured Pokemon tags, and evaluates team legality for points, 5-point, and Mega limits.
- Files changed: `lib/data/battleFrontierMasterRules.ts`, `lib/data/battleFrontierMasterRules.test.ts`, `lib/AGENTS.md`, `prd.json`, `progress.txt`
- Commit: `e924968`
- Checks:
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
- Decisions (why):
  - Reused `getRankedSpeciesIds('battle-frontier-master')` to decide which shadow forms inherit costs so the helper follows active format rankings instead of assuming every `_shadow` form should count.
  - Used Pokemon `tags` from `data/pokemon.json` for Mega detection so legality checks stay tied to structured gamemaster data instead of brittle naming heuristics.
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
  - `lib/data` runtime helpers typically lazy-load checked-in data and cache parsed results per format or feature, which keeps file I/O centralized and deterministic.
  - Battle Frontier Master legality can treat missing point entries as zero-cost species while still enforcing point and Mega rules through one shared helper.
  - Useful context (e.g., "the evaluation panel is in component X")
  - `getBattleFrontierMasterTeamLegality(...)` now provides the reusable legality summary shape for upcoming API, generator, and UI wiring work.
- Known issues / follow-ups:
  - `progress.txt` commit hash placeholder needs to match the final git commit created in this iteration.
  - US-003 still needs API-level validation to turn legality violations into deterministic HTTP 400 errors.
- Next iteration start:
  - Wire `getBattleFrontierMasterTeamLegality(...)` into `app/api/generate-team/route.ts` so illegal Battle Frontier Master anchors are rejected before generation.
---
## 2026-04-14 17:18:09 MST - US-003
- What was implemented
- Wired Battle Frontier Master anchor validation into `POST /api/generate-team` so illegal anchor combinations fail fast with deterministic 400 errors for point-cap, 5-point, and Mega violations before team generation starts.
- Files changed: `app/api/generate-team/route.ts`, `app/api/generate-team/route.test.ts`, `prd.json`, `progress.txt`
- Commit: `<pending>`
- Checks:
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass)
- Decisions (why):
  - Reused `getBattleFrontierMasterTeamLegality(...)` in the route instead of duplicating rule checks so the API stays aligned with the shared format rules module used by later generator and UI work.
  - Returned the first legality violation as the API error so responses stay deterministic and easy for tests and UI handling to match.
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
  - `app/api/generate-team/route.test.ts` is the contract test for request validation, so new format-specific request guards should be added there with mocked `lib/` helpers rather than pulling real data into the route adapter tests.
  - Useful context (e.g., "the evaluation panel is in component X")
  - Battle Frontier Master API validation currently happens only on anchors, before `generateTeam(...)`; later generator stories still need legality enforcement during random initialization, mutation, and crossover.
- Known issues / follow-ups:
  - `progress.txt` commit hash placeholder needs to match the final git commit created in this iteration.
  - US-004 still needs generator initialization to avoid building illegal Battle Frontier Master teams without relying on API anchor validation.
- Next iteration start:
  - Update initial random chromosome construction in `lib/genetic/algorithm.ts` and related helpers so Battle Frontier Master teams start within the point, 5-point, and Mega limits.
---
