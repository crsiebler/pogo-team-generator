## Codebase Patterns
- Default-only simulation publication projects each ranking-derived candidate set to exactly one ranked default before both simulation and manifest preparation, fails before simulation when a synchronized species lacks that authority, rejects prepared CSVs not declared by the prepared manifests, and rechecks stale cleanup targets as regular files immediately before unlink; explicit variant mode retains the bounded full candidate sets.
- Moveset variant simulation is opt-in across sync tooling: normalize omitted `includeMovesetVariants` to `false` at the simulation boundary, expose variants only through `--moveset-variants`, keep projection read-only while reporting the selected mode, and reject unknown CLI arguments before any sync work starts.
- `RosterMovesetAssignment` authority is per species in `authorityBySpeciesId`; its keys must exactly match the canonical assigned roster and variant map, every authority belongs in the fingerprint, manifest defaults use qualified simulation lookups, and ranked-default fallbacks remain unqualified through `getAssignedMovesetVariantId(...)`.
- Returned assignments require two gates before projection: structural parsing/fingerprint validation, then repository-authority validation. Prepare compact snapshots only when a species claims manifest authority, require exact policy and complete active-variant equality, compare fallback species with current format rankings, and propagate snapshot integrity failures without fallback.
- Variant-enabled resolution may use a per-species ranked default only when `MovesetVariantSimulationDataError` is an unqualified `variant-unavailable` failure matching the current format and canonical species; singleton fallback slots preserve assignment bounds, while missing or corrupt authority and mismatched/specific-variant failures propagate.
- Turbopack compiles `outputFileTracingExcludes` into glob regexes and can panic when thousands of generated paths are listed individually; use narrow non-overlapping semantic exclusions for source simulation CSVs/manifests, explicitly include catalog snapshots, and let post-build trace validation reject every unauthorized data asset.
- Next.js `outputFileTracingIncludes` is additive to NFT discovery: derive exact generate-team assets plus team-details snapshots, Overall rankings, Pokemon, and moves from the battle-format catalog; use route-specific excludes for automatically over-traced files; validate both runtime route traces and their deduplicated snapshot budget; and keep imports inside `next.config.ts` build helpers relative because config transpilation does not preserve project aliases correctly.
- Route-rooted runtime dependency checks must use TypeScript syntax context to distinguish executable imports from erased type imports and exports, preserve mixed default-plus-type-only clauses and mixed named-export siblings, track global and `node:process` process owners plus Node module namespaces from namespace, named `default`/`Module`, dynamic, CommonJS, import-equals, destructured, direct/aliased/supported `call`/`apply` `process.getBuiltinModule('module')`, direct-loader, and supported `call`/`apply` loader-alias loads, track `createRequire` from both Node module specifiers through named, namespace, import-equals, CommonJS, destructured, assigned, reassigned, chained-assignment, indirect sequence, `call`/`apply`, and `module.require` alias bindings, track aliases of CommonJS and global loader owners, traverse loader-factory arguments plus every assignment/comma callee and indirect-loader receiver operand, reject computed members, unsupported destructuring assignment/parameter flows, defaults, class/parameter initializers, loop or aggregate containers, unsupported assignment operators, returned/yielded/thrown provenance, unknown call/new/tagged wrappers, recursively wrapped exports, unsupported nested static factory/loader indirection, exported provenance, and symlinked entrypoints that cannot be followed safely, follow runtime JavaScript instead of declaration-only resolutions, consistently exclude TypeScript-classified external packages from the project-local graph, fail closed on every non-literal executable dynamic import or builtin-module lookup, and validate the transitive source graph plus exact catalog data set.
- Resolve `node:process` namespaces through the same tracked loader predicate used for direct, CommonJS alias, `module.require`, destructured, supported `call`/`apply`, and literal `process.getBuiltinModule('process')` forms; apply complete namespace predicates when rejecting computed access on inline builtin-module results.
- Treat static `process.mainModule` access as unresolved CommonJS loader provenance so its `.require(...)` path cannot bypass route-rooted dependency validation.
- Thread the complete recursive process-namespace predicate through CommonJS loader recognition so fully inline builtin-process to builtin-module to `createRequire` chains, including supported `call`/`apply`, remain extractable.
- CommonJS dependency extraction must track static `globalThis`/`global` require members plus direct, static `.global`, and supported object-destructured aliases of those global loader owners; treat global owners as tracked provenance in unsupported flows; track `module.require` owner aliases and supported variable-declaration destructuring aliases; reject unknown computed access on a tracked loader owner; traverse every recognized loader argument subtree and assignment/sequence-wrapped loader-factory callee after extracting its own specifier; and preserve every validated lexical `node_modules/<package>` authority identity plus the canonical target when trusted roots, individual packages, project-targeting packages, or canonical duplicates are symlinked so vendor classification cannot be erased while canonical bytes remain deduplicated.
- Follow chained symlink targets within trusted dependency roots and retain every intermediate package identity that resolves to the same canonical traced file; canonical containment and byte accounting remain deduplicated by the final target.
- Before recursive dependency symlink traversal, project traced canonical paths through every matching lexical trusted-root alias; resolve target components physically in order so `..` applies after preceding symlinks, and retain every intermediate package identity for policy checks.
- Extract executable ESM and CommonJS module specifiers from decoded TypeScript AST literals rather than source regexes, fail closed on unresolved executable loaders, resolve local modules with parsed project compiler options, and canonicalize the project root before deriving trusted ancestor dependency roots.
- Next.js NFT trace entries resolve relative to the trace file directory; deployment-size tooling must include emitted route entrypoints, reject symbolic links in every lexical repository-data path component, allow external files only under explicit canonical ancestor `node_modules` roots that neither equal the filesystem root nor contain the project, retain every traced in-project lexical alias and any validated intermediate package-symlink authority for policy, fail closed when a project symlink erases package dependency authority, classify and count project aliases by canonical target, validate recognizable CSV, manifest, index, snapshot, Pokemon, move, and type-effectiveness evidence against exact route plans regardless of project directory, deduplicate canonical targets, and report deterministic project-relative paths and raw uncompressed bytes.
- Runtime app, component, and optimizer code must not import, require, bundle, or load PvPoke vendor JavaScript; local PvPoke execution remains isolated to sync tooling.
- Moveset variant work must read `.opencode/skills/gbl-optimizer/SKILL.md` and the required optimizer documents before changing ranking inputs, simulation data, scoring, or diagnostics.
- Required optimizer docs: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
- Implement behavioral changes test-first and record the failing and passing focused test commands in each iteration log.
- Preserve unrelated worktree changes and do not remove generated files outside the tested stale-variant cleanup boundary.
- Initialize `vendor/pvpoke` with `git submodule update --init vendor/pvpoke` before running tests that validate bundled PvPoke source data.
- Keep species ID, species display-name, and move spelling aliases in `lib/data/aliases.ts`; distinguish battle-state aliases from moveset variants and retain source provenance until canonical ranking deduplication finishes.
- Purified Return eligibility requires an exact checked-in shadow counterpart for the canonical choosable form and the non-shadow form's finite `level25CP` at or below the format cap; never infer shadow forms from tags, family, or Dex identity.
- Legacy move availability approvals are canonical species-move pairs classified as event-exclusive; exhaustive tests must map every checked-in `legacyMoves` pair to an explicit allow or exclude expectation.
- Ranking sync validates and returns `RankingSyncResult` with unaggregated source movesets, nullable fast/charged usage, per-source alias provenance, and one optional override set per supported format; keep override path knowledge and missing-file handling inside `lib/sync/adapter.ts`.
- Normalize ranking move usage per species entry and fast/charged slot before category weighting; retain exact observed movesets and overrides above usage-only evidence, then sort weighted evidence by category occurrence, exact Overall use, and move ID.
- Move-mechanics filtering may reject only strictly dominated usage-only speculation; observed and override moves remain eligible, and status effects or distinct type coverage keep moves strategically incomparable until simulation.
- Moveset derivation preserves the eligible preferred Overall set first, retains exact observed and override sets, limits expansion to two fast and four charged moves, generates only single-move substitutions around observed anchors, and emits at most eight canonical variants using the current sync Pokemon snapshot for availability.
- Invalid preferred Overall sets retain rejection evidence and the original PvPoke score prior; ranking sync derives replacements before CSV conversion, changes only Overall move columns, and omits an Overall row only when no complete eligible candidate exists.
- Emitted Overall rankings are the authoritative TeamRanker opponent pool; resume sync fingerprints each format, prunes changed format caches, and initializes canonical deduplicated targets with sanitized defaults before simulation.
- Simulation projection bypasses the destructive full sync pipeline, derives candidates with no-op persistence boundaries, projects alternates only for sanitized top-150 Overall targets with multiple candidates, and recognizes stale files only through strict canonical variant filenames and required shield scenarios.
- Moveset variant manifest parsing and canonical serialization live in `lib/data/movesetVariantManifest.ts`; source hashing and pure generated-manifest construction live in `lib/sync/movesetVariantManifest.ts`, and shared hard caps come from the data-layer manifest contract.
- Manifest validation rejects known species aliases and requires canonical uppercase ASCII move IDs instead of normalizing alternate representations, preventing parallel species storage keys and moveset variant identities in authoritative generated data.
- Simulation sync consumes ranking-derived candidate sets and sanitized top-150 target ids directly, validates candidates against the fresh Pokemon and move snapshots, keeps defaults on unqualified paths, and writes alternates only to canonical format-scoped variant paths.
- Active moveset selection compares complete finite candidate/default cells across all required scenarios and explicit top/full opponent pools using soft matchup scores; top-meta uses 70% weight, the primary must improve beyond a numerical epsilon, and every remaining complete candidate can compete for the secondary through positive marginal coverage beyond both default and primary.
- Epsilon-sensitive deterministic rankings quantize each metric independently before comparison; pairwise tolerance comparators are non-transitive and can make shuffled input change the winner.
- Parse TeamRanker opponent cells with `extractSpeciesNameFromSimulationCell(...)` before canonical lookup or opponent-set comparison because PvPoke appends compact moveset tokens after species and form names.
- Prepare every supported format manifest in memory after simulation and cross-validation succeed; production simulation generation returns validated deferred CSVs, and one rollback-capable batch stages all files, replaces manifests last, and restores every prior CSV and manifest target on failure.
- Stale simulation cleanup runs only after successful generation publication, requires physical directories to match workspace catalog paths before enumeration and each unlink, deletes only regular strict-parser variant files omitted by all manifest candidate storage keys, and reports each successful deletion immediately; default matrices and declared inactive candidates remain untouched.
- Runtime simulation availability comes only from each format manifest: build default matrices from active default candidates and exact scenario storage keys, expose alternatives from the same authority, validate finite CSV rows and candidate/default opponent parity before caching, never enumerate runtime simulation directories or filenames, and never substitute default rows for unavailable alternates.
- Resolve one deeply immutable roster moveset assignment against the complete bring-six roster before lineup enumeration; bind it into scoring contexts, normalize omitted scoring formats to the default before validation, and include its format, manifest policy identity, and canonical variants in every assignment-dependent cache fingerprint.
- Route every roster-member aggregate matchup, shield scenario, matchup-quality, and move-typing read through the bound assignment's variant ID; ranked-default transition assignments remain unqualified, and missing shield rows remain unevaluated rather than synthetic neutral results.
- Final threat analysis reuses the assignment-aware `scoreBreakdown.threatScore`; retain the exact final roster assignment for shield-scenario and Pokemon-contribution diagnostics rather than resolving or selecting movesets in analysis modules.
- Only PlayPokemon evolution uses unqualified ranked-default fast scoring; GBL evolution remains team-aware. Retain a canonical, deduplicated top-ten PlayPokemon archive across generations, preserve explicit anchor order, sort flexible roster members, and use lexical canonical keys for equal-fitness ordering before assignment-aware finalist work.
- Post-evolution PlayPokemon reranking enumerates at most `3^6` manifest assignments per finalist, caches aggregate matrix reads for a 70/30 top-meta/full-meta answer-depth prefilter, fully scores only the top twelve assignments across all 120 ordered lineups, and selects by variant-aware fitness, default-only fitness, then canonical roster key.
- Retain the exact scored roster assignment in generation output for both tournament modes; pass it unchanged through the client, bound and validate its format, canonical roster, move slots, variant identities, and fingerprint on return, and project move/acquisition details without selecting movesets in API adapters.
- Enforce public JSON payload limits by validating any declared `Content-Length` and counting bytes from the request stream; headers may be absent or inaccurate and are not an authoritative size boundary.
- Export move IDs and preferred charged-move order directly from the scored `RosterMovesetAssignment`; pass validated acquisition requirements separately as informational text and omit regular requirements.
- Memoize production lineup-context ranking, aggregate matchup, shield-scenario, and matchup-quality reads by every result-affecting input, including variant identity; keep these caches per run while retaining the complete assignment fingerprint in assignment-aware lineup cache keys.
- Validate checked-in generated variant data through the battle-format catalog: parse every manifest, check every active scenario CSV and default opponent parity, enforce move availability, and cover representative variants plus forbidden synthesized forms.
- Publish `data/simulations/runtime-asset-index.json` from reparsed manifests for every catalog format, including each manifest and only active candidate scenario paths; install it after manifests in the rollback-capable batch while retaining inactive CSVs as sync evidence.
- Compact runtime snapshots use canonical dictionaries plus little-endian `uint16` Base64 ratings in variant/opponent/scenario order, reserve `0xffff` for missing rows, overlay deferred CSV bytes before reused files, and retain every validated active CSV row because manifest `evaluationCounts` describe bounded selection pools rather than full matrix sizes.
- Prepare compact runtime snapshots once per format before optimizer context construction; keep warm matchup, active-variant, and policy lookups synchronous and snapshot-only, preserve the default variant plus source opponent iteration order, bind behavior-affecting identity to the active digest, and fail closed without manifest or CSV fallback.

## Iteration Log
- Next iteration start: US-001 - Define shared moveset contracts and canonical variant IDs

## 2026-08-05 18:56:01 MST - US-001
- Implemented shared `Moveset`, `MovesetVariant`, `MovesetVariantId`, `MoveAvailability`, and `RosterMovesetAssignment` contracts; canonicalized charged-move identity without changing preferred order; typed Elite and legacy Pokemon moves; and replaced equivalent local moveset shapes.
- Files changed: `AGENTS.md`, `lib/types.ts`, `lib/data/movesetVariants.ts`, `lib/data/movesetVariants.test.ts`, `lib/data/pokemon.test.ts`, `lib/genetic/moveset.ts`, `lib/sync/types.ts`, `progress.txt`
- Commit message: `feat: US-001 - Define shared moveset contracts and canonical variant IDs`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/data/movesetVariants.test.ts lib/data/pokemon.test.ts` (fail as expected before implementation: charged-order identity and typed Pokemon fields)
  - `npx vitest run lib/sync/simulations.test.ts lib/data/movesetVariants.test.ts lib/data/pokemon.test.ts lib/genetic/moveset.test.ts` (pass, 23 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm run build` (pass)
  - `git diff --check` (pass)
  - `npm test` (fail: 541 pass, 1 pre-existing data-fixture failure in `lib/data/simulations.test.ts` because the expected generated Golisopod alternate CSV is not checked in)
- Implementation agents:
  - Recommended: `typescript-pro`, `refactoring-specialist`
  - Used: `typescript-pro`; skipped `refactoring-specialist` because the standard-mode one-agent budget and focused contract refactor made it redundant
- Specialist review:
  - Agents used: skipped because the mandatory full test gate did not pass, so no candidate diff was staged for review
  - Findings: review not run; commit blocked before the review stabilization loop
- Decisions (why):
  - Canonicalized the charged pair in reverse lexical order because identity must be order-independent while the existing Golisopod storage ID `x_scissor--aqua_jet` must remain compatible.
  - Kept nullable UI/export moveset boundaries separate because they represent incomplete data, while the shared domain `Moveset` requires all three moves.
  - Did not generate or fabricate missing alternate simulation CSVs because complete variant-data generation and publication belong to later stories and partial production data would be unsafe.
- **Learnings for future iterations:**
  - Read `.opencode/skills/gbl-optimizer/SKILL.md`, `docs/pokemon-go-team-optimization.md`, and all required `docs/team-optimization/` references listed in Codebase Patterns.
  - Full data tests require the initialized `vendor/pvpoke` submodule.
  - `lib/data/simulations.test.ts` currently assumes a generated Golisopod alternate CSV that is absent from the checked-in `data/simulations/` tree.
- Known issues / follow-ups:
  - `US-001` remains `passes: false`; no commit was created because `npm test` is not green.
  - Resolve the missing complete Golisopod alternate simulation fixture through the authorized deterministic generation path before staging and specialist review.
- Next iteration start:
  - Resume US-001 after the required alternate simulation data is available, rerun all checks, then stage and run the standard-mode `code-reviewer` plus `qa-expert` pass.
---

## 2026-08-05 19:01:03 MST - US-001
- Completed the shared moveset contract implementation and generated the missing simulation-backed Golisopod Shadow Claw variant data for every supported format where Golisopod is ranked, preserving the existing runtime variant lookup.
- Files changed: `AGENTS.md`, `lib/AGENTS.md`, `lib/types.ts`, `lib/data/movesetVariants.ts`, `lib/data/movesetVariants.test.ts`, `lib/data/pokemon.test.ts`, `lib/data/simulations.test.ts`, `lib/genetic/moveset.ts`, `lib/sync/types.ts`, `lib/sync/validation.ts`, `lib/sync/gamemaster.test.ts`, `data/simulations/cp10000/{all,coupedusillage}/golisopod--shadow_claw--x_scissor--aqua_jet_*.csv`, `data/simulations/cp1500/{all,copadiluvio,weather}/golisopod--shadow_claw--x_scissor--aqua_jet_*.csv`, `data/simulations/cp2500/{all,ligaultra}/golisopod--shadow_claw--x_scissor--aqua_jet_*.csv`, `progress.txt`
- Commit message: `feat: US-001 - Define shared moveset contracts and canonical variant IDs`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/data/movesetVariants.test.ts lib/data/pokemon.test.ts lib/genetic/moveset.test.ts lib/sync/simulations.test.ts` (pass, 23 tests)
  - `npx tsc --noEmit` (pass)
  - `npm test` before data generation (fail as expected: 541 pass, missing generated Golisopod alternate simulation fixture)
  - `bun -e "import { generateSimulations } from './lib/sync/simulations.ts'; await generateSimulations({ resume: true, sourcePath: './vendor/pvpoke' });"` (pass; generated only missing Golisopod alternate files and reused valid defaults)
  - `npx prettier --write "lib/types.ts" "lib/data/movesetVariants.ts" "lib/data/movesetVariants.test.ts" "lib/data/pokemon.test.ts" "lib/genetic/moveset.ts" "lib/sync/types.ts"` (pass; unchanged)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 542 tests)
  - `npm run build` (pass)
  - `git diff --check` (pass)
  - `npx vitest run lib/sync/gamemaster.test.ts lib/data/simulations.test.ts` before review fixes (fail as expected: malformed supplemental move lists were accepted; generated-data completeness test passed)
  - `npx vitest run lib/sync/gamemaster.test.ts lib/data/simulations.test.ts` after review fixes (pass, 13 tests)
  - `npx prettier --write "lib/sync/validation.ts" "lib/sync/gamemaster.test.ts" "lib/data/simulations.test.ts"` (pass; unchanged)
  - `npx tsc --noEmit` after review fixes (pass)
  - `npm run lint` after review fixes (pass)
  - `npm test` after review fixes (pass, 544 tests)
  - `npm run build` after review fixes (pass)
  - `git diff --check` after review fixes (pass)
- Implementation agents:
  - Recommended: `typescript-pro`, `refactoring-specialist`
  - Used: `typescript-pro` in the initial US-001 implementation; skipped `refactoring-specialist` because the standard-mode one-agent budget and focused contract consolidation made overlapping guidance unnecessary
- Specialist review:
  - Agents used: initial `code-reviewer` and `qa-expert`; targeted `qa-expert` re-review
  - Findings: `code-reviewer` found none; fixed two medium QA findings by validating supplemental move arrays and enforcing complete generated variant scenario sets with opponent parity; targeted re-review found none
- Decisions (why):
  - Kept reverse-lexical charged-move canonicalization so charged order is identity-independent without changing the existing Golisopod storage key.
  - Generated all three Golisopod alternate shield scenarios through the existing resume-mode PvPoke sync path instead of weakening the runtime test or fabricating one matchup row.
  - Included complete generated Golisopod alternate sets for every applicable checked-in format because publishing only the single CSV required by the failing assertion would leave partial simulation data.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` skill plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references before resuming the implementation.
  - Resume-mode simulation sync validates and reuses existing default CSVs while generating missing configured alternate files.
  - Generated moveset variants require complete `0-0`, `1-1`, and `2-2` scenario sets per applicable format to keep runtime data coherent.
  - Extending `PokemonData` requires matching runtime validation and malformed-input coverage; TypeScript declarations alone do not protect synced JSON.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-001.
- Next iteration start:
  - US-002 - Centralize species and move alias normalization
---

## 2026-08-05 19:23:21 MST - US-002
- Centralized species ID, display-name, and move spelling aliases in pure shared helpers; wired runtime Pokemon/ranking loading plus ranking and simulation sync to those helpers; retained all source entries and alias-kind provenance through deterministic ranking deduplication; resolved stateful move names against canonical species movepools; and preserved non-choosable candidate filtering.
- Files changed: `lib/AGENTS.md`, `lib/data/aliases.ts`, `lib/data/aliases.test.ts`, `lib/data/pokemon.ts`, `lib/data/pokemon.test.ts`, `lib/data/rankings.ts`, `lib/data/rankings.test.ts`, `lib/sync/rankings.ts`, `lib/sync/rankings.test.ts`, `lib/sync/simulations.ts`, `progress.txt`
- Commit message: `feat: US-002 - Centralize species and move alias normalization`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/data/rankings.test.ts lib/data/pokemon.test.ts` (fail as expected before implementation: `Superpower` returned `SUPERPOWER` instead of `SUPER_POWER`)
  - `npx vitest run lib/data/aliases.test.ts lib/data/pokemon.test.ts lib/data/rankings.test.ts lib/sync/rankings.test.ts lib/sync/simulations.test.ts` (pass, 46 tests)
  - `npx vitest run lib/data/rankings.test.ts lib/sync/rankings.test.ts` before review fixes (fail as expected: stateful runtime move IDs were context-free and ranking normalization was not exported/provenance-complete)
  - `npx vitest run lib/data/rankings.test.ts lib/sync/rankings.test.ts` after review fixes (pass, 17 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` before import-order fix (fail: one `import/order` error in `lib/data/rankings.ts`)
  - `npm run lint` after import-order fix (pass)
  - `npm test` after review fixes (pass, 564 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `typescript-pro`, `refactoring-specialist`
  - Used: `typescript-pro`; skipped `refactoring-specialist` because the standard-mode one-agent budget and small pure-helper consolidation made overlapping refactor guidance unnecessary
- Specialist review:
  - Agents used: initial `code-reviewer` and `qa-expert`; targeted `code-reviewer` and `qa-expert` re-review
  - Findings: fixed two medium findings by resolving runtime moves against canonical species movepools and retaining all source entries with deterministic canonical-preferred tie-breaking; targeted re-review found none
- Decisions (why):
  - Classified aliases as `battle-state` or `moveset-variant` so Morpeko and Aegislash source forms remain distinguishable from ordinary alternate-moveset pseudo-forms.
  - Kept existing Pokemon normalization exports available from `lib/data/pokemon.ts` while moving the source of truth to `lib/data/aliases.ts` to avoid unnecessary consumer churn.
  - Normalized only the known `SUPERPOWER` and `VISE_GRIP` spelling differences; stateful form-specific move IDs remain untouched.
  - Preferred canonical source entries on equal scores, then used stable source keys, while retaining every colliding source record so input order cannot change deduplication output or erase pseudo-form evidence.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` and `test-driven-development` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Runtime ranking conversion previously drifted from simulation sync and emitted non-canonical `SUPERPOWER` and `VISE_GRIP` IDs.
  - Ranking entries carry every original source record plus selected source species ID, display name, and alias kind after canonicalization, allowing later evidence collection to preserve pseudo-form provenance.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-002.
- Next iteration start:
  - US-003 - Implement regular, Elite, purified, and Frustration availability rules
---

## 2026-08-05 19:45:40 MST - US-003
- Implemented deterministic move availability classification for regular, Elite, purified, excluded legacy, and unavailable moves; rejected Frustration unconditionally; required exact checked-in shadow counterparts and valid purified level-25 CP floors for Return; and exposed per-slot moveset acquisition requirements without changing battle scores.
- Files changed: `lib/AGENTS.md`, `lib/types.ts`, `lib/data/moveAvailability.ts`, `lib/data/moveAvailability.test.ts`, `progress.txt`
- Commit message: `feat: US-003 - Implement regular, Elite, purified, and Frustration availability rules`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/data/moveAvailability.test.ts` (fail as expected before implementation: six availability classifications returned the excluded placeholder)
  - `npx vitest run lib/data/moveAvailability.test.ts` (pass, 10 tests)
  - `npx vitest run lib/data/moveAvailability.test.ts lib/data/pokemon.test.ts` (pass, 18 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` before import-order fix (fail: one `import/order` error in `lib/data/moveAvailability.ts`)
  - `npm run lint` after import-order fix (pass)
  - `npm test` (pass, 574 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/data/moveAvailability.test.ts` after review fix (pass, 11 tests)
  - `npx tsc --noEmit` after review fix (pass)
  - `npm run lint` after review fix (pass)
  - `git diff --check` after review fix (pass)
- Implementation agents:
  - Recommended: `typescript-pro`, `test-automator`
  - Used: `typescript-pro`; skipped `test-automator` because the standard-mode one-agent budget and focused pure-function test matrix made a second overlapping implementation advisor unnecessary
- Specialist review:
  - Agents used: initial `code-reviewer` and `qa-expert`; targeted `qa-expert` re-review
  - Findings: `code-reviewer` found none; fixed one medium QA finding by covering the legal exact-cap purified CP boundary; targeted re-review found none
- Decisions (why):
  - Classified Elite moves before ordinary movepool membership and unapproved legacy moves before regular moves because checked-in supplemental moves can also appear in general move arrays.
  - Required an exact `${canonicalSpeciesId}_shadow` record rather than tags, family, or Dex matching so regional, Mega, and unavailable shadow forms cannot be synthesized.
  - Left species-scoped legacy moves excluded for US-004 rather than introducing that story's allowlist or denylist early.
  - Added optional `level25CP` to the runtime Pokemon contract and failed Return eligibility closed when it is absent or invalid because not every checked-in Pokemon record has a purified CP floor.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` and `test-driven-development` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - `eliteMoves` and `legacyMoves` can overlap `fastMoves` or `chargedMoves`, so availability rule order is part of the domain policy.
  - Purified CP eligibility uses the canonical non-shadow Pokemon's `level25CP`; the shadow record proves availability but does not supply the floor.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-003.
  - US-004 must replace the provisional legacy exclusion branch with explicit species-move-pair policy.
- Next iteration start:
  - US-004 - Implement species-scoped legacy move policy
---

## 2026-08-05 19:59:34 MST - US-004
- Implemented a canonical species-move-pair legacy policy that allows the approved Counter and signature moves as event-exclusive while retaining explicit exclusions for accidental historical moves; added exhaustive coverage for all 20 checked-in legacy pairs and nearby-form rejection regressions.
- Files changed: `lib/AGENTS.md`, `lib/data/moveAvailability.ts`, `lib/data/moveAvailability.test.ts`, `prd.json`, `progress.txt`
- Commit message: `feat: US-004 - Implement species-scoped legacy move policy`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/data/moveAvailability.test.ts` (fail as expected before implementation: 9 approved pairs remained excluded)
  - `npx vitest run lib/data/moveAvailability.test.ts` (pass, 32 tests)
  - `npx prettier --write "lib/data/moveAvailability.ts" "lib/data/moveAvailability.test.ts"` (pass; unchanged)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 596 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/data/moveAvailability.test.ts` after review fix (pass, 33 tests)
  - `npx tsc --noEmit` after review fix (pass)
  - `npm run lint` after review fix (pass)
  - `npm test` after review fix (pass, 597 tests)
  - `npm run build` after review fix (pass; existing multiple-lockfile workspace-root warning only)
- Implementation agents:
  - Recommended: `typescript-pro`, `test-automator`
  - Used: `typescript-pro`; skipped `test-automator` because the standard-mode one-agent budget and focused pure-function test matrix made a second overlapping advisor unnecessary
- Specialist review:
  - Agents used: initial `code-reviewer` and `qa-expert`; targeted `qa-expert` re-review
  - Findings: `code-reviewer` found none; fixed one medium QA finding by directly testing the pure pair predicate against the same approved move IDs on unapproved species/forms; targeted re-review found none
- Decisions (why):
  - Classified approved legacy moves as `eventExclusive` because they are intentionally available but require special acquisition, unlike ordinary or Elite moves.
  - Keyed the allowlist by canonical species and move together so approval of Counter or a signature move cannot leak to another form that shares the move family.
  - Compared the explicit test table with checked-in Pokemon data so newly synced legacy pairs fail tests until maintainers choose allow or exclude behavior.
  - Exported the pure pair predicate with TSDoc so species scoping can be tested independently of the checked-in `legacyMoves` branch and dataset composition.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` and `test-driven-development` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - The checked-in Pokemon data currently contains 20 legacy pairs: 9 approved event-exclusive pairs and 11 excluded historical pairs.
  - Legacy policy executes after Elite classification because supplemental move lists can overlap ordinary movepools and acquisition categories.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-004.
- Next iteration start:
  - US-005 - Preserve PvPoke ranking moveset evidence during sync
---

## 2026-08-05 20:16:20 MST - US-005
- Preserved unaggregated Overall, Leads, Switches, Closers, Chargers, Attackers, and Consistency moveset evidence for every supported format; retained move usage and per-source alias provenance; added explicit optional PvPoke moveset override adapter reads; and kept existing ranking CSV output and runtime vendor boundaries unchanged.
- Files changed: `lib/AGENTS.md`, `lib/sync/adapter.ts`, `lib/sync/adapter.test.ts`, `lib/sync/index.ts`, `lib/sync/rankings.ts`, `lib/sync/rankings.test.ts`, `progress.txt`
- Commit message: `feat: US-005 - Preserve PvPoke ranking moveset evidence during sync`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/sync/adapter.test.ts lib/sync/rankings.test.ts` (fail as expected before implementation: 5 tests failed for missing override adapter methods, evidence result, and source provenance)
  - `npx vitest run lib/sync/adapter.test.ts lib/sync/rankings.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts` (pass, 20 tests)
  - `npx prettier --write "lib/AGENTS.md" "lib/sync/adapter.ts" "lib/sync/adapter.test.ts" "lib/sync/index.ts" "lib/sync/rankings.ts" "lib/sync/rankings.test.ts"` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 600 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/sync/rankings.test.ts` after initial review tests (fail as expected: 3 tests failed before nullable usage and source-shape validation support)
  - `npx vitest run lib/sync/adapter.test.ts lib/sync/rankings.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts` after review fixes (pass, 23 tests)
  - `npx tsc --noEmit` after review fixes (pass)
  - `npm run lint` after review fixes (pass)
  - `npm test` after review fixes (pass, 603 tests)
  - `npm run build` after review fixes (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` after review fixes (pass)
- Implementation agents:
  - Recommended: `data-engineer`, `typescript-pro`
  - Used: `data-engineer`; skipped `typescript-pro` because one domain advisor was sufficient for the focused sync contract and the existing TypeScript patterns were clear
- Specialist review:
  - Agents used: initial `code-reviewer` and `qa-expert`; targeted `code-reviewer` and `qa-expert` re-review
  - Findings: fixed four medium findings by modeling nullable source usage, validating ranking and override structures, asserting distinct evidence for every format/category pair, and covering moveset-variant pseudo-form provenance; targeted re-review found no blocking issues and one deferred low optional-stats validation improvement
- Decisions (why):
  - Returned source evidence alongside CSV rows in one `RankingSyncResult` so later aggregation can consume the exact data already read without duplicate I/O or premature US-006 weighting.
  - Retained raw category usage and explicit overrides without aggregating or normalizing their weights because normalization and weighting belong to US-006.
  - Treated missing override files as empty optional evidence because several supported PvPoke cups have no override file, while malformed present files remain errors.
  - Read each format's overrides once before its seven categories to avoid redundant source reads.
  - Validated source evidence before type assertions so malformed but syntactically valid vendor JSON cannot enter `RankingSyncResult` under an unsound contract.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - PvPoke ranking move usage lives under `moves.fastMoves` and `moves.chargedMoves`; explicit overrides live separately under `src/data/overrides/{cup}/{cp}.json` and may omit either fast or charged moves.
  - PvPoke can emit `uses: null` for move usage, including checked-in Aegislash data, so downstream aggregation must handle null explicitly rather than assuming every usage value is numeric.
  - `RankingSyncResult.categoryEvidence` preserves every original alias source entry after canonical grouping, including its source species id, canonical species id, alias kind, moveset, and usage arrays.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-005.
  - A targeted reviewer noted that optional ranking `stats` members are not structurally validated; this low-severity hardening is deferred because it does not affect retained moveset evidence or the US-005 acceptance criteria.
  - US-006 should normalize usage within each source ranking entry before category weighting and consume the retained evidence without rereading vendor paths outside the adapter.
- Next iteration start:
  - US-006 - Normalize and aggregate category move usage evidence
---

## 2026-08-05 20:39:06 MST - US-006
- Implemented deterministic format-scoped move evidence aggregation that normalizes each species entry's fast and charged usage independently, applies the required 3/2/1 category weights, records category occurrence and exact Overall use, retains canonicalized observed movesets and overrides above usage-only evidence, and exposes the result from ranking sync.
- Files changed: `lib/AGENTS.md`, `lib/sync/rankings.ts`, `lib/sync/rankings.test.ts`, `progress.txt`
- Commit message: `feat: US-006 - Normalize and aggregate category move usage evidence`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/sync/rankings.test.ts` (fail as expected before implementation: 3 aggregation tests failed because `aggregateRankingMoveEvidence` did not exist)
  - `npx vitest run lib/sync/rankings.test.ts` (pass, 12 tests)
  - `npx vitest run lib/sync/rankings.test.ts lib/sync/adapter.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts` (pass, 26 tests before the final tie-break assertion was added)
  - `npx prettier --write "lib/AGENTS.md" "lib/sync/rankings.ts" "lib/sync/rankings.test.ts"` (pass; unchanged)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 607 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/sync/rankings.test.ts` after review fix (pass, 13 tests)
  - `npx tsc --noEmit` after review fix (pass)
  - `npm run lint` after review fix (pass)
  - `git diff --check` after review fix (pass)
- Implementation agents:
  - Recommended: `data-engineer`, `test-automator`
  - Used: `data-engineer`; skipped `test-automator` because the standard-mode one-agent budget and focused pure-function test matrix made a second implementation advisor redundant
- Specialist review:
  - Agents used: initial `code-reviewer` and `qa-expert`; targeted `qa-expert` re-review
  - Findings: `code-reviewer` found none; the initial medium QA finding prompted a two-species scale-isolation test, but targeted re-review found that `createCategoryEvidence(...)` pre-sorts entries before the reversed fixture reaches the aggregation boundary; this remaining medium finding blocks the commit
- Decisions (why):
  - Kept aggregation in `lib/sync/rankings.ts` and attached it to `RankingSyncResult` so later candidate derivation consumes the exact evidence already loaded without rereading vendor paths or introducing a premature persistence format.
  - Normalized positive usage within each entry and move slot before weighting so different source scales cannot dominate; retained `null` as unknown and explicit zero as zero for Overall tie-breaking.
  - Assigned observed movesets and overrides priority 0 and usage-only move evidence priority 1 so future candidate derivation can seed exact evidence before speculative substitutions without implementing US-008 early.
  - Aggregated numeric usage from each canonical preferred category entry once while retaining every alias source moveset separately, preventing alias rows from multiplying category weight while preserving pseudo-form evidence.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - The total available category weight is 12: Overall 3; Leads, Switches, and Closers 2 each; Chargers, Attackers, and Consistency 1 each.
  - Move evidence ordering is weighted normalized use, distinct positive category occurrences, exact Overall use with unknown last, then canonical move ID.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-006.
  - Commit blocked: reverse `RankingCategoryEvidence.entries` after normalization so the shuffled-input test proves `aggregateRankingMoveEvidence(...)` is deterministic when its direct species-entry input order changes, then rerun focused checks and review in the next iteration.
  - US-007 should consume the aggregated usage as evidence for mechanics filtering without replacing exact observed movesets or matchup simulation.
- Next iteration start:
  - Resume US-006 by fixing the aggregation-boundary shuffle fixture, rerun checks, and complete review stabilization before marking the story passed.
---

## 2026-08-05 20:48:09 MST - US-006
- Resumed the completed move-evidence aggregation candidate and corrected the determinism regression so it reverses normalized `RankingCategoryEvidence.entries` at the direct aggregation boundary instead of allowing the fixture helper to pre-sort both inputs.
- Files changed: `lib/AGENTS.md`, `lib/sync/rankings.ts`, `lib/sync/rankings.test.ts`, `prd.json`, `progress.txt`
- Commit message: `feat: US-006 - Normalize and aggregate category move usage evidence`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/sync/rankings.test.ts` (pass, 13 tests)
  - `npx prettier --write "lib/AGENTS.md" "lib/sync/rankings.ts" "lib/sync/rankings.test.ts"` (pass; unchanged)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 608 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check && git diff --cached --check` (pass)
- Implementation agents:
  - Recommended: `data-engineer`, `test-automator`
  - Used: no new agent in this resumed iteration; the prior `data-engineer` guidance was already applied, and the remaining test-fixture correction was exact and did not justify another overlapping implementation advisor
- Specialist review:
  - Agents used: prior initial `code-reviewer` and `qa-expert`; targeted `qa-expert` re-review for the corrected complete staged diff
  - Findings: prior medium test-boundary finding fixed; targeted re-review found none and relied on the recorded passing quality checks
- Decisions (why):
  - Reversed entries only after `normalizeRankingSourceEntries(...)` returned so the assertion now proves `aggregateRankingMoveEvidence(...)` itself is deterministic for shuffled direct input.
  - Left production code unchanged because it already sorts category entries at the aggregation boundary and the focused regression passes against that behavior.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Determinism tests must shuffle the direct input accepted by the unit under test; fixture builders that normalize or sort can otherwise mask the ordering behavior being asserted.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-006.
  - US-007 should consume aggregated evidence for mechanics filtering without replacing observed movesets or matchup simulation.
- Next iteration start:
  - US-007 - Rank viable moves using battle mechanics
---

## 2026-08-05 21:01:34 MST - US-007
- Implemented pure battle-mechanics evidence for fast and charged move candidates, including DPT, EPT, turns, DPE, energy, first-charge pacing, STAB, type coverage, and expected status effects; added conservative Pareto filtering that removes only strictly dominated usage speculation while retaining observed, override, bait, status, and distinct-coverage moves.
- Files changed: `lib/AGENTS.md`, `lib/types.ts`, `lib/data/moves.ts`, `lib/sync/types.ts`, `lib/sync/validation.ts`, `lib/sync/gamemaster.test.ts`, `lib/sync/moveMechanics.ts`, `lib/sync/moveMechanics.test.ts`, `progress.txt`
- Commit message: `feat: US-007 - Rank viable moves using battle mechanics`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/sync/moveMechanics.test.ts lib/sync/gamemaster.test.ts` (fail as expected before implementation: missing mechanics module and malformed status fields accepted)
  - `npx vitest run lib/sync/moveMechanics.test.ts lib/sync/gamemaster.test.ts` (pass, 11 tests)
  - `npx tsc --noEmit` before compatibility fix (fail: `getBuffDetails` exposed mutable status stages)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npx vitest run lib/sync/moveMechanics.test.ts lib/sync/gamemaster.test.ts lib/sync/rankings.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts` (pass, 28 tests)
  - `npm test` (pass, 614 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/sync/moveMechanics.test.ts lib/sync/gamemaster.test.ts` after initial review tests (fail as expected: 4 tests exposed missing power, target-coherence, and validation behavior)
  - `npx vitest run lib/sync/moveMechanics.test.ts lib/sync/gamemaster.test.ts` after review fixes (pass, 17 tests)
  - `npx tsc --noEmit` after review fixes (pass)
  - `npm run lint` after review fixes (pass after correcting one import-order error)
  - `npx vitest run lib/sync/moveMechanics.test.ts lib/sync/gamemaster.test.ts lib/sync/rankings.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts` after review fixes (pass, 34 tests)
  - `npm test` after review fixes (pass, 620 tests)
  - `npm run build` after review fixes (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check && git diff --cached --check` after review fixes (pass)
- Implementation agents:
  - Recommended: `typescript-pro`, `test-automator`
  - Used: `typescript-pro`; skipped `test-automator` because the standard-mode one-agent budget and focused pure-function test matrix made a second implementation advisor redundant
- Specialist review:
  - Agents used: initial `code-reviewer` and `qa-expert`; targeted `code-reviewer` and `qa-expert` re-review
  - Findings: fixed four medium findings by preserving higher-power nukes, rejecting contradictory status targets, validating checked-in move data, and covering observed/override protection in both move slots; targeted re-review found none
- Decisions (why):
  - Kept mechanics in a pure sync/tooling helper instead of ranking aggregation so later candidate derivation can supply explicit observed, override, or usage provenance without coupling aggregation to Pokemon and move datasets.
  - Required strict Pareto domination within the same move type and status signature; lower energy, status effects, and distinct type coverage remain incomparable so mechanics cannot replace matchup simulation.
  - Required equal-or-higher one-hit power for charged domination so an efficient lower-energy move cannot mechanically remove a stronger shield-down nuke.
  - Calculated coverage from the checked-in Pokemon GO type chart and represented `both` effects as separate expected self and opponent stage changes.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Checked-in move status data uses attack/defense tuples, string probabilities, and `OBSTRUCT`-style separate `buffsSelf` and `buffsOpponent` tuples when `buffTarget` is `both`.
  - First-charge pacing intentionally ignores residual energy and full sequencing; simulation remains authoritative for matchup outcomes.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-007.
  - US-008 should map aggregated exact movesets and overrides to protected mechanics candidates, then use viable usage moves only for bounded substitutions.
- Next iteration start:
  - US-008 - Derive bounded deterministic moveset candidates
---

## 2026-08-05 21:29:43 MST - US-008
- Implemented deterministic format/species moveset candidate derivation from eligible Overall, role-category, override, and normalized usage evidence; added preferred-source provenance, mechanics-filtered two-fast/four-charged expansion pools, exact-seed retention, non-recursive single-move substitutions, canonical deduplication, and an eight-candidate cap; exposed candidate sets from ranking sync using availability bound to the freshly loaded Pokemon snapshot.
- Files changed: `lib/AGENTS.md`, `lib/data/moveAvailability.ts`, `lib/data/moveAvailability.test.ts`, `lib/sync/movesetCandidates.ts`, `lib/sync/movesetCandidates.test.ts`, `lib/sync/rankings.ts`, `lib/sync/rankings.test.ts`, `progress.txt`
- Commit message: `feat: US-008 - Derive bounded deterministic moveset candidates`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/sync/movesetCandidates.test.ts` (fail as expected before implementation: candidate derivation module missing)
  - `npx vitest run lib/sync/rankings.test.ts` (fail as expected before integration: `candidateSets` missing from ranking sync output)
  - `npx vitest run lib/data/moveAvailability.test.ts` (fail as expected before snapshot support: resolver factory missing)
  - `npx vitest run lib/data/moveAvailability.test.ts lib/sync/movesetCandidates.test.ts lib/sync/rankings.test.ts lib/sync/moveMechanics.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts` (pass, 65 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` before import-order fix (fail: 5 `import/order` errors in the new candidate files)
  - `npm run lint` after import-order fix (pass)
  - `npm test` (pass, 627 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/sync/movesetCandidates.test.ts lib/sync/rankings.test.ts` after review fixes (pass, 21 tests)
  - `npx tsc --noEmit` after review fixes (pass)
  - `npm run lint` after review fixes (pass)
  - `npm test` after review fixes (pass, 629 tests)
  - `npm run build` after review fixes (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check && git diff --cached --check` after review fixes (pass)
  - `npx vitest run lib/sync/movesetCandidates.test.ts lib/sync/rankings.test.ts` after targeted review fixture fixes (pass, 21 tests)
  - `npx tsc --noEmit` after targeted review fixture fixes (pass)
  - `npm run lint` after targeted review fixture fixes (pass)
  - `npm test` after targeted review fixture fixes (pass, 629 tests)
  - `npm run build` after targeted review fixture fixes (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check && git diff --cached --check` after targeted review fixture fixes (pass)
- Implementation agents:
  - Recommended: `data-engineer`, `test-automator`
  - Used: `data-engineer`; skipped `test-automator` because one domain advisor plus the focused test-first matrix covered the complex sync transformation without overlapping guidance
- Specialist review:
  - Agents used: initial `code-reviewer` and `qa-expert`; targeted `code-reviewer` and `qa-expert` re-review
  - Findings: fixed four initial medium findings by rejecting oversized source shapes, strengthening cap/dedup/lexical assertions, exercising Golisopod alias evidence through ranking sync, and covering partial overrides; also replaced one low brittle serialized-substring assertion with structural checks. Targeted code re-review found no issues; targeted QA found one medium alias-fixture false-positive risk and one low partial-override isolation risk, both fixed by making alias usage exclusive and asserting exact-seed ordering before substitutions.
- Decisions (why):
  - Marked the preferred source entry explicitly during category aggregation because category name and alias provenance alone cannot identify the ranked Overall default deterministically.
  - Retained every eligible exact observed and override set even when one of its moves falls outside the bounded expansion pool because direct exact-format evidence is stronger than usage-only speculation.
  - Expanded only observed anchors by one fast or one charged move and never recursively expanded generated candidates, preventing a full legal movepool Cartesian product.
  - Bound move availability policy to the Pokemon array read by ranking sync so a same-process gamemaster refresh cannot derive candidates against stale statically imported data.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - `rankViableMoves(...)` filters candidates but preserves caller order, so derivation must sort provenance and usage evidence before mechanics filtering and apply caps afterward.
  - Exact charged-move display order remains source-preferred while `getMovesetVariantId(...)` deduplicates the charged pair order-independently.
  - Partial overrides apply only to the deterministic preferred Overall baseline; generated substitutions never become new expansion anchors.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-008.
  - US-009 must record and replace ranked defaults rejected by availability policy; US-008 intentionally leaves such candidate sets without a default rather than promoting one early.
- Next iteration start:
  - US-009 - Replace invalid PvPoke ranked defaults
---

## 2026-08-05 22:04:38 MST - US-009
- Implemented deterministic rejection metadata and replacement selection for unavailable or incomplete preferred Overall movesets; retained the original PvPoke score as a candidate-quality prior; delayed ranking CSV conversion until candidate derivation; and retained source evidence while omitting species that cannot form a complete eligible set. Simulation sync now uses emitted Overall rows as the exact canonical opponent pool, initializes every target with sanitized defaults, fingerprints all Overall output for resume invalidation, and prunes changed format caches before regeneration.
- Files changed: `lib/AGENTS.md`, `lib/data/rankings.test.ts`, `lib/sync/index.ts`, `lib/sync/movesetCandidates.ts`, `lib/sync/movesetCandidates.test.ts`, `lib/sync/rankings.ts`, `lib/sync/rankings.test.ts`, `lib/sync/simulations.ts`, `lib/sync/simulations.test.ts`, six `data/rankings/**/overall_rankings.csv` files, 2,682 regenerated `data/simulations/**/*.csv` files, `prd.json`, `progress.txt`
- Commit message: `feat(sync): US-009 replace invalid PvPoke ranked defaults`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/sync/movesetCandidates.test.ts lib/sync/rankings.test.ts` (fail as expected before implementation: missing score prior and rejection metadata)
  - `npx vitest run lib/data/rankings.test.ts -t "never returns a rejected ranked default moveset"` (fail as expected before data replacement: Muk returned Acid)
  - `npx vitest run lib/sync/rankings.test.ts -t "converts local ranking JSON into validated CSV outputs"` (fail as expected before incomplete-set handling: Shadow Wobbuffet had no complete eligible replacement)
  - `bun -e "import { scrapeRankings } from './lib/sync/rankings.ts'; await scrapeRankings({ sourcePath: './vendor/pvpoke' });"` (pass; replaced or omitted invalid Overall sets and preserved source scores)
  - runtime availability scan across every supported Overall ranking (pass; no excluded moves remain)
  - `npx vitest run lib/sync/movesetCandidates.test.ts lib/sync/rankings.test.ts lib/data/rankings.test.ts` (pass, 37 tests)
  - `npx vitest run lib/sync/movesetCandidates.test.ts lib/sync/rankings.test.ts lib/sync/simulations.test.ts lib/data/rankings.test.ts` after simulation and review fixes (pass, 45 tests)
  - generated Overall/simulation invariant across six affected formats (pass; all 2,682 files have unique opponent sets exactly matching emitted Overall rows)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 635 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `debugger`, `test-automator`
  - Used: `debugger`; skipped `test-automator` because one domain advisor plus the focused test-first candidate, sync, and runtime regressions covered the standard-mode implementation budget
- Specialist review:
  - Agents used: targeted `code-reviewer` and `qa-expert` re-review
  - Findings: fixed battle-state-only usage leaking into canonical substitutions, arbitrary Overall changes bypassing resume invalidation, orphaned changed-format cache files, unresolved/incomplete repository invariant gaps, and missing unchanged-format cache reuse coverage. Battle-state Overall source output remains intentionally handled by the existing contextual alias normalization path rather than ordinary availability rejection.
- Decisions (why):
  - Preserved invalid preferred sets as non-emitted substitution anchors so one evidence-backed move replacement can repair Muk, Starmie, and similar defaults without generating a full movepool product.
  - Promoted the first deterministically ranked eligible candidate only after canonical deduplication so exactly one default exists and the rejected set can never regain priority through its original score.
  - Changed only Overall move columns and charge counts while preserving the source ranking score, rank order, and full category evidence because the score is a candidate prior rather than a replacement simulation result.
  - Omitted Shadow Wobbuffet and two Liga Ultra Mewtwo records from Overall output because their checked-in data cannot form any complete eligible two-charged-move set; source evidence and rejection details remain available, while runtime and simulations fail closed.
  - Exempted battle-state pseudo-form evidence from ordinary availability rejection so Morpeko and Aegislash state-specific move names remain handled by the existing contextual normalization path.
  - Compared byte-identical pre-sync and emitted Overall CSVs for every format because any moveset, species, or ordering change can invalidate every matchup file in that format, not only explicitly repaired defaults.
  - Constructed explicit TeamRanker targets from sanitized Overall rankings rather than the generic PvPoke cup filter because emitted rankings include valid entries such as Camerupt (Mega) and Hoopa (Confined) that the generic filter omits.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Ranking CSV conversion must happen after format-wide evidence aggregation and candidate derivation when derived legality can change the published Overall moveset.
  - The bundled source currently contains some preferred sets that cannot form a complete legal two-charged-move variant; retaining source evidence while omitting their Overall rows prevents illegal runtime or simulation defaults.
  - A repository-wide runtime loader test now validates every checked-in Overall move against the same availability policy used by sync.
  - Changed-format resume regeneration must prune the format directory first so species leaving the top 150 and removed moveset variants cannot survive as stale files.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-009.
  - Role-category CSVs intentionally retain raw source moveset evidence; only Overall output is the runtime and default-simulation authority changed by this story.
- Next iteration start:
  - US-010 - Add deterministic simulation projection reporting
---

## 2026-08-05 23:49:58 MST - US-010
- Implemented a deterministic read-only simulation projection for every supported format; reports multi-candidate species, total and alternate variants, per-species eight-candidate caps, required shield-scenario CSV totals, and stale strictly recognized variant files; exposed sanitized top-150 simulation species from ranking sync; and added the non-interactive `npm run sync -- --project-simulations` command.
- Files changed: `lib/AGENTS.md`, `lib/scripts/sync.ts`, `lib/sync/gamemaster.ts`, `lib/sync/rankings.ts`, `lib/sync/rankings.test.ts`, `lib/sync/simulationProjection.ts`, `lib/sync/simulationProjection.test.ts`, `progress.txt`
- Commit message: `feat: US-010 - Add deterministic simulation projection reporting`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/sync/simulationProjection.test.ts lib/sync/rankings.test.ts` (fail as expected before implementation: projection module and sanitized simulation target map were missing)
  - `npx vitest run lib/sync/simulationProjection.test.ts lib/sync/rankings.test.ts` (pass, 17 tests)
  - `npm run sync -- --project-simulations` with before/after worktree digest comparison (pass; all eight formats reported and no files changed)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass after correcting one import-order error)
  - `npm test` (pass, 638 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/sync/simulationProjection.test.ts lib/sync/rankings.test.ts` after initial review tests (fail as expected: noncanonical charged order, polluted CLI stdout, and missing top-150 helper)
  - `npx vitest run lib/sync/simulationProjection.test.ts lib/sync/rankings.test.ts lib/sync/gamemaster.test.ts` after review fixes (pass, 29 tests)
  - `npx tsc --noEmit` after review fixes (pass)
  - `npm run lint` after review fixes (pass)
  - `npm test` after review fixes (pass, 641 tests)
  - `npm run build` after review fixes (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` after review fixes (pass)
- Implementation agents:
  - Recommended: `tooling-engineer`, `data-engineer`
  - Used: `tooling-engineer`; skipped `data-engineer` because the read-only CLI boundary and existing candidate derivation contract were clear after one focused tooling review
- Specialist review:
  - Agents used: initial `code-reviewer` and `qa-expert`; targeted `code-reviewer` and `qa-expert` re-review
  - Findings: fixed four medium findings by emitting standalone JSON, requiring canonical charged-move ordering, automating read-only orchestration and real CLI output, and directly testing the 150-target cap; targeted QA found none, and one targeted npm lifecycle-banner suggestion was discarded because package-manager framing is outside the application report contract and standalone npm stdout was not an acceptance criterion
- Decisions (why):
  - Added a dedicated `--project-simulations` branch before `runSync(...)` because the existing full pipeline deletes rankings and may delete simulations before deriving candidates.
  - Derived projection data from the current local PvPoke source through no-op persistence dependencies so source validation and candidate policy remain identical without writing Pokemon, move, ranking, manifest, or simulation files.
  - Exposed canonical species IDs that actually survive Overall conversion and the top-150 simulation cut so omitted invalid defaults and non-target ranking evidence cannot inflate projected variant output.
  - Counted candidate variants including each qualifying species default while projecting shield CSV growth only for non-default variants, making both evidence volume and generated-file growth explicit.
  - Restricted stale recognition to lowercase canonical species/move IDs, exactly three move tokens, and `0-0`, `1-1`, or `2-2` scenarios so default and malformed files are never reported as cleanup candidates.
  - Added quiet read-only source/ranking derivation and reserved projection stdout for JSON so repeated CLI output is machine-readable and byte-comparable.
  - Kept the documented `npm run sync -- --project-simulations` invocation consistent with the existing sync command; machine consumers can call the tested Bun entry point directly, while npm's own lifecycle banner does not alter report ordering or totals.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` and `test-driven-development` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - The full sync entry point is destructive before ranking derivation; dry tooling must not call `runSync(...)`.
  - Ranking candidate sets cover more species than simulation generation, so projection must intersect them with sanitized top-150 Overall targets rather than count every evidence record.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-010.
  - US-012 will replace the hardcoded simulation alternate generation with the projected derived candidates; US-015 will use the strict recognized stale-file boundary for deletion only after successful publication.
- Next iteration start:
  - US-011 - Define and validate the moveset variant manifest schema
---

## 2026-08-06 00:19:25 MST - US-011
- Defined a versioned typed moveset variant manifest contract with format and policy metadata, SHA-256 source digests, derivation settings, per-species ranking evidence, per-candidate acquisition requirements, deterministic storage keys, completeness and evaluation maps, and active/default status; added strict unknown-input parsing, path-aware typed validation errors, canonical serialization, and a pure sync-layer builder.
- Files changed: `lib/AGENTS.md`, `lib/data/movesetVariantManifest.ts`, `lib/data/movesetVariantManifest.test.ts`, `lib/sync/movesetVariantManifest.ts`, `lib/sync/movesetVariantManifest.test.ts`, `lib/sync/movesetCandidates.ts`, `progress.txt`
- Commit message: `feat: US-011 - Define and validate the moveset variant manifest schema`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/data/movesetVariantManifest.test.ts lib/sync/movesetVariantManifest.test.ts` (fail as expected before implementation: both manifest modules were missing)
  - `npx vitest run lib/data/movesetVariantManifest.test.ts lib/sync/movesetVariantManifest.test.ts` (fail as expected before validation hardening: declared derivation caps and unsafe logical source keys were accepted)
  - `npx vitest run lib/data/movesetVariantManifest.test.ts lib/sync/movesetVariantManifest.test.ts` after initial review tests (fail as expected: five regressions exposed missing path, normalized identity, canonical evidence, and total-sort behavior)
  - `npx vitest run lib/data/movesetVariantManifest.test.ts lib/sync/movesetVariantManifest.test.ts lib/sync/movesetCandidates.test.ts` after review fixes (pass, 36 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass after correcting two import-order errors)
  - `npm test` after review fixes (pass, 666 tests before the final positive acquisition fixture)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `typescript-pro`, `architect-reviewer`
  - Used: `architect-reviewer`; skipped `typescript-pro` because one architecture-focused advisor covered the complex schema boundary and the repository's strict TypeScript parsing patterns were already clear
- Specialist review:
  - Agents used: initial `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `code-reviewer`, `qa-expert`, and `security-engineer` re-review
  - Findings: fixed six deduplicated initial medium findings by normalizing charged-move distinctness, requiring canonical evidence variant IDs, defining the exact format-scoped manifest path, covering required metadata and default/declared-cap failures, and giving rejection evidence a total deterministic comparator; also added the suggested low-severity positive acquisition-category fixture. Targeted code and QA re-reviews found none, but targeted security re-review found two remaining medium alias-canonicalization findings, so the commit is blocked.
- Decisions (why):
  - Kept the wire-format DTO and all unknown-input validation in `lib/data` while limiting `lib/sync` to exact-byte source hashing and pure construction, preserving the runtime vendor boundary and leaving atomic publication to US-014.
  - Stored logical source digests as sorted key/algorithm/digest records instead of absolute paths or timestamps so identical repository inputs serialize byte-identically across machines.
  - Required canonical variant identities and exact default-versus-alternate storage basenames while preserving charged moves in preferred source order inside each candidate.
  - Centralized the eight-candidate and three-active hard caps in the manifest contract, then re-exported the candidate cap from derivation to avoid breaking current consumers while preventing policy drift.
  - Defined the repository-relative manifest path from typed format metadata without adding publication or filesystem behavior that belongs to US-014.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Manifest validation must compare format id, cup, and CP against `lib/data/battleFormats.ts`, and declared per-manifest caps must be at least as strict as each species record.
  - Deterministic serialization sorts logical source digests, species, candidates, candidate source categories, and source variant IDs without sorting the candidate's preferred charged-move display order or ranked retained-move pools.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-011.
  - Commit blocked after the one allowed targeted re-review: require `normalizeToChoosableSpeciesId(speciesId) === speciesId` so known species aliases cannot create parallel manifest/storage identities, and require move IDs to equal `normalizeMoveId(...)` so spelling aliases such as `SUPERPOWER` and `SUPER_POWER` cannot create distinct variants for one logical moveset.
  - US-012 should populate this contract from exact derived candidates and simulation results; US-013 should select active alternatives; US-014 should publish the fully constructed manifest atomically.
- Next iteration start:
  - Resume US-011 with failing species-alias and move-alias parser regressions, apply canonicalization validation, rerun checks, and perform a fresh review stabilization loop before marking the story passed.
---

## 2026-08-06 00:37:21 MST - US-011
- Resumed the manifest schema story and closed the blocked canonicalization gaps by rejecting known species aliases and move spelling aliases across candidates, source variant IDs, retained move evidence, and rejection evidence.
- Files changed: `lib/AGENTS.md`, `lib/data/movesetVariantManifest.ts`, `lib/data/movesetVariantManifest.test.ts`, `lib/sync/movesetVariantManifest.ts`, `lib/sync/movesetVariantManifest.test.ts`, `lib/sync/movesetCandidates.ts`, `progress.txt`
- Commit message: `feat: US-011 - Define and validate the moveset variant manifest schema`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/data/movesetVariantManifest.test.ts` (fail as expected: five alias regressions were accepted before implementation)
  - `npx vitest run lib/data/movesetVariantManifest.test.ts lib/sync/movesetVariantManifest.test.ts lib/sync/movesetCandidates.test.ts` (pass, 45 tests after review fixes)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (fail before mechanical import-order fix; pass afterward)
  - `npm test` (pass, 676 tests after review fixes)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check && git diff --cached --check` (pass)
- Implementation agents:
  - Recommended: `typescript-pro`, `architect-reviewer`
  - Used: no new agent in this resumed iteration; the prior `architect-reviewer` guidance was already applied, and the two explicit parser findings did not justify overlapping implementation advice
- Specialist review:
  - Agents used: fresh initial `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `code-reviewer`, `qa-expert`, and `security-engineer` re-review
  - Findings: fixed one deduplicated medium canonical-ID finding by requiring uppercase ASCII move fields, preserving separately validated lowercase variant-ID components, and adding isolated `excludedMove` alias coverage. Targeted code and security re-reviews found none; targeted QA found one remaining medium test-isolation issue, so the commit is blocked. Deferred one medium catalog-reference suggestion to US-012 because correct species, move-slot, legality, and acquisition validation requires the exact freshly synced source snapshot supplied during generated candidate construction rather than stale runtime imports in the schema parser.
- Decisions (why):
  - Rejected aliases instead of silently normalizing them because the manifest is authoritative generated data and accepting alternate spellings would make serialized identity depend on source representation.
  - Applied canonical move validation to every manifest field that carries move identity, not only candidate movesets, so retained and rejection evidence cannot reintroduce ambiguous IDs.
  - Kept structural parsing independent from checked-in Pokemon and move snapshots; US-012 must validate generated candidates against the same freshly loaded sync snapshot that produced them.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Canonical manifest move fields use uppercase ASCII IDs, while canonical variant IDs use lowercase components that must round trip through the uppercase move-alias map.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-011.
  - Commit blocked after the one allowed targeted re-review: make the source-moveset alias regression keep `excludedMove` canonical so only `sourceMoveset.chargedMove1` triggers validation; the following fixture already isolates the inverse `excludedMove` case.
  - US-012 should populate this contract from exact derived candidates and simulation results; US-013 should select active alternatives; US-014 should publish the fully constructed manifest atomically.
- Next iteration start:
  - Resume US-011 by isolating the source-moveset alias fixture, rerun focused and full checks, then perform a fresh review stabilization loop before marking the story passed.
---

## 2026-08-06 00:50:01 MST - US-011
- Resumed the manifest schema story and isolated the final alias-validation regression so the source-moveset case keeps `excludedMove` canonical and independently proves that a move spelling alias inside `sourceMoveset` is rejected.
- Files changed: `lib/AGENTS.md`, `lib/data/movesetVariantManifest.ts`, `lib/data/movesetVariantManifest.test.ts`, `lib/sync/movesetCandidates.ts`, `lib/sync/movesetVariantManifest.ts`, `lib/sync/movesetVariantManifest.test.ts`, `prd.json`, `progress.txt`
- Commit message: `feat: US-011 - Define and validate the moveset variant manifest schema`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/data/movesetVariantManifest.test.ts lib/sync/movesetVariantManifest.test.ts lib/sync/movesetCandidates.test.ts` (pass, 45 tests)
  - `npx prettier --write "lib/data/movesetVariantManifest.test.ts" "lib/data/movesetVariantManifest.ts" "lib/sync/movesetCandidates.ts" "lib/sync/movesetVariantManifest.test.ts" "lib/sync/movesetVariantManifest.ts"` (pass; unchanged)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 676 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check && git diff --cached --check` (pass)
  - `npx vitest run lib/data/movesetVariantManifest.test.ts lib/sync/movesetVariantManifest.test.ts lib/sync/movesetCandidates.test.ts` after review fix (pass, 45 tests)
  - `npx prettier --write "lib/data/movesetVariantManifest.test.ts"` after review fix (pass; unchanged)
  - `npx tsc --noEmit` in parallel with build after review fix (fail: Next.js temporarily removed `.next/types/validator.ts` while TypeScript read it)
  - `npm run lint` after review fix (pass)
  - `npm test` after review fix (pass, 676 tests)
  - `npm run build` after review fix (pass; existing multiple-lockfile workspace-root warning only)
  - `npx tsc --noEmit` after build completed (pass)
- Implementation agents:
  - Recommended: `typescript-pro`, `architect-reviewer`
  - Used: no new implementation agent in this resumed iteration; prior `architect-reviewer` guidance was already incorporated, and the remaining isolated fixture correction was exact and did not justify overlapping advice
- Specialist review:
  - Agents used: initial `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `qa-expert` re-review
  - Findings: code and security reviewers found none; fixed one medium QA finding by rebuilding the zero-default candidate with alternate-form storage keys and asserting the exact default-consistency errors instead of accepting any validation failure; targeted QA re-review found none
- Decisions (why):
  - Kept `excludedMove` as canonical `SUPER_POWER` in the source-moveset regression because each table case must mutate only the field named by that case; the following fixture independently covers an aliased `excludedMove`.
  - Made no production change in this resumed pass because parser canonicalization already rejected the isolated source alias and all focused and full checks passed.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Validation table fixtures must keep every non-target field valid so a passing assertion proves the intended parser boundary rather than an earlier sibling-field failure.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-011.
  - Generated candidates still require source-snapshot species, move-slot, legality, and acquisition validation in US-012 rather than coupling the structural manifest parser to stale checked-in runtime data.
- Next iteration start:
  - US-012 - Generate candidate variant simulation files
---

## 2026-08-06 01:16:15 MST - US-012
- Replaced the hardcoded Golisopod simulation alternate map with ranking-derived bounded candidate sets, passed sanitized top-150 format targets from ranking sync, validated canonical candidate metadata and move legality against freshly synced snapshots, forced each exact candidate moveset through the isolated PvPoke VM boundary, and retained separate default versus canonical variant output paths with strict resume validation for all three required shield scenarios.
- Files changed: `lib/AGENTS.md`, `lib/sync/index.ts`, `lib/sync/simulations.ts`, `lib/sync/simulations.test.ts`, `prd.json`, `progress.txt`
- Commit message: `feat(sync): US-012 generate candidate variant simulation files`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/sync/simulations.test.ts -t "generates ranking-derived alternate movesets"` (fail as expected before implementation: Feraligatr alternate was not generated by the hardcoded Golisopod map)
  - `npx vitest run lib/sync/simulations.test.ts -t "rejects duplicate canonical identities"` (fail as expected before duplicate validation: duplicate candidate identity was simulated)
  - `npx prettier --write "lib/sync/index.ts" "lib/sync/simulations.ts" "lib/sync/simulations.test.ts"` (pass)
  - `npx vitest run lib/sync/simulations.test.ts lib/sync/simulationProjection.test.ts lib/sync/rankings.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts` (pass, 32 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 677 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/sync/simulations.test.ts -t "ranking-derived alternate|path-unsafe"` before review fixes (fail as expected: structurally valid opponent mismatch was reused and path-unsafe move ids reached output construction)
  - `npx vitest run lib/sync/simulations.test.ts lib/sync/simulationProjection.test.ts lib/sync/rankings.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts` after review fixes (pass, 33 tests)
  - `npx tsc --noEmit` after review fixes (pass)
  - `npm run lint` after review fixes (pass)
  - `npm test` after review fixes (pass, 678 tests)
  - `npm run build` after review fixes (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check && git diff --cached --check` after review fixes (pass)
  - `npx prettier --write "lib/sync/simulations.test.ts"` after targeted re-review (pass)
  - `npx vitest run lib/sync/simulations.test.ts` after targeted re-review (pass, 9 tests)
  - Final `npx tsc --noEmit`, `npm run lint`, `npm test`, and `npm run build` after targeted re-review (pass, 678 tests; existing multiple-lockfile workspace-root warning only)
- Implementation agents:
  - Recommended: `data-engineer`, `test-automator`
  - Used: `data-engineer`, `test-automator`; both were within the standard-mode complex-story budget and provided non-overlapping sync architecture and test-boundary guidance
- Specialist review:
  - Agents used: initial and one targeted re-review pass with `code-reviewer`, `qa-expert`, and `security-engineer`
  - Findings: fixed the deduplicated medium opponent-completeness finding by requiring exact parity with each default scenario, including direct regression coverage for generated and resume-reused alternates; fixed the medium path-safety finding with strict storage-token validation plus resolved-path containment; discarded the ordered-charged-move cache finding because the shared contract explicitly defines the charged pair as order-independent identity and PvPoke `resetMoves()` derives battle priority from the selected pair rather than source display order; targeted re-review found no remaining material correctness, QA, or security findings
- Decisions (why):
  - Passed `candidateSets` and `simulationSpeciesIdsByFormatId` from `RankingSyncResult` into simulation sync instead of re-deriving candidates, preserving the exact fresh sync snapshot and sanitized Overall target boundary.
  - Kept default simulations on `{speciesId}_{scenario}.csv` and emitted only non-default candidates to `{speciesId}--{variantId}_{scenario}.csv`, preserving the default species matrix as a separate authority.
  - Reused a resume file only at its exact canonical format, species, variant, and scenario path after CSV validation; malformed scenarios regenerate independently while valid siblings are retained.
  - Validated format metadata, path-safe storage tokens and containment, one default, candidate caps, canonical and unique variant ids, move slots, move availability, emitted Overall default agreement, and opponent-set parity before accepting candidate simulations.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, `code-formatter`, and `test-runner` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - `RankingSyncResult.simulationSpeciesIdsByFormatId` is the authoritative sanitized top-150 selection list; simulation sync should not reconstruct it from broader candidate evidence.
  - Canonical variant ids remain charged-order-independent, while the candidate's preferred charged move order is passed unchanged to PvPoke `selectMove` calls.
  - A candidate generation pass is successful only after valid `0-0`, `1-1`, and `2-2` scenario files are generated or reused from the exact canonical path.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-012.
  - Manifest completeness records and active alternate selection remain intentionally deferred to US-013 and atomic publication to US-014.
- Next iteration start:
  - US-013 - Select active simulation-backed alternatives
---

## 2026-08-06 01:46:05 MST - US-013
- Implemented deterministic simulation-backed variant selection with complete finite scenario evidence, soft matchup comparisons over common explicit opponent pools, 70/30 top-meta/full-meta weighting, positive primary improvement, positive secondary marginal coverage, stable canonical-ID tie-breakers, and a hard default-plus-two active cap; simulation sync now returns manifest-ready selections for generated and reused CSV evidence, and manifest validation rejects incomplete or unevaluated active alternatives.
- Files changed: `lib/AGENTS.md`, `lib/data/movesetVariantManifest.ts`, `lib/data/movesetVariantManifest.test.ts`, `lib/sync/index.ts`, `lib/sync/movesetVariantManifest.ts`, `lib/sync/movesetVariantManifest.test.ts`, `lib/sync/simulations.ts`, `lib/sync/simulations.test.ts`, `progress.txt`
- Commit message: `feat: US-013 - Select active simulation-backed alternatives`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/sync/movesetVariantManifest.test.ts` (fail as expected before implementation: selector export missing)
  - `npx vitest run lib/sync/simulations.test.ts -t "generates ranking-derived alternate movesets"` (fail as expected before integration: simulation result exposed no variant selections)
  - `npx vitest run lib/data/movesetVariantManifest.test.ts -t "active alternatives"` (fail as expected before validation hardening: incomplete active alternate accepted)
  - `npx vitest run lib/data/movesetVariantManifest.test.ts lib/sync/movesetVariantManifest.test.ts lib/sync/simulations.test.ts` (pass, 50 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass after removing one type-only test constant)
  - `npm test` (pass, 684 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/data/movesetVariantManifest.test.ts lib/sync/movesetVariantManifest.test.ts` after initial review tests (fail as expected: 3 regressions exposed secondary-pool, floating-tie, and sparse-manifest gaps)
  - `npx vitest run lib/data/movesetVariantManifest.test.ts lib/sync/movesetVariantManifest.test.ts lib/sync/simulations.test.ts` after review fixes (pass, 51 tests)
  - `npx prettier --write "lib/data/movesetVariantManifest.test.ts" "lib/data/movesetVariantManifest.ts" "lib/sync/movesetVariantManifest.test.ts" "lib/sync/movesetVariantManifest.ts"` after review fixes (pass)
  - `npx tsc --noEmit` after review fixes (pass)
  - `npm run lint` after review fixes (pass)
  - `npm test` after review fixes (pass, 685 tests)
  - `npm run build` after review fixes (pass; existing multiple-lockfile workspace-root warning only)
- Implementation agents:
  - Recommended: `typescript-pro`, `test-automator`
  - Used: `typescript-pro`; skipped `test-automator` because one focused TypeScript domain advisor plus the test-first selector, manifest, and simulation integration coverage fit the standard-mode budget
- Specialist review:
  - Agents used: initial and targeted `code-reviewer` and `qa-expert` passes
  - Findings: fixed three deduplicated initial medium findings by evaluating every complete non-primary candidate for the secondary slot, treating improvements within `1e-12` as ties, and requiring active manifest alternatives to match default evaluation counts in every scenario; targeted re-review found one remaining medium non-transitive epsilon-ordering blocker
- Decisions (why):
  - Required full coverage of the caller's explicit bounded top-meta and full-meta pools in every scenario instead of inventing a fixed minimum sample count, so small cups remain eligible while sparse candidates fail closed.
  - Reused the optimizer's continuous `scoreMatchupRating(...)` and established 70/30 top-meta/full-meta weighting so close battle-rating changes affect preference without replacing categorical thresholds elsewhere.
  - Required the secondary to improve a matchup beyond the better of default and primary, preventing duplicate alternatives from consuming the second active slot.
  - Allowed a secondary with negative overall improvement because its role is complementary marginal coverage, while retaining overall improvement as a deterministic tie-breaker after marginal quality.
  - Used a `1e-12` comparison epsilon so floating-point residue cannot activate mathematically tied candidates or bypass stable variant-ID tie-breaking.
  - Returned selections from simulation sync but deferred manifest publication to US-014, preserving the story boundary between selection and atomic authority publication.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Selection evidence must retain format, species, variant, scenario, canonical opponent, and finite rating identity; the previous flat `SimulationsCsv` return was insufficient to construct authoritative active manifest entries.
  - Generated and resume-reused alternate CSVs must produce identical active selections when their exact simulation evidence is identical.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-013.
  - Commit blocked after the one allowed targeted re-review: replace pairwise epsilon comparison with a transitive deterministic ranking strategy and add shuffled primary and secondary epsilon-chain regressions before rerunning checks and a fresh review stabilization loop.
  - US-014 must construct and atomically publish one complete manifest per format from candidate metadata and the manifest-ready simulation selections returned here.
- Next iteration start:
  - Resume US-013 by implementing transitive epsilon ordering, adding permutation regressions, rerunning all checks, and completing a fresh staged review loop.
---

## 2026-08-06 12:36:41 MST - US-013
- Resumed the simulation-backed active variant selector and replaced its non-transitive pairwise epsilon comparator with independently quantized ranking keys; added all six candidate-order permutations for primary and secondary epsilon chains so fixed simulation evidence now selects the same canonical winners regardless of source order.
- Files changed: `lib/AGENTS.md`, `lib/data/movesetVariantManifest.test.ts`, `lib/data/movesetVariantManifest.ts`, `lib/data/simulations.ts`, `lib/sync/index.ts`, `lib/sync/movesetVariantManifest.test.ts`, `lib/sync/movesetVariantManifest.ts`, `lib/sync/simulations.test.ts`, `lib/sync/simulations.ts`, `progress.txt`
- Commit message: `feat: US-013 - Select active simulation-backed alternatives`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/sync/movesetVariantManifest.test.ts` before comparator fix (fail as expected: primary and secondary winners changed across candidate permutations)
  - `npx vitest run lib/sync/movesetVariantManifest.test.ts` after comparator fix (pass, 10 tests)
  - `npx vitest run lib/data/movesetVariantManifest.test.ts lib/sync/movesetVariantManifest.test.ts lib/sync/simulations.test.ts` (pass, 53 tests)
  - `npx prettier --write "lib/data/movesetVariantManifest.test.ts" "lib/data/movesetVariantManifest.ts" "lib/sync/index.ts" "lib/sync/movesetVariantManifest.test.ts" "lib/sync/movesetVariantManifest.ts" "lib/sync/simulations.test.ts" "lib/sync/simulations.ts"` (pass; unchanged)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 687 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npx vitest run lib/data/movesetVariantManifest.test.ts lib/sync/simulations.test.ts` after initial review tests (fail as expected: incomplete defaults were accepted and moveset-suffixed TeamRanker cells produced empty evidence)
  - `npx vitest run lib/data/movesetVariantManifest.test.ts lib/data/simulations.test.ts lib/sync/movesetVariantManifest.test.ts lib/sync/simulations.test.ts` after review fixes (pass, 64 tests)
  - `npx tsc --noEmit` after review fixes (pass)
  - `npm run lint` after review fixes (pass)
  - `npm test` after review fixes (pass, 690 tests)
  - `npm run build` after review fixes (pass; existing multiple-lockfile workspace-root warning only)
- Implementation agents:
  - Recommended: `typescript-pro`, `test-automator`
  - Used: no new implementation agent; prior `typescript-pro` guidance was already incorporated, and the explicit comparator blocker plus focused permutation regressions did not justify overlapping advice
- Specialist review:
  - Agents used: initial and targeted `code-reviewer` and `qa-expert` passes
  - Findings: fixed one high and one deduplicated medium finding by stripping TeamRanker moveset suffixes through the shared simulation-cell parser before opponent resolution and by rejecting active alternatives when the default is incomplete; targeted re-review found none
- Decisions (why):
  - Quantized each ranking metric by the existing `1e-12` comparison epsilon before lexicographic comparison because independent integer ranks form a transitive ordering while retaining stable canonical-ID tie-breaking.
  - Left the raw positive-improvement activation threshold unchanged so the fix affects deterministic ordering only, not eligibility, top/full weighting, primary improvement, or secondary marginal-coverage semantics.
  - Reused the runtime simulation-cell parser in sync evidence and opponent-set checks so parenthesized forms and PvPoke moveset suffixes resolve identically across runtime and tooling.
  - Required default completeness only when an alternative is active, preserving valid default-only manifests while preventing an alternate from claiming comparison against a partial baseline.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - A comparator that returns equality whenever two raw floating-point values are within epsilon is non-transitive across epsilon chains; permutation tests must cover both primary and secondary selection boundaries.
  - Generated TeamRanker CSV cells include compact moveset suffixes such as `H+WBR/EB`; normalizing the full cell erases canonical species identity and silently makes selection evidence sparse.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-013.
  - US-014 must construct and atomically publish one complete manifest per format from candidate metadata and the manifest-ready simulation selections returned here.
- Next iteration start:
  - US-014 - Publish complete manifests atomically
---

## 2026-08-06 13:08:59 MST - US-014
- Implemented all-format moveset manifest preparation from ranking-derived candidate provenance and validated simulation selections; retained acquisition requirements and deterministic source digests; deferred production CSV publication until manifests prepare; and published CSVs plus manifests through a rollback-capable transaction that preserves the prior generation after validation, simulation, staging, CSV replacement, or manifest replacement failure.
- Files changed: `lib/AGENTS.md`, `lib/sync/index.ts`, `lib/sync/index.test.ts`, `lib/sync/movesetCandidates.ts`, `lib/sync/movesetCandidates.test.ts`, `lib/sync/movesetVariantManifest.ts`, `lib/sync/movesetVariantManifest.test.ts`, `lib/sync/simulations.ts`, `lib/sync/simulations.test.ts`, `progress.txt`
- Commit message: `feat: US-014 - Publish complete manifests atomically`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/sync/movesetVariantManifest.test.ts lib/sync/simulations.test.ts` (fail as expected before implementation: publication APIs were missing and forced regeneration deleted the format directory)
  - `npx vitest run lib/sync/movesetVariantManifest.test.ts` after determinism regression (fail as expected: equivalent candidate ordering changed the format source digest)
  - `npx vitest run lib/sync/movesetVariantManifest.test.ts` after temporary-write regression (fail as expected: a partially written temporary file was not cleaned up)
  - `npx vitest run lib/sync/simulations.test.ts -t "generates ranking-derived alternate movesets"` after initial review regression (fail as expected: default CSVs were written before a later alternate simulation failed)
  - `npx vitest run lib/sync/movesetCandidates.test.ts lib/sync/movesetVariantManifest.test.ts lib/sync/simulations.test.ts` (pass, 35 tests)
  - `npx vitest run lib/sync/index.test.ts lib/sync/movesetCandidates.test.ts lib/sync/movesetVariantManifest.test.ts lib/sync/simulations.test.ts` after review fixes (pass, 38 tests)
  - `npx prettier --write "lib/sync/index.ts" "lib/sync/movesetCandidates.ts" "lib/sync/movesetCandidates.test.ts" "lib/sync/movesetVariantManifest.ts" "lib/sync/movesetVariantManifest.test.ts" "lib/sync/simulations.ts" "lib/sync/simulations.test.ts"` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` after review fixes (pass, 697 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/sync/index.test.ts lib/sync/movesetVariantManifest.test.ts` after targeted review regressions (fail as expected: production publication did not receive deferred CSVs and no rollback-capable generation publisher existed)
  - `npx vitest run lib/sync/index.test.ts lib/sync/movesetCandidates.test.ts lib/sync/movesetVariantManifest.test.ts lib/sync/simulations.test.ts` after targeted review fixes (pass, 42 tests)
  - `npx tsc --noEmit` after targeted review fixes (pass)
  - `npm run lint` after targeted review fixes (pass)
  - `npm test` after targeted review fixes (pass, 701 tests)
  - `npm run build` after targeted review fixes (pass; existing multiple-lockfile workspace-root warning only)
- Implementation agents:
  - Recommended: `data-engineer`, `test-automator`
  - Used: `data-engineer`; skipped `test-automator` because one architecture-focused advisor plus test-first manifest, publication, determinism, cleanup, and simulation-preservation coverage fit the standard-mode complex-story budget
- Specialist review:
  - Agents used: initial and targeted `code-reviewer`, `qa-expert`, and `security-engineer` passes
  - Findings: fixed one initial high and three medium findings by delaying every generated CSV write until all simulations validate, using exclusive same-directory atomic CSV replacement, testing validation/simulation failures at the publication orchestration boundary, and constraining manifest targets to exact catalog-derived paths; also fixed one low contradictory AGENTS instruction. The targeted code and security re-review found one deduplicated high batch-integrity gap: a later CSV or manifest failure could leave an old manifest over mixed CSV generations. Fixed it by deferring production writes through manifest preparation, staging every CSV and manifest before replacement, backing up prior targets, replacing manifests last, and rolling back the complete batch on any failure; QA found no additional issues.
- Decisions (why):
  - Prepared and validated every format manifest before staging any temporary files so simulation, cross-validation, or construction failure cannot publish a partial authority set.
  - Preserved existing simulation directories in both non-resume and forced-regeneration flows because deleting a format directory removes the prior manifest before replacement and stale recognized variants belong to post-publication cleanup in US-015.
  - Buffered generated CSV contents in memory and persisted them only after all formats validate so a late simulation failure cannot replace files declared by the prior manifest; production writes use exclusive temporary files and same-directory rename to avoid truncated live CSVs.
  - Kept direct `generateSimulations(...)` publication behavior for existing tooling while making the production orchestrator request deferred files, which minimizes API disruption and gives manifest preparation and batch publication one explicit authority boundary.
  - Staged all new bytes before moving any live target, retained same-directory backups until every manifest replacement succeeded, and restored prior files in reverse generation order when publication failed so handled filesystem errors cannot leave a mixed authoritative generation.
  - Retained preferred, observed, override, and substitution provenance directly on derived candidates so publication does not reconstruct or invent ranking evidence after simulation.
  - Derived format source digests from canonical manifest species content plus exact Pokemon and move source bytes so equivalent candidate ordering produces byte-identical manifest output.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Atomic manifest replacement requires the temporary file to share the target directory; write and rename failures clean only temporary files and never unlink the prior target.
  - Simulation regeneration must bypass reuse for changed formats without deleting the directory, and generated CSV writes must remain buffered until the complete simulation pass validates so the prior manifest generation stays intact on failure.
  - Per-file atomic rename is insufficient when stable storage keys are shared across generations; production publication must treat the generated CSV set and manifests as one recoverable batch, with manifests ordered last and prior targets retained until the whole batch commits.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-014.
  - US-015 must remove only strictly recognized stale variant CSVs after successful manifest publication and must not delete default matrices or files outside the active format directory.
- Next iteration start:
  - US-015 - Delete stale generated variant files after successful sync
---

## 2026-08-06 13:44:18 MST - US-015
- Implemented deterministic post-publication cleanup that validates every catalog-scoped manifest plan before unlinking, preserves default matrices and all declared candidates, ignores malformed files and non-regular entries, deletes only strict-parser stale variant CSVs, and logs each deleted repository-relative path after successful cleanup.
- Files changed: `lib/AGENTS.md`, `lib/sync/index.ts`, `lib/sync/index.test.ts`, `lib/sync/simulationCleanup.ts`, `lib/sync/simulationCleanup.test.ts`, `prd.json`, `progress.txt`
- Commit message: `feat: US-015 - Delete stale generated variant files after successful sync`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/sync/simulationCleanup.test.ts lib/sync/index.test.ts` (fail as expected before implementation: cleanup module was missing and orchestration did not invoke cleanup)
  - `npx vitest run lib/sync/simulationCleanup.test.ts lib/sync/index.test.ts` (pass, 10 tests)
  - `npx vitest run lib/sync/simulationCleanup.test.ts lib/sync/index.test.ts lib/sync/simulationProjection.test.ts lib/sync/movesetVariantManifest.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts` (pass, 36 tests before review fixes; pass, 37 tests after review fixes)
  - `npx vitest run lib/sync/simulationCleanup.test.ts lib/sync/index.test.ts` after initial review regressions (fail as expected: 3 tests exposed missing physical-directory validation and incremental deletion reporting)
  - `npx vitest run lib/sync/simulationCleanup.test.ts lib/sync/index.test.ts` after review fixes (pass, 11 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass after correcting two import-order errors)
  - `npm test` (pass, 707 tests before review; pass, 708 tests after review fixes)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `tooling-engineer`, `test-automator`
  - Used: `tooling-engineer`; skipped `test-automator` because one focused tooling advisor plus the test-first cleanup and orchestration coverage fit the standard-mode budget
- Specialist review:
  - Agents used: initial and targeted `code-reviewer`, `qa-expert`, and `security-engineer` passes
  - Findings: fixed one deduplicated high physical-path finding and one medium partial-reporting finding by rejecting format directories whose real paths differ from their workspace catalog paths, revalidating the physical parent before each unlink, and reporting each successful deletion immediately before later cleanup can fail; targeted re-review found none
- Decisions (why):
  - Kept cleanup outside the rollback-capable publication transaction because stale files are not declared by the new authoritative manifests and deletion must begin only after every new manifest is installed successfully.
  - Preserved storage keys for every non-default manifest candidate, not only active alternatives, because the manifest declares complete bounded simulation evidence that later policy changes may activate.
  - Validated all manifest targets and contents before reading directories or unlinking so a malformed later format cannot cause partial cleanup of earlier formats.
  - Used the projection's strict canonical variant filename parser and regular-file checks so default matrices, malformed names, directories, and symlinks cannot become cleanup targets.
  - Added physical directory validation before enumeration and immediately before every unlink because lexical containment alone cannot prevent a catalog directory symlink from redirecting deletion outside the worktree.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, `test-runner`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Cleanup failures occur after authoritative manifest publication; the sync must fail before freshness metadata is written and allow a later run to retry cleanup rather than roll back a valid generation.
  - Report each successful deletion immediately; cleanup is non-transactional, so a later unlink failure must not hide files already removed from sync output.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-015.
- Next iteration start:
  - US-016 - Load runtime variants exclusively from manifests
---

## 2026-08-06 14:14:52 MST - US-016
- Implemented a format-scoped manifest-backed runtime variant loader that exposes only active candidates, reads exact declared scenario storage keys, validates finite unique rows and scenario/default opponent parity before caching, and raises actionable typed errors for missing, malformed, incompatible, incomplete, or unavailable data. Aggregate and shield lookups now accept explicit variant identities, while runtime moveset selection no longer uses the hardcoded Golisopod variant map.
- Files changed: `lib/AGENTS.md`, `lib/data/movesetVariantSimulations.ts`, `lib/data/movesetVariantSimulations.test.ts`, `lib/data/simulations.ts`, `lib/data/simulations.test.ts`, `lib/genetic/moveset.ts`, `lib/genetic/moveset.test.ts`, `progress.txt`
- Commit message: `feat: US-016 - Load runtime variants exclusively from manifests`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/data/movesetVariantSimulations.test.ts lib/genetic/moveset.test.ts` (fail as expected before implementation: manifest-backed loader module and injected active-variant boundary were missing)
  - `npx vitest run lib/genetic/moveset.test.ts -t "until a format manifest is published"` (fail as expected before transition handling: absent checked-in manifest reached lineup moveset selection)
  - `npx vitest run lib/data/movesetVariantSimulations.test.ts lib/data/simulations.test.ts lib/genetic/moveset.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts` (pass, 24 tests after implementation and transition fix)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass after one import-order correction)
  - `npm test` before transition fix (fail as expected: 714 pass, 2 manifest-missing failures in calibration and representative generation)
  - `npm test` after transition fix (pass, 719 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/data/movesetVariantSimulations.test.ts lib/genetic/moveset.test.ts` after initial review regressions (fail as expected: 4 tests exposed unvalidated active storage, present-manifest ranked fallback, absent-species fallback, and blank numeric acceptance)
  - `npx vitest run lib/data/movesetVariantSimulations.test.ts lib/genetic/moveset.test.ts lib/data/simulations.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts` after review fixes (pass, 30 tests)
  - Final `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build`, and diff checks after review fixes (pass, 723 tests; existing multiple-lockfile workspace-root warning only)
- Implementation agents:
  - Recommended: `backend-developer`, `architect-reviewer`
  - Used: `architect-reviewer`; skipped `backend-developer` because one architecture-focused advisor covered the runtime loader contract, error boundary, cache scoping, and migration risk without redundant implementation guidance
- Specialist review:
  - Agents used: initial `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `code-reviewer` and `qa-expert` re-review
  - Findings: fixed three deduplicated medium findings by validating every active candidate's declared storage before exposure, rejecting absent species and using the manifest default whenever a present manifest governs selection, and rejecting blank numeric CSV cells. Discarded the suggested exact `evaluationCounts` row equality because those counts represent the bounded top/full comparison pool while simulation CSVs intentionally retain the broader opponent matrix; initial security and targeted code/QA reviews found no remaining actionable issues.
- Decisions (why):
  - Added an injected loader instance instead of filesystem monkey-patching or global cache-clearing hooks so tests use deterministic repository-path fixtures and production keeps format-scoped caches.
  - Kept unqualified default matrix loading unchanged because checked-in manifests are intentionally regenerated in US-024, but routed every explicit variant and active-candidate lookup through manifest authority.
  - Allowed only `manifest-missing` to retain the ranked default at the high-level team moveset selector during the staged-data transition; present malformed, incompatible, incomplete, and unavailable authority still fails closed.
  - Loaded all required scenarios and compared exact opponent identities against the candidate and its default before caching so a partial alternate cannot silently borrow or appear equivalent to default rows.
  - Required CSV rows to contain nonblank finite numeric fields, but retained the `parsed rows >= evaluationCounts` invariant because manifest evaluation counts cover only the bounded selection pools rather than the complete simulation opponent matrix.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - `parseMovesetVariantManifestJson(...)` validates structural manifest identity, while runtime completeness must additionally validate declared files, finite unique CSV rows, scenario parity, and default-versus-alternate opponent parity.
  - Runtime variant caches must include format, canonical species, and canonical variant identity; the same variant ID can validly resolve to different matrices in different formats.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-016.
  - No manifests are checked in yet; US-024 must regenerate them for every format, after which runtime selection will consume active manifest candidates instead of the temporary ranked-default transition path.
- Next iteration start:
  - US-017 - Create immutable roster moveset assignments
---

## 2026-08-06 14:44:29 MST - US-017
- Implemented deeply immutable roster moveset assignments resolved once against the complete bring-six roster; preserved selected variant IDs; bound assignments into fast and full scoring before all 120 ordered lineups; exposed narrow manifest policy identity metadata; and isolated full and fast lineup caches with deterministic format-, policy-, and variant-sensitive fingerprints.
- Files changed: `lib/AGENTS.md`, `lib/types.ts`, `lib/data/movesetVariantSimulations.ts`, `lib/data/movesetVariantSimulations.test.ts`, `lib/data/simulations.ts`, `lib/genetic/moveset.ts`, `lib/genetic/moveset.test.ts`, `lib/genetic/fitness/lineupScoring.ts`, `lib/genetic/fitness/lineupScoring.test.ts`, `lib/genetic/fitness/rosterScoring.ts`, `lib/genetic/fitness/rosterScoring.test.ts`, `lib/genetic/fitness/index.ts`, `lib/genetic/fitness/index.test.ts`, `lib/genetic/algorithm.ts`, `lib/genetic/algorithm.test.ts`, `progress.txt`
- Commit message: `feat: US-017 - Create immutable roster moveset assignments`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/data/movesetVariantSimulations.test.ts lib/genetic/moveset.test.ts lib/genetic/fitness/lineupScoring.test.ts lib/genetic/fitness/index.test.ts` (fail as expected before implementation: 8 tests exposed missing policy identity, assignment creation/resolution, assignment binding, and cache isolation)
  - `npx vitest run lib/data/movesetVariantSimulations.test.ts lib/genetic/moveset.test.ts lib/genetic/fitness/lineupScoring.test.ts lib/genetic/fitness/rosterScoring.test.ts lib/genetic/fitness/index.test.ts lib/genetic/algorithm.test.ts` (pass, 123 tests)
  - `npx vitest run lib/genetic/moveset.test.ts lib/genetic/fitness/lineupScoring.test.ts lib/genetic/fitness/rosterScoring.test.ts lib/genetic/algorithm.test.ts` after initial review regressions (fail as expected: 3 tests exposed incomplete-assignment fallback, incomplete fingerprint identity, and eager ranked fallback behavior)
  - `npx vitest run lib/genetic/moveset.test.ts lib/genetic/fitness/lineupScoring.test.ts lib/genetic/fitness/rosterScoring.test.ts lib/genetic/fitness/index.test.ts lib/genetic/algorithm.test.ts` after review fixes (pass, 115 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (fail before import-order correction; pass after correction)
  - `npm test` after review fixes (pass, 735 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `architect-reviewer`, `typescript-pro`
  - Used: `architect-reviewer`; skipped `typescript-pro` because one architecture-focused advisor covered assignment identity, context binding, transition policy, cache isolation, and the US-018 boundary within the standard-mode complex-story budget
- Specialist review:
  - Agents used: initial and targeted `code-reviewer` and `qa-expert` passes
  - Findings: fixed five deduplicated initial medium findings by rejecting incomplete and cross-format assignments before enumeration, making ranked fallback lookup lazy, fingerprinting default status, observing species-specific assigned moves across all 120 lineups, and asserting final diagnostics bind the resolved assignment; targeted code review found none, but targeted QA found one remaining medium resolver-level fallback injection blocker
- Decisions (why):
  - Evolved the previously unused assignment map alias into a structured immutable value carrying format, policy authority, canonical variants, and fingerprint so later API propagation cannot detach moves from their scoring identity.
  - Fingerprinted canonical JSON containing format, manifest schema/policy, sorted species, variant IDs, and exact move slots rather than delimiter concatenation, preventing ambiguous cache identities without adding a hashing dependency.
  - Resolved the entire roster under either manifest authority or the explicit ranked-default transition policy; only a missing manifest activates fallback, while malformed, incompatible, incomplete, and unavailable authority continues to fail closed.
  - Bound assignments to scoring contexts while leaving opponent default-moveset lookup intact and deferred assigned-variant matchup matrices, shield paths, and threat diagnostics to US-018.
  - Failed closed when a bound assignment is incomplete, contains non-roster species, or belongs to another format so cache fingerprints always describe the exact moves consumed by scoring.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Manifest policy identity can be exposed narrowly as schema version plus policy version without leaking the full manifest into optimizer code.
  - Assignment-sensitive cache isolation requires the assignment fingerprint in both fast and full lineup namespaces even when ordered species IDs are unchanged.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-017.
  - Commit blocked after the one allowed targeted re-review: add an injected ranked-default boundary to `RosterMovesetAssignmentDependencies`, use it only when manifest policy lookup returns `manifest-missing`, and add resolver-level lazy-success and injected-fallback regressions before starting a fresh review stabilization loop.
  - US-018 must route aggregate and shield-scenario matchup reads through each assigned variant while preserving missing rows as unevaluated.
- Next iteration start:
  - Resume US-017 by injecting resolver-level ranked fallback, rerunning checks, and completing a fresh staged review loop before marking the story passed.
---

## 2026-08-06 15:05:30 MST - US-017
- Resumed the immutable roster assignment candidate by injecting ranked-default resolution at the roster resolver boundary, keeping it lazy while manifest authority is available, and using it for every species only when policy lookup reports a missing manifest.
- Files changed: `lib/AGENTS.md`, `lib/types.ts`, `lib/data/movesetVariantSimulations.ts`, `lib/data/movesetVariantSimulations.test.ts`, `lib/data/simulations.ts`, `lib/genetic/moveset.ts`, `lib/genetic/moveset.test.ts`, `lib/genetic/fitness/lineupScoring.ts`, `lib/genetic/fitness/lineupScoring.test.ts`, `lib/genetic/fitness/rosterScoring.ts`, `lib/genetic/fitness/rosterScoring.test.ts`, `lib/genetic/fitness/index.ts`, `lib/genetic/fitness/index.test.ts`, `lib/genetic/algorithm.ts`, `lib/genetic/algorithm.test.ts`, `progress.txt`
- Commit message: `feat: US-017 - Create immutable roster moveset assignments`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/genetic/moveset.test.ts` before resolver fix (fail as expected: injected resolver fallback was ignored)
  - `npx vitest run lib/genetic/moveset.test.ts` after resolver fix (pass, 12 tests)
  - `npx vitest run lib/data/movesetVariantSimulations.test.ts lib/genetic/moveset.test.ts lib/genetic/fitness/lineupScoring.test.ts lib/genetic/fitness/rosterScoring.test.ts lib/genetic/fitness/index.test.ts lib/genetic/algorithm.test.ts` (pass, 129 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 736 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check && git diff --cached --check` (pass)
  - `npx vitest run lib/genetic/moveset.test.ts lib/genetic/fitness/index.test.ts lib/genetic/fitness/rosterScoring.test.ts` after initial review regressions (fail as expected: omitted scoring formats bypassed two cross-format assignment guards)
  - `npx vitest run lib/data/movesetVariantSimulations.test.ts lib/genetic/moveset.test.ts lib/genetic/fitness/lineupScoring.test.ts lib/genetic/fitness/rosterScoring.test.ts lib/genetic/fitness/index.test.ts lib/genetic/algorithm.test.ts` after review fixes (pass, 135 tests)
  - Final `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build`, and diff checks after review fixes (pass, 742 tests; existing multiple-lockfile workspace-root warning only)
- Implementation agents:
  - Recommended: `architect-reviewer`, `typescript-pro`
  - Used: no new implementation agent; the prior `architect-reviewer` guidance was already incorporated, and the remaining explicit dependency-injection blocker did not justify overlapping advice
- Specialist review:
  - Agents used: fresh initial `code-reviewer` and `qa-expert`; targeted `qa-expert` re-review
  - Findings: initial code review found none; fixed two medium QA findings by normalizing omitted scoring formats before assignment validation and covering every typed non-missing resolver failure without ranked fallback; targeted QA re-review found none
- Decisions (why):
  - Added ranked fallback to `RosterMovesetAssignmentDependencies` rather than calling ranking data directly so resolver behavior is deterministic and testable without module patching.
  - Selected the manifest policy before resolving any roster member, then invoked exactly one assignment strategy for the complete roster so a roster cannot mix manifest-backed and transition-fallback identities.
  - Kept the fallback lazy and limited to the typed `manifest-missing` case; malformed, incompatible, incomplete, and unavailable manifest authority still fails closed.
  - Normalized absent scoring-context formats to `DEFAULT_BATTLE_FORMAT_ID` at both binding and direct roster-scoring validation boundaries so optional format syntax cannot weaken cache or assignment isolation.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` and `test-driven-development` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Transition fallbacks must be injected at every orchestration boundary that invokes them, not only at lower-level selectors, so tests can prove both lazy success and exact fallback behavior.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-017.
  - US-018 must route aggregate and shield-scenario matchup reads through each assigned variant while preserving missing rows as unevaluated.
- Next iteration start:
  - US-018 - Make ordered lineup scoring variant-aware.
---

## 2026-08-06 15:31:16 MST - US-018
- Implemented assignment-aware aggregate and shield matchup lookup for ordered lineup and fast roster scoring; extended production matchup-quality reads to explicit variants; aligned coverage, soft matchup quality, safety, overwhelming-loss checks, structure adjustments, shield reliability, resource paths, roster consistency, and offensive move typing with one fixed assignment; and excluded missing shield rows as unevaluated.
- Files changed: `lib/AGENTS.md`, `lib/data/simulations.ts`, `lib/genetic/fitness/lineupScoring.ts`, `lib/genetic/fitness/lineupScoring.test.ts`, `lib/genetic/fitness/rosterScoring.ts`, `lib/genetic/fitness/rosterScoring.test.ts`, `prd.json`, `progress.txt`
- Commit message: `feat: US-018 - Make ordered lineup scoring variant-aware`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/genetic/fitness/lineupScoring.test.ts lib/genetic/fitness/rosterScoring.test.ts` before implementation (fail as expected: 4 tests exposed unqualified aggregate, shield, and matchup-quality reads plus synthetic neutral shield rows)
  - `npx vitest run lib/genetic/fitness/lineupScoring.test.ts lib/genetic/fitness/rosterScoring.test.ts lib/data/simulations.test.ts lib/genetic/fitness/index.test.ts lib/genetic/fitness/optimizerValidationFixtures.test.ts` (pass, 111 tests)
  - `npx prettier --write "lib/AGENTS.md" "lib/data/simulations.ts" "lib/genetic/fitness/lineupScoring.ts" "lib/genetic/fitness/lineupScoring.test.ts" "lib/genetic/fitness/rosterScoring.ts" "lib/genetic/fitness/rosterScoring.test.ts"` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 745 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/genetic/fitness/lineupScoring.test.ts lib/genetic/fitness/rosterScoring.test.ts` after review regressions (pass, 90 tests)
  - `npx prettier --write "lib/genetic/fitness/lineupScoring.test.ts" "lib/genetic/fitness/rosterScoring.test.ts"` after review regressions (pass; unchanged)
  - `npx tsc --noEmit` after review regressions (pass)
  - `npm run lint` after review regressions (pass)
  - `git diff --check && git diff --cached --check` after review regressions (pass)
- Implementation agents:
  - Recommended: `performance-engineer`, `test-automator`
  - Used: `performance-engineer`; skipped `test-automator` because one scoring/performance advisor plus test-first full, fast, ABC, ABB, ABA, threshold, and shield-path regressions covered the standard-mode complex-story budget
- Specialist review:
  - Agents used: initial `code-reviewer` and `qa-expert`; targeted `qa-expert` re-review
  - Findings: code review found none; fixed three initial medium QA coverage findings by strengthening full-lineup qualification assertions, adding assignment-sensitive ABA structure and roster shield-stability regressions, and covering unqualified ranked-default fallback reads. Targeted QA found two remaining medium assertion gaps, so the commit is blocked under the one-re-review limit. Deferred one low direct data-layer quality test because checked-in manifests are intentionally absent until US-024 and existing manifest-loader tests cover explicit variant aggregate reads.
- Decisions (why):
  - Added optional variant identity to existing injected matchup boundaries instead of introducing parallel scoring contexts, keeping old deterministic test fakes and ranked-default transition behavior compatible.
  - Passed canonical variant IDs only for manifest-backed assignments because ranked-default fallback intentionally uses existing unqualified matrices while manifests are not checked in.
  - Centralized assignment-aware aggregate, shield, and quality reads in lineup scoring helpers so full and fast paths cannot drift or independently select movesets.
  - Left lineup and roster Threat Score diagnostic adapters unqualified for US-019, which explicitly owns variant-aware roster diagnostics and analysis, while keeping Threat Score outside weighted fitness.
  - Excluded null shield rows from averages rather than inserting rating 500 because the story and module guidance require missing rows to remain unevaluated.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` and `test-driven-development` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - The runtime simulation APIs already accept explicit variant IDs for aggregate and shield lookups; matchup-quality mean and median helpers needed the same optional identity to keep lineup consistency aligned.
  - Manifest-backed roster assignments should pass variant IDs to repository-owned matrices, while the temporary ranked-default fallback must continue using unqualified files.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-018.
  - Commit blocked after the one allowed targeted re-review: make the fast-lineup regression assert every recorded aggregate and shield tuple matches its species assignment, and assert fallback shield and matchup-quality read arrays are non-empty before their `every(...)` checks; then rerun focused checks and start a fresh review loop.
  - US-019 must route roster Threat Score, threat analysis, shield-scenario analysis, and Pokemon-contribution analysis through the same assignment without adding diagnostics to GA hot paths.
- Next iteration start:
  - Resume US-018 by fixing the two remaining QA assertion gaps, rerunning checks, and completing a fresh staged review loop before marking the story passed.
---

## 2026-08-06 15:51:58 MST - US-018
- Resumed the assignment-aware lineup scoring candidate by strengthening the fast-path regression to reject every mismatched aggregate or shield qualifier and proving fallback shield and matchup-quality reads occur before asserting they remain unqualified.
- Files changed: `lib/AGENTS.md`, `lib/data/simulations.ts`, `lib/genetic/fitness/lineupScoring.ts`, `lib/genetic/fitness/lineupScoring.test.ts`, `lib/genetic/fitness/rosterScoring.ts`, `lib/genetic/fitness/rosterScoring.test.ts`, `prd.json`, `progress.txt`
- Commit message: `feat: US-018 - Make ordered lineup scoring variant-aware`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/genetic/fitness/lineupScoring.test.ts lib/genetic/fitness/rosterScoring.test.ts` (pass, 90 tests)
  - `npx prettier --write "lib/genetic/fitness/lineupScoring.test.ts" "lib/genetic/fitness/rosterScoring.test.ts"` (pass; unchanged)
  - Parallel `npx tsc --noEmit` with `npm run build` (fail: transient `.next/types/validator.ts` race while Next rebuilt generated types)
  - `npx tsc --noEmit` after build completed (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 748 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check && git diff --cached --check` (pass)
- Implementation agents:
  - Recommended: `performance-engineer`, `test-automator`
  - Used: `performance-engineer`; skipped `test-automator` because the remaining work was limited to two exact QA assertion gaps and another implementation advisor would duplicate the existing test strategy
- Specialist review:
  - Agents used: fresh `code-reviewer` and `qa-expert`
  - Findings: no blocking findings; `code-reviewer` found none, and the one low QA suggestion for a production manifest-backed quality-forwarding test is deferred until checked-in manifests are available in US-024
- Decisions (why):
  - Kept the production candidate unchanged because the prior review identified assertion vacuity rather than a scoring defect, and the strengthened tests pass against the existing implementation.
  - Asserted both per-species coverage and all-read qualification so a future extra unqualified read cannot hide behind one correct call for each lineup member.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` and `test-driven-development` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Assertions using `every(...)` need a separate non-empty assertion when no earlier assertion guarantees the same collection was populated.
  - Avoid running standalone TypeScript checks concurrently with `next build` because Next can replace generated `.next/types` files during compilation.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-018.
  - Add direct manifest-backed matchup-quality production wiring coverage after US-024 checks in the required manifest fixtures; current injected-context and explicit-variant loader tests cover the story behavior.
- Next iteration start:
  - US-019 - Make roster diagnostics and analysis variant-aware.
---

## 2026-08-06 16:13:25 MST - US-019
- Implemented assignment-aware lineup and roster Threat Score reads; retained the exact final PlayPokemon roster assignment for downstream diagnostics; derived threat analysis from the existing final Threat Score; and qualified shield-scenario and Pokemon-contribution matrix reads with the same assignment while preserving ranked-default fallback and missing-row behavior.
- Files changed: `lib/AGENTS.md`, `lib/types.ts`, `lib/genetic/moveset.ts`, `lib/genetic/algorithm.ts`, `lib/genetic/algorithm.test.ts`, `lib/genetic/fitness/lineupScoring.ts`, `lib/genetic/fitness/lineupScoring.test.ts`, `lib/genetic/fitness/rosterScoring.ts`, `lib/genetic/fitness/rosterScoring.test.ts`, `lib/analysis/threatAnalysis.ts`, `lib/analysis/threatAnalysis.test.ts`, `lib/analysis/shieldScenarioAnalysis.ts`, `lib/analysis/shieldScenarioAnalysis.test.ts`, `lib/analysis/pokemonContributionAnalysis.ts`, `lib/analysis/pokemonContributionAnalysis.test.ts`, `app/api/generate-team/route.ts`, `app/api/generate-team/route.test.ts`, `prd.json`, `progress.txt`
- Commit message: `feat: US-019 - Make roster diagnostics and analysis variant-aware`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/genetic/fitness/lineupScoring.test.ts lib/genetic/fitness/rosterScoring.test.ts lib/analysis/threatAnalysis.test.ts lib/analysis/shieldScenarioAnalysis.test.ts lib/analysis/pokemonContributionAnalysis.test.ts lib/genetic/algorithm.test.ts app/api/generate-team/route.test.ts` before implementation (fail as expected: 12 tests exposed unqualified Threat Score and analysis reads, discarded assignment identity, and missing API propagation)
  - `npx vitest run lib/genetic/fitness/lineupScoring.test.ts lib/genetic/fitness/rosterScoring.test.ts lib/analysis/threatAnalysis.test.ts lib/analysis/shieldScenarioAnalysis.test.ts lib/analysis/pokemonContributionAnalysis.test.ts lib/genetic/algorithm.test.ts app/api/generate-team/route.test.ts` (pass, 142 tests)
  - `npx prettier --write "app/api/generate-team/route.ts" "app/api/generate-team/route.test.ts" "lib/types.ts" "lib/genetic/moveset.ts" "lib/genetic/algorithm.ts" "lib/genetic/algorithm.test.ts" "lib/genetic/fitness/lineupScoring.ts" "lib/genetic/fitness/lineupScoring.test.ts" "lib/genetic/fitness/rosterScoring.ts" "lib/genetic/fitness/rosterScoring.test.ts" "lib/analysis/threatAnalysis.ts" "lib/analysis/threatAnalysis.test.ts" "lib/analysis/shieldScenarioAnalysis.ts" "lib/analysis/shieldScenarioAnalysis.test.ts" "lib/analysis/pokemonContributionAnalysis.ts" "lib/analysis/pokemonContributionAnalysis.test.ts"` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 751 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/genetic/fitness/lineupScoring.test.ts lib/genetic/fitness/rosterScoring.test.ts lib/analysis/shieldScenarioAnalysis.test.ts lib/analysis/pokemonContributionAnalysis.test.ts` after review fixes (pass, 104 tests)
  - `npx tsc --noEmit`, `npm run lint`, `npm test`, and staged/unstaged diff checks after review fixes (pass, 754 tests)
- Implementation agents:
  - Recommended: `performance-engineer`, `test-automator`
  - Used: `performance-engineer`, `test-automator`; both fit the standard-mode complex-story budget and provided non-overlapping final-diagnostic data-flow and regression-test guidance
- Specialist review:
  - Agents used: initial `code-reviewer` and `qa-expert`; targeted `qa-expert` re-review
  - Findings: code review found none; fixed two medium QA findings by covering ranked-default fallback across every new diagnostic consumer and directly proving Threat Score diagnostics do not alter weighted components or fitness; targeted QA re-review found none
- Decisions (why):
  - Derived `ThreatAnalysis` from the existing final `OptimizerThreatScore` so the API does not repeat the roster-by-threat aggregate simulation pass and cannot drift from the assignment used for scoring.
  - Retained the exact immutable assignment on the internal final chromosome but did not expose it in the HTTP response because structured API assignment propagation belongs to US-022.
  - Centralized manifest-versus-ranked-default simulation qualification in `getAssignedMovesetVariantId(...)` so scoring and analysis use one policy without selecting moves in diagnostic modules.
  - Left top-meta/full-meta Threat Score weights and normalized weighted fitness unchanged; assignment changes diagnostic matchup inputs only, and fast/GA scoring still omits Threat Score.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` and `test-driven-development` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Final full PlayPokemon scoring is the first point that simultaneously owns the selected assignment and complete roster Threat Score, so downstream analyses should consume that output rather than reconstruct policy.
  - Assignment qualification tests must assert lookup collections are non-empty before `every(...)` checks and make wrong or missing qualifiers produce opposite matchup outcomes.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-019.
  - US-022 must expose the retained assignment through generation and team-details API contracts; this story keeps it internal while synchronizing existing analysis output.
- Next iteration start:
  - US-020 - Retain a deterministic top-ten GA finalist set.
---

## 2026-08-06 18:13:28 MST - US-020
- Implemented ranked-default-only PlayPokemon population scoring and a bounded cross-generation archive of the ten strongest default-scored rosters; canonicalized roster identity by preserving explicit anchor order and sorting flexible members; deduplicated reordered copies; and used canonical lexical keys for deterministic equal-fitness ordering while leaving `bestOverall` selection and one-roster full diagnostics unchanged.
- Files changed: `lib/AGENTS.md`, `lib/types.ts`, `lib/genetic/algorithm.ts`, `lib/genetic/algorithm.test.ts`, `lib/genetic/algorithm.performance.test.ts`, `lib/genetic/fitness/index.ts`, `lib/genetic/fitness/index.test.ts`, `lib/genetic/fitness/lineupScoring.ts`, `lib/genetic/fitness/lineupScoring.test.ts`, `progress.txt`
- Commit message: `feat: US-020 - Retain a deterministic top-ten GA finalist set`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/genetic/fitness/index.test.ts lib/genetic/algorithm.test.ts` before implementation (fail as expected: three tests exposed assignment-backed GA scoring and the missing finalist archive)
  - `npx vitest run lib/genetic/fitness/index.test.ts lib/genetic/algorithm.test.ts` after implementation (pass, 24 tests)
  - `npx vitest run lib/genetic/algorithm.test.ts lib/genetic/algorithm.performance.test.ts lib/genetic/fitness/index.test.ts lib/genetic/fitness/lineupScoring.test.ts lib/genetic/fitness/rosterScoring.test.ts` (pass, 118 tests; representative generation completed under one minute)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (fail before removing one unused import; pass afterward)
  - `npm test` (pass, 756 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/genetic/algorithm.test.ts lib/genetic/fitness/index.test.ts` after review regression (pass, 25 tests)
  - `npx tsc --noEmit`, `npm run lint`, and staged/unstaged diff checks after review regression (pass)
  - `npx vitest run lib/genetic/fitness/index.test.ts` before GA policy-scope fix (fail as expected: the fast context did not explicitly request ranked-default movesets)
  - `npx vitest run lib/genetic/fitness/index.test.ts lib/genetic/algorithm.test.ts lib/genetic/fitness/lineupScoring.test.ts` after GA policy-scope fix (pass, 60 tests)
  - Final `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build`, and diff checks after GA policy-scope fix (pass, 757 tests; existing multiple-lockfile workspace-root warning only)
- Implementation agents:
  - Recommended: `performance-engineer`, `test-automator`
  - Used: `performance-engineer`; skipped `test-automator` because one performance-focused advisor plus test-first algorithm, cache-boundary, deterministic-order, and representative-generation coverage fit the standard-mode complex-story budget
- Specialist review:
  - Agents used: `code-reviewer`, `qa-expert`
  - Findings: initial specialists found no blocking issues; applied the low QA suggestion with explicit multi-anchor canonicalization coverage; self-review narrowed ranked-default behavior to the per-run context; targeted code review found none, but targeted QA found one remaining medium blocker because that context is shared by GBL evolution and still changes GBL from team-aware scoring
- Decisions (why):
  - Stored canonical finalist chromosome snapshots instead of rerunning scoring because US-020 must retain default evidence without adding variant-aware or full diagnostic work to evolution.
  - Preserved explicit anchor prefix order while sorting only flexible roster members because anchor positions are generation constraints but flexible bring-six order does not define a distinct roster.
  - Kept the existing strict-improvement `bestOverall` update and final full-scoring path unchanged so the new archive is available for US-021 without changing the selected roster early.
  - Removed assignment resolution and team-aware variant selection from fast population scoring so checked-in manifests cannot silently make the GA hot path variant-aware.
  - Added an explicit lineup-context moveset policy instead of globally changing default context behavior so final GBL and other non-GA consumers retain their existing team-aware selection semantics.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - A deterministic finalist archive needs a canonical stored team representation as well as a canonical key; otherwise deduplication can preserve encounter-order differences in the retained chromosome.
  - `LineupAwareFitnessContext.resolveMovesetAssignment` remains available for post-evolution full scoring, but fast PlayPokemon population evaluation must keep its lineup cache in the unassigned ranked-default namespace.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-020.
  - Commit blocked after the one allowed targeted re-review: make lineup-context moveset policy mode-specific so only PlayPokemon evolution requests `ranked-default`, retain `team-aware` GBL evolution, and add a direct GBL policy regression before starting a fresh review loop.
  - US-021 should consume `defaultScoredFinalists` for bounded assignment prefiltering and full variant-aware reranking without changing the GA population-scoring path.
- Next iteration start:
  - Resume US-020 by making GA moveset policy mode-specific, rerunning checks, and completing a fresh staged review loop before marking the story passed.
---

## 2026-08-06 18:31:05 MST - US-020
- Resumed the deterministic finalist candidate by making the GA scoring context moveset policy explicit per tournament mode: PlayPokemon evolution remains ranked-default-only, while GBL evolution retains team-aware moveset selection. Added direct mode-policy regressions without changing finalist retention, `bestOverall`, or final diagnostics behavior.
- Files changed: `lib/AGENTS.md`, `lib/types.ts`, `lib/genetic/algorithm.ts`, `lib/genetic/algorithm.test.ts`, `lib/genetic/fitness/index.ts`, `lib/genetic/fitness/index.test.ts`, `lib/genetic/fitness/lineupScoring.ts`, `progress.txt`
- Commit message: `feat: US-020 - Retain a deterministic top-ten GA finalist set`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/genetic/algorithm.test.ts -t "loads top-ranked names|builds bounded full diagnostics"` before implementation (fail as expected: both modes created a context without an explicit mode policy)
  - `npx vitest run lib/genetic/fitness/index.test.ts lib/genetic/algorithm.test.ts` after implementation (pass, 25 tests)
  - `npx prettier --write "lib/genetic/algorithm.ts" "lib/genetic/algorithm.test.ts" "lib/genetic/fitness/index.ts" "lib/genetic/fitness/index.test.ts"` (pass)
  - `npx vitest run lib/genetic/algorithm.test.ts lib/genetic/algorithm.performance.test.ts lib/genetic/fitness/index.test.ts lib/genetic/fitness/lineupScoring.test.ts lib/genetic/fitness/rosterScoring.test.ts` (pass, 119 tests; representative generation completed under one minute)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 757 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check && git diff --cached --check` (pass)
  - `npx vitest run lib/genetic/fitness/lineupScoring.test.ts lib/genetic/algorithm.performance.test.ts` after initial review fixes (fail before correcting an invalid exact-ten fixture assumption; pass afterward, 39 tests)
  - Final `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build`, and diff checks after review fixes (pass, 760 tests; existing multiple-lockfile workspace-root warning only)
- Implementation agents:
  - Recommended: `performance-engineer`, `test-automator`
  - Used: no new agent in this resumed iteration; prior `performance-engineer` guidance was already incorporated, and the remaining explicit mode-policy blocker did not justify overlapping implementation advice
- Specialist review:
  - Agents used: fresh initial `code-reviewer` and `qa-expert`; targeted `qa-expert` re-review
  - Findings: code review found none; fixed two medium QA findings by testing moveset policy at the production provider boundary and running complete generation twice with a reset fixed random sequence to compare bounded finalist identities, fitnesses, and ordering; targeted QA re-review found none
- Decisions (why):
  - Added an explicit `LineupMovesetPolicy` argument to per-run context creation so callers cannot accidentally make every tournament mode ranked-default-only.
  - Selected `ranked-default` only for PlayPokemon evolution because US-020 requires default-scored bring-six finalists; retained `team-aware` for GBL because GBL has no finalist reranking boundary and must preserve existing assignment-aware scoring.
  - Kept one context per GA run and left post-evolution full diagnostics unchanged so cache behavior and existing final output remain stable.
  - Asserted the fixed-seed archive is non-empty and bounded rather than exactly ten because small populations can legitimately produce fewer than ten unique canonical rosters.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - A scoring policy intended for finalist reranking must be scoped to the tournament mode that owns that reranking boundary; applying it in a shared context factory can silently regress another mode.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-020.
  - US-021 should consume `defaultScoredFinalists` for bounded assignment prefiltering and full variant-aware reranking without changing the PlayPokemon GA population-scoring path.
- Next iteration start:
  - US-021 - Prefilter assignments and rerank roster finalists.
---

## 2026-08-06 19:01:30 MST - US-021
- Implemented deterministic post-evolution assignment enumeration and finalist reranking: each retained roster evaluates at most 729 immutable assignments through a cached aggregate matchup objective, only twelve assignments receive full 120-lineup scoring, and the final winner uses variant-aware fitness followed by default-only fitness and canonical roster identity. Final diagnostics reuse assignment-fingerprinted lineup cache entries and expose bounded work counters.
- Files changed: `lib/AGENTS.md`, `lib/types.ts`, `lib/types.lineup.test.ts`, `lib/genetic/moveset.ts`, `lib/genetic/moveset.test.ts`, `lib/genetic/algorithm.ts`, `lib/genetic/algorithm.test.ts`, `lib/genetic/algorithm.performance.test.ts`, `lib/genetic/fitness/index.ts`, `lib/genetic/fitness/index.test.ts`, `lib/genetic/fitness/rosterScoring.ts`, `lib/genetic/fitness/finalistReranking.ts`, `lib/genetic/fitness/finalistReranking.test.ts`, `progress.txt`
- Commit message: `feat: US-021 - Prefilter assignments and rerank roster finalists`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/genetic/fitness/finalistReranking.test.ts lib/genetic/moveset.test.ts` (fail as expected before implementation: reranking module and assignment enumeration API were missing)
  - `npx vitest run lib/genetic/algorithm.test.ts -t "uses bounded variant-aware reranking"` (fail as expected before orchestration: finalist reranker was not called)
  - `npx vitest run lib/genetic/fitness/finalistReranking.test.ts -t "prefilters every bounded assignment"` (fail as expected before matchup precomputation: 8,748 matrix reads instead of 36)
  - `npx vitest run lib/genetic/fitness/finalistReranking.test.ts lib/genetic/moveset.test.ts lib/genetic/algorithm.test.ts lib/genetic/fitness/index.test.ts lib/genetic/fitness/rosterScoring.test.ts lib/types.lineup.test.ts` (pass, 107 tests)
  - `npx vitest run lib/genetic/fitness/finalistReranking.test.ts lib/genetic/algorithm.performance.test.ts` (pass, 6 tests; representative generation about 1.2 seconds)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (fail before removing one unused test parameter; pass afterward)
  - `npm test` (pass, 766 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/genetic/fitness/index.test.ts -t "distinct top and full-meta"` after initial review regression (fail as expected: full-meta wiring still requested 50 threats)
  - `npx vitest run lib/genetic/fitness/finalistReranking.test.ts -t "does not evaluate all 120"` after initial review regression (fail as expected: a 119-lineup full score was accepted)
  - `npx vitest run lib/genetic/fitness/index.test.ts lib/genetic/fitness/finalistReranking.test.ts lib/genetic/algorithm.test.ts lib/genetic/algorithm.performance.test.ts` after review fixes (pass, 35 tests)
  - Final `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build`, and diff checks after review fixes (pass, 769 tests; existing multiple-lockfile workspace-root warning only)
- Implementation agents:
  - Recommended: `performance-engineer`, `test-automator`
  - Used: `performance-engineer`, `test-automator`; both fit the standard-mode complex-story budget and provided non-overlapping bounded-scoring architecture and regression-test guidance
- Specialist review:
  - Agents used: initial and targeted `code-reviewer` and `qa-expert` passes
  - Findings: fixed two medium findings by using distinct 50-entry top-meta and 100-entry full-meta pools, requiring every full assignment score to evaluate all 120 lineups, and directly asserting twelve assignment namespaces produce 1,440 misses plus 120 winner-reuse hits; also fixed the low final logging/validation ordering issue; targeted re-review found none
- Decisions (why):
  - Kept PlayPokemon GA evolution ranked-default-only and created a separate post-evolution team-aware context so assignment work cannot enter the hot population-scoring path or alter GBL behavior.
  - Used a lightweight aggregate objective that rewards best, second, and third roster answers per threat with 70/30 top-meta/full-meta weighting; this approximates coverage and answer depth without enumerating lineups, while full scoring remains authoritative.
  - Preferred fewer alternates and then lexical assignment fingerprints for equal prefilter/full scores, while cross-finalist ties use only the required variant-aware score, preserved default score, and canonical roster key.
  - Added a full-scoring/no-display-diagnostics configuration for prefiltered assignments so Threat Score and recommendation payloads are built only for the selected final assignment.
  - Moved final legality, anchor validation, and logging after reranking so they describe and guard the actual returned roster rather than the default-only GA leader.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, `test-runner`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Cache aggregate matchup reads by species, opponent, and variant during prefiltering; six species with three variants and two threat fixtures need 36 reads regardless of the 729 assignment combinations.
  - Re-scoring the selected assignment for final diagnostics should hit all 120 assignment-fingerprinted lineup cache entries rather than add misses.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-021.
  - Checked-in manifests remain deferred to US-024; until then production enumeration deterministically yields the single ranked-default transition assignment.
- Next iteration start:
  - US-022 - Propagate scored assignments through generation and team details APIs.
---

## 2026-08-06 19:35:02 MST - US-022
- Propagated the exact final scored roster assignment through GBL and PlayPokemon generation, the generate-team response, client result state, and team-details requests; team-details now validates the serialized assignment and projects its exact move IDs with regular, Elite, event-exclusive, and purified acquisition metadata without selecting another moveset.
- Files changed: `app/AGENTS.md`, `app/api/generate-team/route.ts`, `app/api/generate-team/route.test.ts`, `app/api/team-details/route.ts`, `app/api/team-details/route.test.ts`, `components/AGENTS.md`, `components/organisms/TeamManager/TeamManager.tsx`, `components/organisms/TeamManager/TeamManager.test.tsx`, `components/organisms/ResultsPanel/ResultsPanel.tsx`, `components/organisms/TeamDisplay/TeamDisplay.tsx`, `components/organisms/TeamDisplay/TeamDisplay.test.tsx`, `lib/AGENTS.md`, `lib/types.ts`, `lib/data/movesetVariantManifest.ts`, `lib/genetic/moveset.ts`, `lib/genetic/algorithm.ts`, `lib/genetic/algorithm.test.ts`, `progress.txt`
- Commit message: `feat: US-022 - Propagate scored assignments through generation and team details APIs`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/genetic/algorithm.test.ts app/api/generate-team/route.test.ts app/api/team-details/route.test.ts components/organisms/TeamDisplay/TeamDisplay.test.tsx components/organisms/TeamManager/TeamManager.test.tsx` before implementation (fail as expected: 7 assignment retention, API projection, validation, and client propagation regressions)
  - `npx vitest run lib/genetic/moveset.test.ts lib/genetic/algorithm.test.ts app/api/generate-team/route.test.ts app/api/team-details/route.test.ts components/organisms/TeamDisplay/TeamDisplay.test.tsx components/organisms/TeamManager/TeamManager.test.tsx` (pass, 81 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 773 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - Browser verification at `http://localhost:3001` (pass; generated Great League roster rendered, assignment-bearing `/api/team-details` requests returned 200, and response moves/acquisition metadata matched the assignment)
  - `npx vitest run lib/genetic/moveset.test.ts lib/genetic/algorithm.test.ts app/api/generate-team/route.test.ts app/api/team-details/route.test.ts components/organisms/TeamDisplay/TeamDisplay.test.tsx components/organisms/TeamManager/TeamManager.test.tsx` after review fixes (pass, 86 tests)
  - Final `npx tsc --noEmit`, `npm run lint`, `npm test`, and `npm run build` after review fixes (pass, 778 tests; existing multiple-lockfile workspace-root warning only)
- Implementation agents:
  - Recommended: `fullstack-developer`, `test-automator`
  - Used: `fullstack-developer`, `test-automator`; both fit the standard-mode complex-story budget and provided non-overlapping cross-layer propagation and regression-test guidance
- Specialist review:
  - Agents used: initial and targeted `code-reviewer`, `qa-expert`, and `security-engineer` passes. UI/UX specialists skipped under the one-domain-specialist high-risk budget because the rendered UI is unchanged and the client flow received direct browser verification
  - Findings: fixed five deduplicated initial medium findings by preserving diagnostic-enabled final GBL scoring, validating canonical move IDs/distinct charged moves/move slots, exercising the fingerprint boundary directly, using roster-complete generation fixtures, and bounding parsed assignment structures. Targeted re-review found one deduplicated medium request-size blocker because omitted or untrusted `Content-Length` can bypass the pre-parse byte check; one low client error-state hardening suggestion is deferred with the blocker.
- Decisions (why):
  - Reused the per-run lineup-aware fitness context to resolve one GBL assignment and score all six ordered GBL lineups against it, matching PlayPokemon's fixed-assignment output without introducing another scoring path.
  - Kept GBL evolution's cached threat-free scorer in the hot path, but used the same assignment-bound data context with direct diagnostic-enabled scoring for the final six permutations so Threat Score and downstream analyses remain populated.
  - Validated the client-round-tripped assignment against its format, exact canonical roster, canonical variant IDs, and deterministic fingerprint before projection because TypeScript response casts do not validate untrusted JSON.
  - Bounded the public request before JSON parsing when content length is available and again bounded parsed roster, variant, and string structures so a small generated roster cannot carry an unbounded assignment map.
  - Reclassified acquisition requirements from the exact assigned move IDs without changing move selection or scoring; excluded moves fail the team-details request rather than being displayed as buildable.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, `code-formatter`, `test-runner`, and `dev-browser` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - The final GBL branch previously created a new unbound lineup context and discarded assignment identity; final GBL recommendations should reuse the same per-run team-aware context with one resolved assignment.
  - Browser testing must confirm the worktree server port when another repository server is already running; port 3000 served an older checkout while this worktree correctly started on port 3001.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-022.
  - Commit blocked after the one allowed targeted re-review: replace `request.json()` with an actual byte-counted request-stream reader that rejects bodies above 16 KiB even when `Content-Length` is omitted or false, reject invalid declared lengths, add omitted-length and oversized-stream regressions, and start a fresh review loop next iteration.
  - After the blocking request reader is fixed, make `TeamDisplay` reject non-2xx or malformed team-details responses without assigning `undefined` to `pokemonData`.
  - Export consumption of assignment acquisition metadata remains intentionally scoped to US-023.
- Next iteration start:
  - Resume US-022 with a byte-counted team-details request reader and client error-state hardening, rerun all checks, and complete a fresh staged review loop before marking the story passed.
---

## 2026-08-06 19:58:13 MST - US-022
- Resumed the assignment propagation candidate and enforced the 16 KiB team-details boundary by validating declared lengths and counting actual request-stream bytes before JSON parsing; hardened `TeamDisplay` to reject non-2xx and malformed responses without corrupting team state.
- Files changed: `app/AGENTS.md`, `app/api/generate-team/route.ts`, `app/api/generate-team/route.test.ts`, `app/api/team-details/route.ts`, `app/api/team-details/route.test.ts`, `components/AGENTS.md`, `components/organisms/TeamManager/TeamManager.tsx`, `components/organisms/TeamManager/TeamManager.test.tsx`, `components/organisms/ResultsPanel/ResultsPanel.tsx`, `components/organisms/TeamDisplay/TeamDisplay.tsx`, `components/organisms/TeamDisplay/TeamDisplay.test.tsx`, `lib/AGENTS.md`, `lib/types.ts`, `lib/data/movesetVariantManifest.ts`, `lib/genetic/moveset.ts`, `lib/genetic/algorithm.ts`, `lib/genetic/algorithm.test.ts`, `progress.txt`
- Commit message: `feat: US-022 - Propagate scored assignments through generation and team details APIs`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run app/api/team-details/route.test.ts` before the resumed fix (fail as expected: 5 omitted/underreported/invalid content-length regressions)
  - `npx vitest run components/organisms/TeamDisplay/TeamDisplay.test.tsx` before the resumed fix (fail as expected: 2 non-2xx/malformed response regressions and one unhandled render error)
  - `npx vitest run app/api/team-details/route.test.ts` after the resumed fix (pass, 16 tests)
  - `npx vitest run components/organisms/TeamDisplay/TeamDisplay.test.tsx` after the resumed fix (pass, 8 tests)
  - `npx vitest run lib/genetic/moveset.test.ts lib/genetic/algorithm.test.ts app/api/generate-team/route.test.ts app/api/team-details/route.test.ts components/organisms/TeamDisplay/TeamDisplay.test.tsx components/organisms/TeamManager/TeamManager.test.tsx` before review fixes (pass, 93 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` before review fixes (pass, 785 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check && git diff --cached --check` (pass)
  - Browser verification at `http://localhost:3001` (pass; generated Great League roster rendered exact assigned moves and acquisition metadata, and generate-team/team-details requests returned 200; unrelated missing-site-icon 404 remained in the console)
  - `npx vitest run components/organisms/TeamDisplay/TeamDisplay.test.tsx` after the initial review test additions (fail as expected: 3 incomplete roster, mismatched moves, and invalid acquisition metadata regressions)
  - `npx vitest run components/organisms/TeamDisplay/TeamDisplay.test.tsx` after the review fix (pass, 11 tests)
  - `npx vitest run lib/genetic/moveset.test.ts lib/genetic/algorithm.test.ts app/api/generate-team/route.test.ts app/api/team-details/route.test.ts components/organisms/TeamDisplay/TeamDisplay.test.tsx components/organisms/TeamManager/TeamManager.test.tsx` after the review fix (pass, 96 tests)
  - Final `npx tsc --noEmit`, `npm run lint`, `npm test`, and `npm run build` after the review fix (pass, 788 tests; existing multiple-lockfile workspace-root warning only)
  - Final browser verification at `http://localhost:3001` (pass; strict response validation accepted the generated six-member Great League roster and exact assigned moves, with generate-team/team-details returning 200; unrelated missing-site-icon 404 remained in the console)
- Implementation agents:
  - Recommended: `fullstack-developer`, `test-automator`
  - Used: no new implementation agents in this resumed iteration; both recommended agents were used in the prior US-022 iteration, and the remaining review-prescribed regressions and fixes were exact enough that duplicate guidance would add no value
- Specialist review:
  - Agents used: initial `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `qa-expert` re-review
  - Findings: `code-reviewer` found none; fixed one medium QA finding by requiring complete ordered roster entries, exact assignment move/variant fields, and eligible acquisition categories before client state mutation; targeted QA re-review found none. The security specialist's signing recommendation was discarded as out of scope because the unauthenticated endpoint returns only public repository data to its caller, has no persistence or protected action, and the deterministic fingerprint is a consistency/cache identity rather than an authentication claim.
- Decisions (why):
  - Counted `Uint8Array.byteLength` before decoding or retaining each chunk so the request limit applies to actual transferred bytes and oversized bodies are canceled without full buffering.
  - Rejected malformed declared lengths with 400 and oversized declared or observed lengths with 413; underreported lengths remain safe because the stream boundary is authoritative.
  - Validated `TeamDisplay` responses before state mutation and retained the prior empty array on failure, keeping the existing non-visual error behavior without adding UI outside the story.
  - Compared every successful response entry with the expected team order and exact assignment variant before rendering or export so malformed public responses cannot desynchronize display from scored moves.
  - Did not add HMAC or server-side assignment sessions because the route has no authorization boundary or protected state, and doing so would introduce secret/configuration and distributed-session requirements outside this public projection story.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, `code-formatter`, `test-runner`, and `dev-browser` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Fetch `Content-Length` is optional and caller-controlled; request size limits require a streaming byte counter even when a fast header rejection exists.
  - Client response type casts do not validate JSON; check HTTP success and the minimum response structure before mutating React state.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-022.
  - Export consumption of assignment acquisition metadata remains intentionally scoped to US-023.
- Next iteration start:
  - US-023 - Export assigned movesets and acquisition requirements.
---

## 2026-08-06 20:31:29 MST - US-023
- Made the scored `RosterMovesetAssignment` authoritative for export move IDs and preferred charged-move order; passed validated acquisition metadata separately through `TeamDisplay` and `ExportButton`; appended deterministic Elite Fast TM, Elite Charged TM, event-exclusive, and purified requirement comments while leaving regular-only exports unchanged.
- Files changed: `lib/AGENTS.md`, `lib/export/types.ts`, `lib/export/exportTeam.ts`, `lib/export/exportTeam.test.ts`, `lib/export/copyTeamToClipboard.ts`, `lib/export/copyTeamToClipboard.test.ts`, `lib/export/index.ts`, `components/molecules/ExportButton/ExportButton.tsx`, `components/organisms/TeamDisplay/TeamDisplay.tsx`, `components/organisms/TeamDisplay/TeamDisplay.test.tsx`, `prd.json`, `progress.txt`
- Commit message: `feat: US-023 - Export assigned movesets and acquisition requirements`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/export/exportTeam.test.ts lib/export/copyTeamToClipboard.test.ts components/organisms/TeamDisplay/TeamDisplay.test.tsx` before implementation (fail as expected: 7 tests exposed blank reconstructed moves, absent requirements, missing invariant errors, and old `ExportButton` props)
  - `npx vitest run lib/export/exportTeam.test.ts lib/export/copyTeamToClipboard.test.ts components/organisms/TeamDisplay/TeamDisplay.test.tsx` after implementation (pass, 22 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (fail before correcting four import-order errors; pass afterward)
  - `npm test` (pass, 790 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - Browser verification at `http://localhost:3001` (pass; generated a six-member Great League roster, generate-team and team-details requests returned 200, and the existing export button produced the `Team Copied` toast; unrelated missing-site-icon 404 remained in the console)
  - `npx vitest run components/organisms/TeamDisplay/TeamDisplay.test.tsx` before review fix (fail as expected: 5 invalid-response cases left export enabled without acquisition metadata)
  - `npx vitest run components/organisms/TeamDisplay/TeamDisplay.test.tsx lib/export/exportTeam.test.ts lib/export/copyTeamToClipboard.test.ts` after review fix (pass, 22 tests)
  - `npx tsc --noEmit`, `npm run lint`, and `git diff --check` after review fix (pass)
- Implementation agents:
  - Recommended: `fullstack-developer`, `test-automator`
  - Used: `fullstack-developer`; skipped `test-automator` because the standard-mode one-agent budget and direct pure-export, clipboard, and adapter regressions made overlapping test guidance unnecessary
- Specialist review:
  - Agents used: initial `code-reviewer`, `qa-expert`, and `ui-designer`; targeted `ui-designer` re-review
  - Findings: code review found none; fixed one medium UI finding by disabling export when validated acquisition metadata is unavailable; targeted UI re-review found none. One low QA suggestion for a dedicated `ExportButton` integration test is deferred because exact formatting, clipboard delegation/errors, TeamDisplay prop wiring, and the live click/toast flow are covered at their current boundaries.
- Decisions (why):
  - Kept acquisition requirements outside `RosterMovesetAssignment` because they are informational output metadata and must not alter assignment fingerprints, cache identity, or battle scoring.
  - Preserved the first roster lines as four-column PvPoke import rows and appended special requirements as `#` comments so regular exports remain byte-identical and acquisition text does not alter move fields.
  - Failed closed when a non-empty roster lacks an assigned variant or acquisition metadata instead of restoring nullable moves or silently omitting build requirements.
  - Disabled the export interaction after team-details validation failures so users cannot trigger a known incomplete export and receive only a generic clipboard failure toast.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, `code-formatter`, `test-runner`, and `dev-browser` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Canonical variant IDs normalize charged-move identity, so output adapters must use the assignment's explicit charged slots to preserve preferred simulation and display order.
  - The validated team-details response is the acquisition-metadata source, but export move selection remains owned exclusively by the original scored assignment.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-023.
- Next iteration start:
  - US-024 - Regenerate and validate variant data for all supported formats.
---

## 2026-08-06 22:33:35 MST - US-024
- Regenerated deterministic ranking and simulation data from the pinned PvPoke submodule; published valid authoritative manifests for all eight supported formats; generated every bounded candidate scenario; validated active scenario parity and move availability; retained evidence-backed variants for all nine representative species where supported; excluded synthesized Furret and Florges shadows; and restored sub-minute finalist reranking with per-context data lookup memoization.
- Files changed: `lib/AGENTS.md`, `lib/data/simulations.test.ts`, `lib/genetic/fitness/greatLeagueCalibrationFixtures.test.ts`, `lib/genetic/fitness/lineupScoring.ts`, `lib/genetic/fitness/lineupScoring.test.ts`, 16 `data/rankings/**/{chargers,consistency}_rankings.csv` files, `data/sync-metadata.json`, 8 `data/simulations/**/moveset-variants.json` manifests, 16,578 new alternate simulation CSVs, 906 normalized existing simulation CSVs, `progress.txt`
- Commit message: `feat: US-024 - Regenerate and validate variant data for all supported formats`
- Auto approval: `enabled`
- Checks:
  - `env -u PVPOKE_PATH npm run sync -- --project-simulations` (pass; 8 formats, 1,186 candidate species, 6,719 candidates, 16,599 alternate scenario CSVs, 0 stale files, no worktree changes)
  - `env -u PVPOKE_PATH npm run sync` (pass; 10,887,888 simulation entries validated and 8 manifests atomically published)
  - manifest/runtime integrity audit (pass; 2,260 active candidates, 6,780 active scenario files, exact default opponent parity, no excluded moves or synthesized Furret/Florges shadows, 439 Elite defaults, and 23 event-exclusive candidate moves)
  - post-generation projection (pass; identical totals and 0 stale files)
  - second non-resume `env -u PVPOKE_PATH npm run sync` with before/after ranking CSV, simulation CSV, and manifest SHA-256 digest comparison (pass; byte-identical)
  - `npx vitest run lib/data/simulations.test.ts lib/data/movesetVariantSimulations.test.ts lib/data/movesetVariantManifest.test.ts` before transition test update (fail as expected: checked-in manifest replaced the prior manifest-missing state)
  - `npx vitest run lib/data/simulations.test.ts lib/data/movesetVariantSimulations.test.ts lib/data/movesetVariantManifest.test.ts` after transition test update (pass, 56 tests)
  - first `npm test` after manifest publication (fail: two performance safeguards exceeded one minute and calibration fixtures included Skarmory outside the sanitized top-150 manifest target set)
  - `npx vitest run lib/genetic/fitness/greatLeagueCalibrationFixtures.test.ts lib/genetic/algorithm.performance.test.ts` after fixes (pass; representative generation about 3.1 seconds and two deterministic seeded runs about 7.3 seconds)
  - focused sync/data suite (pass, 171 tests)
  - focused scoring/data/performance suite (pass, 121 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 790 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/genetic/fitness/lineupScoring.test.ts -t "memoizes every|keeps production lookup caches"` before review fix (fail as expected: injected data boundaries were not yet accepted or used)
  - `npx vitest run lib/data/simulations.test.ts -t "validates every checked-in manifest"` before test optimization (timeout: per-row matcher overhead made the first integrity-test implementation impractical)
  - `npx vitest run lib/genetic/fitness/lineupScoring.test.ts -t "memoizes every|keeps production lookup caches"` after review fix (pass, 2 tests)
  - `npx vitest run lib/data/simulations.test.ts -t "validates every checked-in manifest"` after test optimization (pass, 1 test in about 2.2 seconds)
  - `npx vitest run lib/data/simulations.test.ts lib/data/movesetVariantSimulations.test.ts lib/data/movesetVariantManifest.test.ts lib/genetic/fitness/lineupScoring.test.ts lib/genetic/fitness/greatLeagueCalibrationFixtures.test.ts lib/genetic/algorithm.performance.test.ts` after review fixes (pass, 99 tests)
  - `npx tsc --noEmit` after review fixes (pass)
  - `npm run lint` after review fixes (pass)
  - `npm test` after review fixes (pass, 793 tests)
  - `npm run build` after review fixes (pass; existing multiple-lockfile workspace-root warning only)
- Implementation agents:
  - Recommended: `data-engineer`, `test-automator`
  - Used: `data-engineer`, `test-automator`; both fit the standard-mode complex-story budget and provided non-overlapping generation, integrity-audit, representative-coverage, and idempotence guidance
- Specialist review:
  - Agents used: initial `code-reviewer`, `qa-expert`, and `data-engineer`; targeted `qa-expert` re-review. Optional security, documentation, compliance, and UI specialists skipped because the change is local generated repository data plus internal scoring-cache behavior with no relevant external surface.
  - Findings: initial code and data reviews found none; fixed two medium QA findings by adding catalog-driven integrity coverage for all checked-in manifests and active scenarios and deterministic per-context memoization key/isolation regressions; targeted re-review found none.
- Decisions (why):
  - Used the pinned `vendor/pvpoke` source with `PVPOKE_PATH` unset and a non-resume initial run so every candidate was regenerated through the tested atomic publication and stale-cleanup path rather than reusing unverified files.
  - Included deterministic line-ending normalization in ranking and existing simulation CSVs because omitting sync side effects would make the committed state differ on the next unchanged full sync.
  - Kept broad historical calibration fixtures on explicit ranked-default scoring because they intentionally include species outside the current sanitized top-150 simulation target set; production team-aware manifest selection continues to fail closed for absent species.
  - Memoized immutable per-context ranking and variant-qualified simulation reads instead of reducing finalist or assignment scoring bounds, preserving all existing variant-aware scoring and assignment cache isolation while reducing representative generation from more than 80 seconds to about 3 seconds.
  - Injected only the five memoized data-loading boundaries into the production context factory so tests can prove cache behavior without module patching while existing callers retain unchanged defaults.
  - Used direct deterministic CSV validation inside the catalog-driven integrity test because millions of per-cell matcher calls obscured the same invariant behind test-framework overhead.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` and `test-driven-development` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Publishing manifests activates real finalist assignment enumeration and makes repeated low-level ranking and simulation reads a hot path; cache those immutable reads inside the production scoring context by every result-affecting input.
  - Generated manifests cover sanitized top-150 simulation targets, not every species retained in broader historical calibration fixtures or category rankings.
  - Full sync normalizes checked-in ranking and simulation CSV line endings; include that deterministic output if the committed state must remain clean after the next unchanged sync.
  - Generated-data regressions should traverse `getBattleFormats()` and manifest storage keys instead of hardcoding one species or directory, so newly supported formats and active variants enter the same validation automatically.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-024.
- Next iteration start:
  - US-025 - Document moveset variant generation and runtime semantics.
---

## 2026-08-07 02:19:50 MST - US-025
- Documented move acquisition policy, exact legacy exceptions, ranking evidence aggregation, mechanics filtering, bounded candidate derivation, invalid-default replacement, manifest authority and fallback, rollback-scoped publication, guarded stale cleanup, fixed roster assignments, bounded finalist reranking, and maintainer projection/generation/validation commands in the existing optimizer guides.
- Files changed: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `prd.json`, `progress.txt`
- Commit message: `docs(optimizer): US-025 - Document moveset variant generation and runtime semantics`
- Auto approval: `enabled`
- Checks:
  - `npx prettier --check docs/pokemon-go-team-optimization.md docs/team-optimization/data-inputs.md` (pass)
  - `npx vitest run lib/data/simulations.test.ts` (pass, 9 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 793 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
- Implementation agents:
  - Recommended: `documentation-engineer`
  - Used: `documentation-engineer` to audit acceptance coverage and implementation terminology before editing
- Specialist review:
  - Agents used: `documentation-engineer` and `qa-expert`; targeted re-review by both agents
  - Findings: corrected incomplete-default rejection claims, removed unsupported fast-move effect filtering, clarified fallback assignments and capped exact-candidate priority, narrowed rollback semantics to the simulation/manifest publication batch, documented earlier full-sync writes and sync metadata, and completed deterministic assignment tie-breaking; final targeted reviews found no remaining issues.
- Decisions (why):
  - Expanded only the existing main optimizer and data-input guides so detailed repository policy has one discoverable source without duplicating it across unrelated scoring documents.
  - Distinguished move eligibility from informational acquisition metadata because Elite, event-exclusive, and purified requirements must not alter battle fitness.
  - Documented candidate, active-variant, assignment, and full-score caps separately so maintainers do not conflate derivation breadth with runtime availability or reranking work bounds.
  - Described simulation and manifest replacement as a rollback-capable batch rather than claiming the entire sync is transactional, because gamemaster and ranking writes occur earlier and remain outside rollback.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` skill plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Documentation for generated data must distinguish full-sync side effects from the narrower rollback boundary used to publish simulations and manifests.
  - Exact observed and override movesets receive evidence priority, but the hard eight-candidate cap can still truncate lower-priority exact sets.
- Known issues / follow-ups:
  - The unrelated existing `.opencode/package-lock.json` modification remains unstaged and is not part of US-025.
- Next iteration start:
  - All PRD stories pass.
---

## 2026-08-07 05:07:52 MST - US-026
- Implemented a non-interactive analyzer for the generated `/api/generate-team` Next.js NFT trace with exact trace-relative path resolution, canonical deduplication, filesystem byte measurement, fixed simulation/ranking/application/dependency/other-data groups, deterministic largest-file ordering, stable JSON output, and actionable missing or malformed trace failures; added checked-in fixtures and package-script wiring.
- Files changed: `lib/AGENTS.md`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/**`, `lib/scripts/analyzeGenerateTeamTrace.ts`, `package.json`, `progress.txt`
- Commit message: `feat: US-026 - Measure generated function trace size`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/build/functionTraceAnalyzer.test.ts` (fail as expected before implementation: analyzer module missing)
  - `npx vitest run lib/build/functionTraceAnalyzer.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts` (pass, 9 tests before review; 11 tests after review fixes)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (fail before fixture extension and import-order fixes; pass afterward)
  - `npx vitest run lib/build/functionTraceAnalyzer.test.ts` after initial review regressions (fail as expected: missing traced-asset errors exposed an absolute host path)
  - `npm test` (pass, 798 tests before review; 800 tests after review fixes)
  - `npm run build` (pass before and after review fixes; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; 20,290 unique files and 357,096,998 uncompressed bytes, including 350,057,183 simulation bytes)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `build-engineer`, `test-automator`
  - Used: `build-engineer`; skipped `test-automator` because one focused build advisor plus test-first analyzer, determinism, CLI, malformed-input, and runtime-boundary coverage fit the standard-mode implementation budget
- Specialist review:
  - Agents used: initial `code-reviewer` and `qa-expert`; targeted `qa-expert` re-review
  - Findings: code review found none; fixed two medium QA findings by sanitizing filesystem errors to stable codes and deriving fixture expectations from checked-in bytes instead of line-ending-sensitive literals; also added the low suggested structural malformed-trace CLI regression; targeted re-review found none
- Decisions (why):
  - Resolved every NFT entry against the trace directory because Next.js emits relative paths from the `.nft.json` location; resolving against the repository root would measure the wrong files.
  - Reported stable machine-readable JSON with raw integer bytes and fixed category order so repeated builds can be compared without locale, color, timing, or human-formatting noise.
  - Classified exact `data/simulations`, `data/rankings`, and `node_modules` path segments before broader data/application groups so similarly named directories cannot be misclassified.
  - Measured the generated trace without imposing a size threshold because US-026 establishes visibility; later US-029 and US-032 own the 200 MiB and 100 MiB deployment limits.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` and `test-driven-development` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - The current generated function trace is `.next/server/app/api/generate-team/route.js.nft.json`, and its entries may traverse to the workspace-level `node_modules` directory.
  - The current trace baseline is dominated by simulation files, confirming later active-index and compact-snapshot stories should change tracing authority rather than optimizing application code first.
- Known issues / follow-ups:
  - The current generated trace is about 340.6 MiB and intentionally exceeds later transition targets; US-027 through US-032 own active asset indexing, trace narrowing, compact snapshots, and final size enforcement.
- Next iteration start:
  - US-027 - Publish an active runtime simulation asset index
---

## 2026-08-07 05:31:27 MST - US-027
- Implemented a versioned canonical runtime simulation asset index derived from reparsed manifests for all eight supported formats; included every format manifest and the exact three scenario paths for active defaults and alternatives only; published the index last in the existing rollback-capable transaction; retained inactive candidate evidence and stale-cleanup semantics; and hardened rollback state tracking when a backup rename fails.
- Files changed: `lib/AGENTS.md`, `lib/data/runtimeSimulationAssetIndex.ts`, `lib/data/runtimeSimulationAssetIndex.test.ts`, `lib/sync/runtimeSimulationAssetIndex.ts`, `lib/sync/runtimeSimulationAssetIndex.test.ts`, `lib/sync/index.ts`, `lib/sync/index.test.ts`, `lib/sync/movesetVariantManifest.ts`, `lib/sync/movesetVariantManifest.test.ts`, `data/simulations/runtime-asset-index.json`, `progress.txt`
- Commit message: `feat: US-027 - Publish an active runtime simulation asset index`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/data/runtimeSimulationAssetIndex.test.ts lib/sync/runtimeSimulationAssetIndex.test.ts lib/sync/index.test.ts lib/sync/movesetVariantManifest.test.ts` (fail as expected before implementation: index contract, derivation, orchestration, and publication behavior were missing)
  - `npx vitest run lib/sync/runtimeSimulationAssetIndex.test.ts` before generated artifact publication (fail as expected: checked-in runtime index was absent)
  - `npx vitest run lib/sync/movesetVariantManifest.test.ts -t "does not restore from a backup"` before rollback fix (fail as expected: rollback attempted to restore a backup whose rename had failed)
  - Focused runtime-index, publication, cleanup, and PvPoke-boundary suite before review (pass, 47 tests; pass after review fixes, 48 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 815 tests before review; pass after review fixes, 816 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - Reverse-order checked-manifest index regeneration comparison (pass; byte-identical with 6,788 unique assets)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `data-engineer`, `security-engineer`
  - Used: `data-engineer`, `security-engineer`; both fit the standard-mode complex-story budget and provided complementary manifest derivation, deterministic serialization, path-confinement, atomic-ordering, and rollback guidance
- Specialist review:
  - Agents used: initial `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `qa-expert` and `security-engineer` re-review
  - Findings: code review found none; fixed two medium QA findings by asserting all scenarios for active/default/inactive candidates and exercising multi-manifest plus CSV ordering/rollback; also added the low schema, filename, malformed/content-mismatch, and nested-order regressions and fixed the low security finding by restoring CSVs and manifests sequentially before the prior authority index; targeted re-review found none
- Decisions (why):
  - Used one flat sorted `assets` allowlist because later tracing needs exact repository paths rather than another runtime availability model; manifests remain the richer per-format authority.
  - Reparses every prepared manifest and requires exactly one catalog-matching target per format before deriving paths, preventing source ordering, stale directories, or unsupported formats from influencing the index.
  - Installed the index after CSVs and manifests inside the same transaction so readers see either the prior complete authority set or the new one, while a late index failure rolls manifests and CSVs back.
  - Left stale cleanup based on all manifest candidates rather than active-only index paths so inactive simulation CSVs remain available as sync and validation evidence.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` and `test-driven-development` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - The active checked-in manifest set currently authorizes 6,788 runtime assets: eight manifests plus 6,780 active candidate scenario CSVs.
  - Publication rollback must distinguish an existing target from a successfully created backup; setting rollback state before the backup rename can turn the original failure into an incomplete-rollback error.
- Known issues / follow-ups:
  - US-029 should consume the generated flat allowlist for exact Next.js output tracing; US-027 intentionally does not change `next.config.ts`.
- Next iteration start:
  - US-028 - Remove runtime simulation directory scanning
---

## 2026-08-07 05:53:01 MST - US-028
- Replaced runtime simulation directory enumeration and filename discovery with one manifest-backed default matrix loader that reads each species' active default candidate through its exact declared scenario storage keys; retained aggregate and shield-specific lookup behavior, explicit variant authority, typed failures, format-scoped caching, and default/alternate opponent parity validation across every supported format.
- Files changed: `lib/AGENTS.md`, `lib/data/movesetVariantSimulations.ts`, `lib/data/movesetVariantSimulations.test.ts`, `lib/data/simulations.ts`, `lib/data/simulations.test.ts`, `prd.json`, `progress.txt`
- Commit message: `feat: US-028 - Remove runtime simulation directory scanning`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/data/movesetVariantSimulations.test.ts` (fail as expected before implementation: `getDefaultMatchupMatrix` was missing; 13 pass, 1 fail)
  - `npx vitest run lib/data/movesetVariantSimulations.test.ts lib/data/simulations.test.ts` (pass, 22 tests)
  - `npx vitest run lib/data/movesetVariantSimulations.test.ts lib/data/simulations.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts` (pass, 26 tests)
  - `npx prettier --write "lib/AGENTS.md" "lib/data/movesetVariantSimulations.ts" "lib/data/movesetVariantSimulations.test.ts" "lib/data/simulations.ts" "lib/data/simulations.test.ts"` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 816 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/data/movesetVariantSimulations.test.ts lib/data/simulations.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts` after review fix (pass, 26 tests)
  - `npx tsc --noEmit` after review fix (pass)
  - `npm run lint` after review fix (pass)
  - `npm test` after review fix (pass, 816 tests)
  - `git diff --check && git diff --cached --check` after review fix (pass)
- Implementation agents:
  - Recommended: `backend-developer`, `architect-reviewer`
  - Used: `backend-developer`; skipped `architect-reviewer` because one runtime data-loading specialist plus the established manifest boundary was sufficient for the standard-mode complex-story budget
- Specialist review:
  - Agents used: initial `code-reviewer` and `qa-expert`; targeted `qa-expert` re-review
  - Findings: code review found none; fixed one medium QA finding by validating implicit/explicit default aggregate parity and all three shield scenarios for every loaded opponent in every supported format; targeted re-review found none
- Decisions (why):
  - Extended the existing manifest-backed simulation loader instead of adding a second runtime index consumer because per-format manifests remain the runtime availability authority and the flat asset index belongs to later output-tracing work.
  - Built and cached the complete default matrix only after every species default loaded and validated, preventing a late malformed CSV from publishing a partial format cache.
  - Loaded only default candidate storage for default matrices; active alternatives remain lazy and are read only when availability or explicit-variant lookups require them.
  - Removed the exported runtime filename parser with the scanning implementation because no consumer remained and filename discovery is no longer an authorized runtime behavior.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - `createMovesetVariantSimulationLoader(...)` now owns both default matrices and explicit variant reads, allowing the public simulation helpers to preserve synchronous interfaces without separate CSV parsing or filesystem enumeration.
  - Calling `getActiveMovesetVariants(...)` validates every active alternate against its default opponent sets, while default matrix preparation reads only manifest-declared default storage.
- Known issues / follow-ups:
  - US-029 should use `data/simulations/runtime-asset-index.json` as the exact Next.js output-tracing allowlist; US-028 intentionally leaves tracing configuration unchanged.
- Next iteration start:
  - US-029 - Trace only runtime-authorized function assets
---

## 2026-08-07 06:16:02 MST - US-029
- Replaced recursive Next.js data tracing with a validated exact asset plan derived from the active runtime simulation index and battle-format catalog; narrowed pokemon-list to Pokemon data plus Overall rankings; added exact supporting-ranking exclusions for NFT automatic discovery; and upgraded the trace analyzer to enforce active-only assets, route data isolation, and the 200 MiB transition budget.
- Files changed: `next.config.ts`, `lib/AGENTS.md`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/pokemon-list/route.js.nft.json`, `lib/scripts/analyzeGenerateTeamTrace.ts`, `progress.txt`
- Commit message: `feat: US-029 - Trace only runtime-authorized function assets`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/build/runtimeFunctionAssets.test.ts` (fail as expected before implementation: runtime asset-plan module missing; later fail as expected before exact pokemon-list exclusions were added)
  - `npx vitest run lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/data/runtimeSimulationAssetIndex.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts` (pass, 23 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (initial fail: import order; pass after fix)
  - `npm run build` (initial fail: Next config helper project-alias resolution; pass after relative-import fix, existing multiple-lockfile warning only)
  - `npm run analyze:generate-team-trace` (initial fail: pokemon-list NFT automatically retained 48 supporting ranking files; pass after exact exclusions with 6,901 files and 126,150,225 uncompressed bytes)
  - `npx vitest run lib/build/functionTraceAnalyzer.test.ts lib/build/runtimeFunctionAssets.test.ts` after review regressions (fail as expected: validation mode accepted custom traces and lacked injectable route-pair coverage; pass after fixes, 14 tests)
  - `npm test` (pass before review, 820 tests; pass after review fixes, 823 tests)
  - `npx prettier --check "next.config.ts" "lib/build/runtimeFunctionAssets.ts" "lib/build/runtimeFunctionAssets.test.ts" "lib/build/functionTraceAnalyzer.ts" "lib/scripts/analyzeGenerateTeamTrace.ts"` (pass)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `build-engineer`, `deployment-engineer`
  - Used: `build-engineer`; skipped `deployment-engineer` because the established local Next.js trace and analyzer boundaries made a second overlapping implementation advisor unnecessary
- Specialist review:
  - Agents used: initial `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `qa-expert` and `security-engineer` re-review
  - Findings: code review found none; fixed medium QA and security findings by verifying every checked-in catalog asset, exercising both route traces through the CLI, and rejecting custom trace overrides in validation mode; also fixed the low CLI wiring coverage finding; targeted re-reviews found none
- Decisions (why):
  - Used the generated active index directly for simulation includes and the battle-format catalog for exact ranking paths so deployment authority cannot drift into a second hardcoded format list.
  - Added exact pokemon-list supporting-category exclusions because Next includes are additive and broad exclusions are applied after includes, which would also remove required Overall rankings.
  - Enforced 200 MiB as an analyzer failure threshold, preserving at least 20 percent headroom below the standard 250 MiB function limit without enabling Large Functions.
  - Kept full manifests and inactive CSV evidence unchanged; only generated route traces were narrowed.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Next.js output tracing first merges automatic NFT files and manual includes, then applies route excludes; exact exclusions are required when static analysis over-traces dynamic filesystem paths.
  - The active-only generate-team trace currently includes 6,788 simulation assets and totals about 120.3 MiB, down from the prior approximately 340.6 MiB recursive baseline.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
  - US-030 will compile active matchup data into compact per-format runtime snapshots; US-032 owns the final 100 MiB function and 50 MiB snapshot targets.
- Next iteration start:
  - US-030 - Generate compact per-format runtime simulation snapshots
---

## 2026-08-07 06:50:44 MST - US-030
- Implemented a versioned per-format compact simulation snapshot with canonical species, opponent, move, and variant dictionaries; active default mappings; manifest and source digest identity; deterministic little-endian `uint16` Base64 Battle Ratings; explicit `0xffff` missing values; active-only manifest storage reads; prepared-CSV overlays; full source-row parity; and rollback-capable publication after manifests. Generated all eight checked-in snapshots from the authoritative active CSV set.
- Files changed: `lib/AGENTS.md`, `lib/data/runtimeSimulationSnapshot.ts`, `lib/data/runtimeSimulationSnapshot.test.ts`, `lib/sync/runtimeSimulationSnapshots.ts`, `lib/sync/runtimeSimulationSnapshots.test.ts`, `lib/sync/index.ts`, `lib/sync/index.test.ts`, `lib/sync/movesetVariantManifest.ts`, `lib/sync/movesetVariantManifest.test.ts`, `docs/team-optimization/data-inputs.md`, `data/simulations/cp{cp}/{cup}/runtime-snapshot.json` for all eight supported formats, `prd.json`, `progress.txt`
- Commit message: `feat: US-030 - Generate compact per-format runtime simulation snapshots`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/data/runtimeSimulationSnapshot.test.ts lib/sync/runtimeSimulationSnapshots.test.ts` (fail as expected before implementation: snapshot contract and generator modules were absent)
  - `npx vitest run lib/sync/runtimeSimulationSnapshots.test.ts -t "keeps every checked-in format snapshot"` (fail as expected before generated publication: all active CSVs prepared successfully and snapshot files were absent)
  - `bun -e "...prepareRuntimeSimulationSnapshots...publishSimulationGeneration..."` (pass; atomically generated 8 snapshots totaling 9,588,374 bytes)
  - focused snapshot, orchestration, publication, and PvPoke boundary suite (pass, 36 tests before final edge-case additions)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (initial fail: import order and one unused type; pass after fixes)
  - `npm test` (pass, 831 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - targeted `npx prettier --check ... && git diff --check` (pass)
  - final `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build`, and format/diff checks after review fixes (pass, 834 tests; existing multiple-lockfile workspace-root warning only)
- Implementation agents:
  - Recommended: `data-engineer`, `performance-engineer`
  - Used: `data-engineer`, `performance-engineer`; both fit the standard-mode complex-story budget and provided complementary schema, source-authority, fixed-width encoding, sentinel, digest, publication, size, and parity guidance
- Specialist review:
  - Agents used: initial `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `qa-expert` and `security-engineer` re-review. `documentation-engineer` skipped because the standard-mode high-risk budget allows one domain specialist and security review is more material for strict parsing and rollback behavior
  - Findings: initial code review found none; fixed four deduplicated medium findings by binding `activeRatingsDigest` to canonical dictionaries/variants/rating bytes, bounding JSON/dictionaries/variants/decoded Base64 before allocation, requiring one manifest-matched snapshot with exact policy/source/active moveset/default identity during publication, and adding generated missing-sentinel plus mixed prepared/resume overlay coverage; targeted QA and security re-review found none. One low malformed-input coverage cluster is deferred because the validator already covers each structural layer and the full catalog parity test validates every checked-in payload
- Decisions (why):
  - Used minified JSON with padded Base64 `uint16` ratings because it preserves exact integer Battle Ratings while reducing the current active CSV payload to about 9.1 MiB and remains simple to validate and hydrate synchronously in US-031.
  - Chose variant/opponent/scenario layout so one variant-opponent lookup stores `0-0`, `1-1`, and `2-2` contiguously; reserved `0xffff` for missing rows and rejected `1001..65534` so missing alternates can never silently become default results.
  - Kept the existing runtime asset index unchanged because runtime still consumes active CSVs in US-030; US-031 owns snapshot-backed loading and US-032 owns removing CSVs from function traces.
  - Derived opponent aliases from the current sync Pokemon snapshot instead of module-cached checked-in data so a same-process gamemaster refresh cannot generate stale identities.
  - Treated manifest `evaluationCounts` as minimum selection-completeness evidence rather than exact CSV lengths after checked-in data showed 100-count selection pools alongside 1,139-row full matrices; snapshots retain every validated active source row.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Snapshot preparation must overlay deferred CSV bytes before filesystem reads so new data wins over stale reused files during resume publication.
  - Candidate order in valid manifest JSON is not an authority boundary; locate the active default explicitly before checking alternate opponent parity.
  - Current snapshots total 9,588,374 bytes across eight formats and preserve every active CSV Battle Rating through a catalog-wide byte-synchronization regression.
- Known issues / follow-ups:
  - US-031 must prepare and validate snapshots before scoring, preserve synchronous lookup APIs, and fail closed without CSV fallback.
  - US-032 must switch tracing to snapshots, remove simulation CSVs from runtime function traces, and enforce the final size/performance/memory budgets.
- Next iteration start:
  - US-031 - Load matchups from compact runtime snapshots
---

## 2026-08-07 07:28:46 MST - US-031
- Implemented a format-scoped compact snapshot repository with explicit transactional preparation, validated schema/format/policy/source digest/dictionary/variant/default/per-species order/rating/sentinel identity before caching, synchronous typed-array aggregate and shield lookups, default-first active movesets, source-order virtual default matrices, bounded no-follow regular-file reads, and fail-closed behavior with no runtime manifest or CSV fallback; moved preparation before optimizer context construction and shared the authoritative policy version across sync/runtime.
- Files changed: `lib/AGENTS.md`, `lib/data/movesetVariantManifest.ts`, `lib/data/movesetVariantSimulations.ts`, `lib/data/runtimeSimulationRepository.ts`, `lib/data/runtimeSimulationRepository.test.ts`, `lib/data/runtimeSimulationSnapshot.ts`, `lib/data/runtimeSimulationSnapshot.test.ts`, `lib/data/runtimeSimulationSnapshotFile.ts`, `lib/data/runtimeSimulationSnapshotFile.test.ts`, `lib/data/simulations.ts`, `lib/data/simulations.test.ts`, `lib/genetic/algorithm.ts`, `lib/genetic/algorithm.test.ts`, `lib/genetic/fitness/lineupScoring.ts`, `lib/genetic/moveset.ts`, `lib/genetic/moveset.test.ts`, `lib/sync/movesetVariantManifest.ts`, `lib/sync/movesetVariantManifest.test.ts`, `lib/sync/runtimeSimulationSnapshots.ts`, `lib/sync/runtimeSimulationSnapshots.test.ts`, `data/simulations/cp{cp}/{cup}/runtime-snapshot.json` for all eight supported formats, `prd.json`, `progress.txt`
- Commit message: `feat(simulations): load US-031 matchups from compact snapshots`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/data/runtimeSimulationRepository.test.ts lib/genetic/algorithm.test.ts -t "prepares one format once|preserves missing alternate|fails closed on missing|loads top-ranked names"` (fail as expected before implementation: repository module missing and preparation ran after context construction)
  - focused snapshot, simulation, moveset, algorithm, lineup-scoring, calibration, and PvPoke boundary suites (pass, including catalog-wide active snapshot/CSV parity)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (initial fail: four import-order/unused-type errors; pass after fixes)
  - `npm test` (initial fail: five direct scoring-context tests lacked explicit preparation; pass after preparing in production context construction, 837 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - targeted `npx prettier --check ... && git diff --check` (pass)
  - review regression tests for digest-bound schema/encoding/default/source identity, bounded stable reads, symlink refusal, per-species source opponent iteration, independent format preparation, complete active moveset parity, and opponent-keyed aggregate parity (fail as expected before review fixes; pass after fixes)
  - rollback-capable snapshot publication command (pass; regenerated all eight schema-v2 snapshots)
  - repeated final snapshot publication produced identical data diff SHA-256 `f947a0d7123ed192ff1eb4cb69973d0e1cc8fb32f7fccbd519bbfb9536c910d4` (pass)
  - final `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build`, and format/diff checks after review fixes (pass, 843 tests; existing multiple-lockfile workspace-root warning only)
- Implementation agents:
  - Recommended: `backend-developer`, `performance-engineer`
  - Used: `backend-developer`, `performance-engineer`; both fit the standard-mode complex-story budget and provided complementary repository/error-contract, hydration, cache, missing-sentinel, no-fallback, hot-path, and parity guidance
- Specialist review:
  - Agents used: initial and targeted `code-reviewer`, `qa-expert`, and `security-engineer` review because the runtime data-path replacement affected correctness, parity, deterministic behavior, and filesystem-backed snapshot parsing
  - Findings: fixed all deduplicated findings by preserving each species default CSV opponent order instead of one global alternate-derived order; binding schema, encoding, shape, format, manifest/source identity, defaults, per-species order, dictionaries, variants, and ratings into the active digest; reading at most the opened file's validated size plus one byte with no-follow flags; adding isolated cross-format cache/failure coverage; comparing complete active movesets; keying aggregate parity by opponent; preparing every test format explicitly; and clarifying snapshot versus legacy CSV authority
- Decisions (why):
  - Retained one decoded `Uint16Array` plus canonical indexes per prepared format and exposed virtual default matchup maps so warm lookups remain synchronous without rebuilding the old CSV object graph.
  - Re-exported the shared typed data error from the legacy CSV reference loader so existing test/tooling consumers retain `instanceof` behavior while production imports no CSV parser or loader.
  - Preserved default-first active variant order before lexical alternates because assignment enumeration, fingerprints, diagnostics, API output, and export must not drift from manifest-era behavior.
  - Added one explicit opponent iteration permutation per species default instead of changing canonical sorted dictionaries, preserving previous tie and floating-point accumulation semantics even when valid source CSV row order differs by species or alternate.
  - Bumped the compact snapshot schema to v2 and regenerated every checked-in snapshot because defaults, manifest/source identity, and opponent order affect runtime behavior and must be covered by the verified active digest.
  - Opened snapshots with `O_NOFOLLOW`, validated the opened descriptor as a bounded regular file, and rejected growth or truncation during a size-limited read so preparation cannot follow a redirected final path or allocate beyond the configured limit.
  - Kept runtime snapshot tracing changes out of this story because US-032 owns removing CSV assets from generated function traces and enforcing deployment size/performance/memory budgets.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Snapshot preparation must finish validation and hydration in local state before publishing the format cache; failed reads, incompatible policy, invalid presence masks, and malformed payloads must leave the format unprepared.
  - Optimizer contexts can be constructed directly outside `generateTeam(...)`, so production context construction also calls the idempotent preparation boundary before threat or matchup reads.
  - Catalog-wide parity now checks every manifest-active species, variant, opponent, and shield rating plus weighted aggregates against source CSV evidence.
  - Default matrix iteration order is observable optimizer behavior: loss ties and floating-point means can change even when every point lookup has exact rating parity.
- Known issues / follow-ups:
  - US-032 must trace compact snapshots instead of simulation CSVs and validate the final function-size, snapshot-size, generation-time, and peak-memory budgets.
- Next iteration start:
  - US-032 - Remove simulation CSVs from runtime function traces
---

## 2026-08-07 08:21:24 MST - US-032
- Switched `/api/generate-team` tracing from active simulation CSVs and manifests to exactly one compact runtime snapshot per supported format; added non-overlapping source-evidence exclusions; enforced 100 MiB total trace and 50 MiB aggregate snapshot budgets; and retained all manifests, CSVs, and the runtime asset index for sync and validation tooling.
- Files changed: `lib/AGENTS.md`, `next.config.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/route.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/shuffled.js.nft.json`, `lib/build/fixtures/functionTrace/valid/data/simulations/cp1500/all/runtime-snapshot.json`, `prd.json`, `progress.txt`
- Commit message: `feat: US-032 - Remove simulation CSVs from runtime function traces`
- Auto approval: `enabled`
- Checks:
  - `npx vitest run lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts` (fail as expected before implementation: 4 snapshot-plan and budget regressions; pass after implementation, 14 tests)
  - `npm run build` before exclusion compaction (fail as expected: 6,788 exact exclusions exceeded Turbopack's 10 MiB compiled-regex limit)
  - `npx vitest run lib/build/runtimeFunctionAssets.test.ts` before exclusion compaction (fail as expected: compact catalog-only planner signature and exclusions missing; pass after fix, 5 tests)
  - `npx vitest run lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/data/runtimeSimulationRepository.test.ts lib/data/simulations.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts app/api/generate-team/route.test.ts` (pass, 53 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 843 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; 121 files, 19,055,267 bytes total, exactly 8 snapshots totaling 12,019,584 bytes, no simulation CSVs or manifests)
  - Production-sized fixed-seed active-only baseline across all formats (pass; cold/warm each under 60 seconds; peak RSS by format: Great 714,129,408; Ultra 526,303,232; Master 309,379,072; Weather 430,522,368; Copa Diluvio 538,181,632; Tsuki 432,111,616; Liga Ultra 373,948,416; Coupe du Sillage 468,008,960 bytes, all below the standard 2 GiB Vercel function memory limit)
  - `npx vitest run lib/build/functionTraceAnalyzer.test.ts -t "rejects aggregate snapshot bytes"` after initial review (fail as expected before injectable trace inspection: CLI ignored the oversized synthetic inventory; pass after fix)
  - `npx vitest run lib/build/functionTraceAnalyzer.test.ts lib/build/runtimeFunctionAssets.test.ts` after initial review fix (pass, 15 tests)
  - `npm test` after initial review fixes (pass, 844 tests)
  - Production-sized fixed-seed post-change comparison across all formats (pass; every cold/warm run remained within 10% of its active-only baseline and under 60 seconds; peak RSS by format: Great 713,555,968; Ultra 510,509,056; Master 305,676,288; Weather 439,058,432; Copa Diluvio 540,606,464; Tsuki 437,288,960; Liga Ultra 378,748,928; Coupe du Sillage 470,319,104 bytes, all below the standard 2 GiB Vercel function memory limit)
- Implementation agents:
  - Recommended: `performance-engineer`, `build-engineer`
  - Used: `performance-engineer`, `build-engineer`; both fit the standard-mode complex-story budget and provided complementary benchmark/memory and Next.js tracing guidance
- Specialist review:
  - Agents used: initial `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `qa-expert` re-review
  - Findings: security review found none; fixed two medium QA findings by exercising computed snapshot bytes through the analyzer CLI and recording post-change cold/warm timing plus peak RSS for every supported format; targeted QA re-review found none. Discarded the low commit-message suggestion because Ralph explicitly requires the unscoped story message.
- Decisions (why):
  - Derived snapshot runtime assets directly from `getBattleFormats()` and removed the tooling-only active CSV index from build-plan loading because runtime availability is now snapshot-authoritative.
  - Replaced 6,788 exact source exclusions with three narrow patterns after Turbopack proved that expanding the generated index exceeded its compiled-regex limit; the CSV and manifest patterns cannot match required `runtime-snapshot.json` files.
  - Kept analyzer validation strict over every traced `data/` path so a future automatic CSV, manifest, inactive candidate, or unrelated data dependency fails the build even if Next.js tracing behavior changes.
  - Used the current standard 2 GiB Vercel function memory default because the repository has no explicit function memory override; no Large Functions setting or dependency was added.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Compact snapshot tracing reduces the current generate-team NFT inventory from the 126,150,225-byte active-CSV transition baseline to 19,055,267 bytes while retaining all source evidence in the repository.
  - Build integration must validate exclusion cardinality as well as semantics; thousands of individually safe paths can still exceed Turbopack's compiled glob limit.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
  - US-033 should document the artifact split, build/analyzer workflow, size budgets, no-Large-Functions constraint, and no remote optimizer hot-path lookup requirement.
- Next iteration start:
  - US-033 - Document runtime artifact and Vercel size contracts
---

## 2026-08-07 08:39:34 MST - US-033
- Documented the distinction between full simulation CSV/manifest evidence and compact runtime snapshots; active-only tooling index semantics; snapshot generation, atomic publication, and fail-closed runtime validation; production build and NFT trace analysis commands; deployment-size budgets; and the constraints against Large Functions and remote optimizer hot-path lookups.
- Files changed: `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `prd.json`, `progress.txt`
- Commit message: `feat: US-033 - Document runtime artifact and Vercel size contracts`
- Auto approval: `enabled`
- Checks:
  - `npx prettier --write "docs/pokemon-go-team-optimization.md" "docs/team-optimization/data-inputs.md"` (pass; unchanged)
  - `npx prettier --check "docs/pokemon-go-team-optimization.md" "docs/team-optimization/data-inputs.md"` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 844 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; 121 files and 19,055,267 uncompressed bytes, including 8 snapshots totaling 12,019,584 bytes)
  - `git diff --check` (pass)
  - `npx prettier --check "docs/pokemon-go-team-optimization.md" "docs/team-optimization/data-inputs.md"` after review fix (pass)
  - `git diff --check` after review fix (pass)
- Implementation agents:
  - Recommended: `documentation-engineer`
  - Used: skipped because standard mode budgets zero implementation agents for this focused trivial documentation story and the existing architecture guidance was complete
- Specialist review:
  - Agents used: Ralph self-review under the standard-mode trivial-diff budget
  - Findings: fixed one medium documentation inconsistency by replacing the legacy direct-manifest runtime-loader description with snapshot-generation authority and explicit snapshot-only fail-closed scoring semantics; final targeted self-review found none
- Decisions (why):
  - Consolidated deployment details in the existing data-input documentation and added only a concise optimizer entry-point summary, avoiding duplicate architecture guidance.
  - Described `runtime-asset-index.json` as active-only tooling evidence rather than a production runtime dependency because compact snapshots are now the deployed simulation authority.
  - Recorded the 200 MiB active-CSV transition budget separately from the enforced 100 MiB compact function and 50 MiB snapshot-data targets so historical and final contracts remain clear.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` skill plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Production trace validation is the executable deployment contract: it verifies exact catalog snapshots, rejects simulation CSV and manifest leakage, and enforces compact artifact budgets.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
- Next iteration start:
  - All PRD stories are complete after US-033 passes review and metadata consistency checks.
---

## 2026-08-07 11:57:30 MST - Opt-in moveset variant simulations
- Added an off-by-default team configuration toggle for simulation-backed moveset variants under a divided Moveset Options section after Pokemon exclusions; disabled generation uses one PvPoke ranked-default assignment for the complete roster in both GBL and PlayPokemon, while enabled generation preserves team-aware selection and bounded finalist reranking.
- Read the `gbl-optimizer` and `test-driven-development` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
- Checks:
  - `npx vitest run components/organisms/TeamManager/TeamManager.test.tsx components/organisms/TeamConfigPanel/TeamConfigPanel.test.tsx app/api/generate-team/route.test.ts lib/genetic/moveset.test.ts` (fail as expected before implementation: UI state/control, API validation/default, and ranked-default resolver missing)
  - `npx vitest run lib/genetic/algorithm.test.ts` (fail as expected before implementation: default GBL and PlayPokemon assignment policies remained variant-aware)
  - `npx vitest run components/organisms/TeamManager/TeamManager.test.tsx components/organisms/TeamConfigPanel/TeamConfigPanel.test.tsx app/api/generate-team/route.test.ts lib/genetic/moveset.test.ts lib/genetic/algorithm.test.ts` (pass, 83 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 852 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npx prettier --check` for all changed supported source and documentation files (pass)
  - `git diff --check` (pass)
  - Chrome desktop and 390x844 mobile accessibility snapshots (pass; switch starts unchecked, toggles checked, and has the accessible name `Simulate moveset variants`; existing missing `favicon.ico` remains the only console error)
---

## 2026-08-07 14:10:44 MST - US-034
- Replaced the roster-wide assignment policy shortcut with a canonical immutable `authorityBySpeciesId` map; bound every per-species authority into deterministic fingerprints; validated authority, variant, and canonical roster key parity; kept manifest defaults variant-qualified and ranked-default fallbacks unqualified across lineup, finalist, shield, and contribution lookups; and projected exact authority through generation, team details, client validation, analysis, and export pass-through boundaries.
- Files changed: `lib/AGENTS.md`, `lib/types.ts`, `lib/genetic/moveset.ts`, `lib/genetic/moveset.test.ts`, `lib/genetic/fitness/finalistReranking.ts`, `lib/genetic/fitness/finalistReranking.test.ts`, `lib/genetic/fitness/lineupScoring.test.ts`, `lib/genetic/fitness/rosterScoring.test.ts`, `lib/genetic/fitness/index.test.ts`, `lib/genetic/algorithm.test.ts`, `lib/analysis/shieldScenarioAnalysis.test.ts`, `lib/analysis/pokemonContributionAnalysis.test.ts`, `app/api/generate-team/route.test.ts`, `app/api/team-details/route.test.ts`, `components/organisms/TeamDisplay/TeamDisplay.tsx`, `components/organisms/TeamDisplay/TeamDisplay.test.tsx`, `components/organisms/TeamManager/TeamManager.test.tsx`, `lib/export/exportTeam.test.ts`, `lib/export/copyTeamToClipboard.test.ts`, `prd.json`, `progress.txt`
- Commit message: `feat: US-034 - Track moveset assignment authority per roster species`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/genetic/moveset.test.ts app/api/team-details/route.test.ts components/organisms/TeamDisplay/TeamDisplay.test.tsx` (fail as expected before implementation: mixed fingerprints, authority key validation, API projection, and client authority checks were missing)
  - focused assignment, finalist, lineup, analysis, API, client, export, and clipboard suite (pass, 141 tests after the TeamDisplay multi-species fixture was corrected)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 857 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `typescript-pro`, `refactoring-specialist`
  - Used: `typescript-pro`; skipped `refactoring-specialist` because one TypeScript contract specialist plus established assignment boundaries covered the standard-mode complex-story budget without overlapping guidance
- Specialist review:
  - Agents used: initial staged `code-reviewer` and `qa-expert` pass
  - Findings: none; both specialists returned empty actionable finding lists and noted only the intentionally deferred US-035 mixed-resolution and US-036 authority-authenticity work
- Decisions (why):
  - Removed the global assignment policy field instead of retaining a compatibility shortcut because transient assignments are not persisted and a roster-wide source cannot represent mixed authority safely.
  - Kept the existing policy identity shape for each species so manifest schema/policy identity and ranked-default fallback identity remain explicit without adding another parallel contract.
  - Centralized simulation qualification in `getAssignedMovesetVariantId(...)` so `isDefault` does not incorrectly make manifest defaults unqualified and consumers do not repeat authority logic.
  - Added authority to team-details move projection and client response validation while leaving export text unchanged because authority is control metadata, not an acquisition requirement or move selection input.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Manifest-backed default variants still require their canonical variant ID for aggregate and shield-scenario snapshot lookup; only ranked-default fallback species use unqualified lookup.
  - Assignment fingerprints are deterministic identity, not authenticity; repository-authority validation remains the responsibility of US-036.
- Known issues / follow-ups:
  - US-035 should create mixed assignments by applying ranked-default fallback only to snapshot-unsupported species while preserving fail-closed snapshot integrity errors.
  - Browser verification was not required because the component change only validates the existing non-visual API contract and adds no rendered UI or interaction change.
- Next iteration start:
  - US-035 - Fall back unsupported variant anchors to default movesets
---

## 2026-08-07 14:30:51 MST - US-035
- Implemented strict per-species ranked-default fallback for snapshot-unsupported roster members while retaining manifest-backed variants for supported teammates; added mixed-authority GBL assignment binding before lineup scoring, singleton fallback slots in bounded PlayPokemon finalist enumeration, and a real Great League Gyarados anchor regression.
- Files changed: `lib/AGENTS.md`, `lib/genetic/moveset.ts`, `lib/genetic/moveset.test.ts`, `lib/genetic/fitness/index.ts`, `lib/genetic/fitness/index.test.ts`, `lib/genetic/algorithm.performance.test.ts`, `progress.txt`
- Commit message: `feat: US-035 - Fall back unsupported variant anchors to default movesets`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/genetic/moveset.test.ts lib/genetic/fitness/index.test.ts` (fail as expected before implementation: 5 mixed fallback, fail-closed, and GBL assignment-binding regressions)
  - `npx vitest run lib/genetic/algorithm.performance.test.ts -t "unsupported explicit anchor"` (fail as expected before implementation: Gyarados variant-unavailable failure escaped generation)
  - `npx vitest run lib/genetic/moveset.test.ts lib/genetic/fitness/index.test.ts lib/genetic/algorithm.performance.test.ts` (pass, 38 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass after removing one unused test import)
  - `npm test` (pass, 863 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `npx vitest run lib/genetic/moveset.test.ts lib/genetic/fitness/index.test.ts lib/genetic/algorithm.performance.test.ts` after review fixes (pass, 51 tests)
  - `npx tsc --noEmit` and `npm run lint` after review fixes (pass)
- Implementation agents:
  - Recommended: `backend-developer`, `debugger`
  - Used: `backend-developer`, `debugger`; both fit the standard-mode complex-story budget and provided complementary mixed-authority structure, strict error filtering, early GBL scoring, assignment-bound, and regression-fixture guidance
- Specialist review:
  - Agents used: initial staged `code-reviewer` and `qa-expert`; targeted `qa-expert` re-review
  - Findings: fixed one medium QA finding by exercising fail-closed errors and mismatched unavailable identities inside both per-species catch boundaries; also fixed low findings by correcting resolver TSDoc and binding the real Gyarados fixture to ranking-pool, top-150, and exact ranked-default expectations; targeted re-review found no blocking issues and deferred one low documentation update to US-037, which explicitly owns mixed-authority and fail-closed documentation
- Decisions (why):
  - Required `variant-unavailable` to match the selected format and canonical species and to omit a specific variant ID, because mismatched or variant-qualified errors indicate authority inconsistency rather than an unsupported roster species.
  - Kept `getSimulationBackedMovesetVariantForTeam(...)` strict and applied fallback in roster resolution or the unbound moveset adapter, because swallowing the error in the variant selector would mislabel a ranked default as manifest-backed.
  - Bound one resolved assignment before GBL lineup enumeration so mixed authority controls matchup, shield, consistency, move typing, diagnostics, and cache identity throughout scoring rather than only in final output.
  - Represented each unsupported PlayPokemon roster member as one fallback enumeration slot, preserving deterministic Cartesian ordering and reducing the six-member upper bound from `3^6` to `1 * 3^5` when one species is unsupported.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Great League `gyarados` is currently an eligible explicit ranking-pool anchor without an active compact-snapshot variant, making it a useful end-to-end mixed-authority regression fixture.
  - A fallback moveset value alone cannot communicate lookup authority; the outer resolver must observe the typed unavailable-species error and set `authorityBySpeciesId` before scoring.
- Known issues / follow-ups:
  - US-036 must validate returned transient assignment values against current ranking and compact-snapshot authorities; deterministic fingerprints remain identity rather than authenticity.
  - US-037 must replace the optimizer guide's pre-US-035 missing-manifest fallback description with mixed per-species authority and fail-closed snapshot semantics.
- Next iteration start:
  - US-036 - Validate returned moveset assignments against runtime authorities
---

## 2026-08-07 14:54:46 MST - US-036
- Added an injectable repository-authority validator after structural assignment parsing; manifest-backed species now require the prepared compact snapshot's current policy and a complete active variant match, ranked-default fallbacks require the current format ranking recommendation and fallback policy, and team details projects only the validated original assignment.
- Files changed: `app/AGENTS.md`, `app/api/team-details/route.ts`, `app/api/team-details/route.test.ts`, `lib/AGENTS.md`, `lib/genetic/moveset.ts`, `lib/genetic/moveset.test.ts`, `prd.json`, `progress.txt`
- Commit message: `feat: US-036 - Validate returned moveset assignments against runtime authorities`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/genetic/moveset.test.ts` (fail as expected before implementation: authority validator was missing)
  - `npx vitest run app/api/team-details/route.test.ts` (fail as expected before route integration: validator was not invoked and authority failures returned 200)
  - `npx vitest run lib/genetic/moveset.test.ts -t "without a complete format ranking"` (fail as expected during review remediation: missing ranking authority raised an untyped error)
  - `npx vitest run lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` (pass, 79 tests after review fixes)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` after review fixes (pass after correcting one import-order error)
  - `npm test` after review fixes (pass, 900 tests)
  - `npm run build` after review fixes (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `security-engineer`, `backend-developer`
  - Used: `security-engineer`, `backend-developer`; both fit the standard-mode high-risk budget and converged on the same injectable validation boundary, exact comparison rules, lazy snapshot preparation, and fail-closed error handling
- Specialist review:
  - Agents used: initial staged `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `qa-expert` and `security-engineer` re-review
  - Findings: `code-reviewer` found none; fixed one medium QA finding by replacing duplicate mocked route failures with distinct recomputed payloads through the real validator, and one medium security finding by rejecting fallback authority without a complete current format ranking; targeted re-review found none
- Decisions (why):
  - Kept validation beside assignment parsing and projection in `lib/genetic/moveset.ts` so the route remains a thin adapter and unit tests can inject snapshot and ranking authorities without filesystem or module patching.
  - Compared every variant field, including preferred charged-move order and `isDefault`, because canonical variant IDs alone intentionally normalize charged-move order and cannot prove the complete scored assignment.
  - Used a strict complete ranking lookup only for returned fallback authority validation; generation keeps its existing movepool fallback behavior, but an external authority claim cannot be authenticated when the selected format has no complete ranking recommendation.
  - Prepared compact simulation data once only when at least one species claims manifest authority; fallback-only assignments remain ranking-only, while malformed, missing, incompatible, incomplete, and unprepared snapshot failures propagate instead of being reclassified as fallback.
  - Converted only the exact unqualified unavailable-species error for a manifest claim into a caller validation failure; mismatched, variant-qualified, and integrity errors remain operational failures.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - A deterministic fingerprint proves that payload fields agree with each other, but current repository-owned compact snapshots and rankings are required to prove those fields remain authoritative.
  - `getActiveMovesetVariants(...)` exposes complete preferred-order variants from the prepared snapshot, allowing authenticity checks without reading manifests, simulation CSVs, or PvPoke runtime code.
- Known issues / follow-ups:
  - US-037 must add `/api/team-details` compact snapshot trace planning, route trace analysis, runtime-boundary coverage, and mixed-authority documentation for the new runtime dependency.
- Next iteration start:
  - US-037 - Guard assignment authority deployment and documentation contracts
---

## 2026-08-07 15:18:55 MST - US-037
- Added an exact catalog-derived `/api/team-details` trace plan containing compact snapshots, Overall rankings, Pokemon, and move data; excluded source simulation evidence, unrelated ranking categories, type-effectiveness data, and PvPoke vendor assets; extended the analyzer to validate generate-team and team-details independently against the 100 MiB function budget and one deduplicated 50 MiB snapshot budget; added focused NFT and runtime-boundary coverage; and documented mixed per-species authority, strict fallback, repository-authority validation, and fail-closed snapshot behavior.
- Files changed: `next.config.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/data/moves.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js.nft.json`, `lib/architecture/pvpokeRuntimeBoundary.ts`, `lib/architecture/pvpokeRuntimeBoundary.test.ts`, `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `progress.txt`
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts` before implementation (fail as expected: 6 tests exposed the missing team-details plan, trace inspection, budgets, and vendor gate)
  - `npx vitest run lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts` (pass, 15 tests)
  - `npx vitest run lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` (pass, 99 tests)
  - `npx prettier --write` for all changed TypeScript, JSON fixture, and optimizer documentation files (pass)
  - `npx prettier --check` for all changed TypeScript and optimizer documentation files (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 901 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; generate-team 121 files and 19,058,643 bytes, team-details 71 files and 16,218,483 bytes, 8 unique snapshots totaling 12,019,584 bytes)
  - `git diff --check` (pass)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts` during review remediation (fail as expected: route-rooted transitive dependency graph helper was missing)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts` after review remediation (pass, 20 tests)
  - `npx tsc --noEmit` and `npm run lint` after review remediation (pass)
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: `build-engineer`, `documentation-engineer`; both fit the standard-mode complex-story budget and provided complementary route-tracing, budget, runtime-boundary, mixed-authority, and stale-documentation guidance
- Specialist review:
  - Agents used: initial staged `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `qa-expert` and `security-engineer` re-review
  - Findings: fixed the initial medium QA finding by replacing a manually maintained source list with a route-rooted transitive runtime module graph, but targeted re-review found four remaining medium boundary gaps; commit blocked after the single allowed targeted re-review
- Decisions (why):
  - Kept `analyze:generate-team-trace` as the established command while making its validation cover both runtime routes, avoiding package-script churn and documenting the retained name explicitly.
  - Gave team-details only catalog snapshots, Overall rankings, Pokemon, and move data because those are the repository authorities used by structural parsing, acquisition projection, and authority validation; unrelated ranking categories and type-effectiveness data are excluded.
  - Counted compact snapshots once across both route inventories because both functions trace the same catalog-derived files and the 50 MiB contract applies to the unique deployed snapshot set.
  - Added explicit PvPoke vendor path rejection because data-only allowlisting cannot detect vendor JavaScript outside `data/`, while the source architecture test continues to guard runtime imports and loads.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - `/api/team-details` needs every format snapshot at deployment even though fallback-only requests skip preparation, because any valid returned mixed or manifest assignment can select any supported format.
  - Route trace validation must combine exact repository-data allowlists with a separate vendor-path gate; unauthorized `data/` checks alone do not cover PvPoke JavaScript.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
  - Commit blocked: the runtime dependency walker can omit value-level `typeof import(...)`, no-substitution template-literal imports, and `.js` specifiers resolved by TypeScript to `.ts`; the boundary predicate also needs to reject direct source simulation manifests, CSVs, the runtime asset index JSON, and equivalent non-compact data authorities.
  - US-037 remains `passes: false`; no commit was created because blocking medium findings remain after the targeted re-review.
- Next iteration start:
  - Resume US-037 by adding failing dependency-graph fixtures for the four bypasses, fix resolution and forbidden source-data assertions, rerun all checks, and complete a fresh staged review loop.
---

## 2026-08-07 15:40:03 MST - US-037
- Resumed the blocked deployment-contract candidate; closed all four boundary bypasses by following executable value-level `typeof import(...)`, no-substitution template imports, and JavaScript specifiers resolved to TypeScript source, while applying one team-details authority predicate to the transitive route graph and exact traced data plan so legacy loaders, source manifests, simulation CSVs, the runtime asset index, and unrelated ranking data are rejected.
- Files changed: `lib/AGENTS.md`, `next.config.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/data/moves.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/ordered.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/pokemon-list/route.js`, `lib/architecture/pvpokeRuntimeBoundary.ts`, `lib/architecture/pvpokeRuntimeBoundary.test.ts`, `lib/genetic/moveset.ts`, `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `progress.txt`
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts` before remediation (fail as expected: missing authority predicate and executable specifier forms produced two failures)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts` (pass, 6 tests)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` (pass, 100 tests)
  - `npx prettier --check` for all changed TypeScript, JSON fixture, AGENTS, and optimizer documentation files (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` before import-order fix (fail: one `import/order` error in `lib/architecture/pvpokeRuntimeBoundary.test.ts`)
  - `npm run lint` after import-order fix (pass)
  - `npm test` (pass, 902 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; generate-team 121 files and 19,058,643 bytes; both route asset plans and budgets validated; 8 unique snapshots total 12,019,584 bytes)
  - `git diff --check && git diff --cached --check` (pass)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/functionTraceAnalyzer.test.ts` during review remediation (fail as expected: 5 tests exposed executable object-value imports, type-only forms, extension precedence, route entrypoint undercounting, and symlinked data authority)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` after review remediation (pass, 96 tests)
  - `npx tsc --noEmit`, `npm run lint`, and changed-file Prettier checks after review remediation (pass)
  - `npm test` after review remediation (pass, 903 tests)
  - `npm run build` after review remediation (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` after review remediation (pass; generate-team 122 files and 19,058,698 bytes including its emitted route entrypoint; both route plans and budgets validated; 8 unique snapshots total 12,019,584 bytes)
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: `build-engineer`, `documentation-engineer`; both fit the standard-mode complex-story budget and confirmed the route-specific plan, shared budgets, fail-closed authority semantics, existing documentation locations, and required checks
- Specialist review:
  - Agents used: fresh staged `code-reviewer`, `qa-expert`, and `security-engineer` high-risk pass; targeted re-review pending for the same affected domains
  - Findings: fixed merged blocking findings by replacing textual type-only heuristics with TypeScript AST ranges, preferring TypeScript extension substitutions, binding data authority to the exact catalog plan, removing the unreferenced type-effectiveness-dependent legacy moveset optimizer, canonicalizing and containing traced paths, rejecting symlinked repository data, and counting emitted route entrypoints; skipped one low shuffled-format suggestion as non-blocking and unrelated to the fixed root causes
- Decisions (why):
  - Extended the existing route-rooted dependency helper rather than adding a parallel scanner so module resolution and type-only filtering remain one reusable boundary.
  - Used TypeScript AST source ranges for erased import forms and kept JavaScript-specifier mapping narrow (`.js`, `.jsx`, `.mjs`, and `.cjs`) with TypeScript candidates first, matching compilation without guessing arbitrary extensions.
  - Bound the team-details authority predicate to the exact catalog-derived asset set rather than CP/cup-shaped path patterns, and removed unused `getOptimalMovesetForTeam(...)`/cache code so team-details no longer reaches type-effectiveness data through its moveset module.
  - Counted each emitted canonical `route.js`, rejected symlinked repository data, confined project paths physically, and allowed external trace targets only under canonical `node_modules` dependency roots before deduplication.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
  - A preceding `typeof` does not by itself make `import(...)` type-only; value-level `typeof import(...)` executes a runtime import and must remain in the transitive graph.
  - TypeScript commonly resolves emitted `.js` module specifiers to `.ts`, `.tsx`, `.mts`, or `.cts` source, so source-level architecture walkers must model that mapping.
  - NFT `files` arrays omit the emitted route entrypoint, so deployment budgets must add the canonical `route.js` and canonicalize physical targets before deduplicating bytes.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
- Next iteration start:
  - Complete the one targeted specialist re-review, mark US-037 passing, and commit if no blocking findings remain.
---

## 2026-08-07 19:40:38 MST - US-037
- Completed the single targeted specialist re-review of the staged deployment-contract candidate; three deduplicated medium boundary findings remain, so the story stays blocked and uncommitted under the bounded review-loop rules.
- Files changed: `progress.txt` (review result only; the prior staged US-037 candidate remains unchanged)
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - Prior staged candidate checks remain recorded in the preceding US-037 entry; no checks were rerun because this iteration made no implementation, test, or documentation changes.
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: skipped in this resumed review-only iteration because both recommended agents had already advised the staged implementation and no new implementation was permitted after the bounded targeted review
- Specialist review:
  - Agents used: targeted `qa-expert` and `security-engineer`
  - Findings: three deduplicated medium blockers remain: preserve executable default imports paired with type-only named bindings, reject or resolve interpolated dynamic imports instead of omitting them, and constrain trace canonicalization against symlinked path components plus arbitrary external `node_modules` roots
- Decisions (why):
  - Stopped without fixing or committing because Ralph permits at most one targeted specialist re-review after substantive remediation and requires unresolved medium findings after that pass to block the story.
  - Merged duplicate QA and security findings for symlink traversal and external dependency roots into one containment remediation area.
- **Learnings for future iterations:**
  - Import-graph erasure must inspect the complete import clause: `import Default, { type T }` still has a runtime dependency.
  - Unresolved local dynamic import templates must fail closed; omitting them from a runtime authority graph creates a boundary bypass.
  - Trace containment must reject symlinks in protected path components and accept external files only beneath explicitly trusted canonical dependency roots.
- Known issues / follow-ups:
  - `US-037` remains `passes: false`; no commit was created.
  - Add failing fixtures for the three blocker areas, remediate them, rerun all required checks, and begin a fresh staged review loop in the next iteration.
- Next iteration start:
  - Resume US-037 with test-first fixes for mixed import clauses, interpolated local dynamic imports, and canonical trace containment.
---

## 2026-08-07 19:50:20 MST - US-037
- Resumed the blocked deployment-contract candidate and closed the three remaining boundary findings: mixed default-plus-type-only imports remain executable dependencies, unresolved local dynamic-import templates fail closed, repository-data symlinks are rejected at every path component, and external trace files are accepted only beneath explicit canonical ancestor dependency roots. Tightened the route-plan documentation and rejected absolute or parent-traversal authority paths.
- Files changed: `next.config.ts`, `lib/AGENTS.md`, `lib/architecture/pvpokeRuntimeBoundary.ts`, `lib/architecture/pvpokeRuntimeBoundary.test.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/data/moves.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/ordered.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/pokemon-list/route.js`, `lib/genetic/moveset.ts`, `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `progress.txt`
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts` before implementation (fail as expected: mixed imports and unresolved local template imports produced 2 failures)
  - `npx vitest run lib/build/functionTraceAnalyzer.test.ts` before implementation (fail as expected: symlinked path components and arbitrary external dependency roots produced 2 failures)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "keeps team-details authority validation"` before adjacent boundary fix (fail as expected: absolute and parent-traversal paths were allowed)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` (pass, 104 tests)
  - `npx prettier --check docs/pokemon-go-team-optimization.md docs/team-optimization/data-inputs.md` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 906 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; generate-team 122 files and 19,058,698 bytes; both runtime route plans and budgets validated; 8 unique snapshots total 12,019,584 bytes)
  - `git diff --check && git diff --cached --check` (pass)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts` during review remediation (fail as expected: computed imports and extensionless TypeScript precedence produced 5 failures)
  - `npx vitest run lib/build/functionTraceAnalyzer.test.ts` during review remediation (fail as expected: canonical project aliases and trusted dependency symlinks produced 2 failures)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` after review remediation (pass, 109 tests)
  - Changed-file `npx prettier --check`, `npx tsc --noEmit`, and `npm run lint` after review remediation (pass)
  - `npm test` after review remediation (pass, 911 tests)
  - `npm run build` after review remediation (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` after review remediation (pass; generate-team 122 files and 19,058,698 bytes; both runtime route plans and budgets validated; 8 unique snapshots total 12,019,584 bytes)
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: `build-engineer`, `documentation-engineer`; both fit the standard-mode complex-story budget and identified the exact import-graph, canonical containment, route-plan, analyzer-scope, and documentation checks needed to complete the resumed candidate
- Specialist review:
  - Agents used: fresh staged `code-reviewer`, `qa-expert`, and `security-engineer` high-risk pass; targeted `code-reviewer` and `security-engineer` re-review
  - Findings: QA found none; fixed the initial merged findings, but targeted re-review found three deduplicated medium blockers: replace regex module-specifier extraction with TypeScript AST extraction for all executable literal imports, check every direct extensionless file candidate before directory indexes, and canonicalize the project root before deriving trusted dependency ancestors
- Decisions (why):
  - Removed the redundant text-based named-import heuristic because the existing TypeScript AST ranges correctly erase pure type imports while preserving a default value import in the same clause.
  - Rejected every unresolved executable dynamic import instead of guessing possible files because an incomplete runtime graph would make the route authority boundary fail open.
  - Trusted only canonical `node_modules` directories on the project-to-filesystem ancestor chain, preserving workspace dependencies without accepting arbitrary external paths that merely contain a `node_modules` segment.
  - Checked every repository-data path component for symlinks before canonicalization so an intermediate linked directory cannot bypass exact traced-data authority.
  - Reported and categorized canonical targets after containment so a project-local alias cannot hide source simulation data or PvPoke vendor assets behind an application-code path.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
  - A named binding list containing only `type` specifiers does not make the complete import type-only when the clause also has a default value binding.
  - Checking only the final traced path with `lstat` misses symbolic links in parent data directories; containment must be established before canonical byte accounting.
  - Extensionless source-graph resolution must prefer TypeScript candidates before JavaScript files to match the application's compiler behavior.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
  - `US-037` remains `passes: false`; no commit was created because medium findings remain after the single targeted re-review.
  - Literal imports containing comments, dollar characters, or escaped delimiters can still evade the regex dependency graph even though non-literal imports fail closed.
  - Extensionless resolution currently interleaves direct and index candidates, and trusted dependency roots are derived before canonicalizing a symlinked project root.
- Next iteration start:
  - Resume US-037 test-first with AST module-specifier extraction, direct-file-before-index extensionless precedence, and canonical project-root trust derivation; rerun checks and begin a fresh staged review loop.
---

## 2026-08-07 20:10:04 MST - US-037
- Resumed the blocked deployment-contract candidate and closed the final three boundary gaps: executable imports now come from decoded TypeScript AST literals, direct extensionless source files precede directory indexes, and NFT trace containment derives trusted dependency roots from the canonical project path while retaining lexical diagnostics and data-symlink checks.
- Files changed: `next.config.ts`, `lib/AGENTS.md`, `lib/architecture/pvpokeRuntimeBoundary.ts`, `lib/architecture/pvpokeRuntimeBoundary.test.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/data/moves.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/ordered.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/pokemon-list/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js.nft.json`, `lib/genetic/moveset.ts`, `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `prd.json`, `progress.txt`
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "discovers executable TypeScript imports"` before implementation (fail as expected: dollar-containing template and escaped static imports were omitted, and a directory index displaced a direct source file)
  - `npx vitest run lib/build/functionTraceAnalyzer.test.ts -t "canonical project path"` before implementation (fail as expected: the canonical route entrypoint was rejected before the lexical dependency trust bypass could be evaluated)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "team-details authority|discovers executable TypeScript imports|rejects unresolved executable|resolve outside"` during review remediation (fail as expected: normalized traversal, Windows absolutes, CommonJS loaders, compiler-resolution collisions, and local containment were not fail-closed)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` after review remediation (pass, 114 tests)
  - Changed-file `npx prettier --check` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` after review remediation (pass, 916 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; generate-team 122 files and 19,058,698 bytes; both runtime route plans and budgets validated; 8 unique snapshots total 12,019,584 bytes)
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: skipped in this resumed iteration because both recommended agents had already advised the existing US-037 candidate and the remaining remediations were exact review findings with focused regressions
- Specialist review:
  - Agents used: initial `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `code-reviewer`, `qa-expert`, and `security-engineer` re-review
  - Findings: fixed normalized traversal, Windows absolutes, direct CommonJS loaders, compiler-option resolution, and local containment; targeted code review found none, but QA and security found two deduplicated remaining medium blockers: incomplete `createRequire` alias coverage and declaration-file resolution that can inspect `.d.ts` instead of the runtime `.js` implementation
- Decisions (why):
  - Replaced dependency regex extraction with TypeScript AST traversal so comments, dollar characters, escaped delimiters, import/export declarations, import-equals declarations, dynamic imports, and `require(...)` literals share compiler-decoded semantics.
  - Kept all non-literal executable ESM and CommonJS loads fail-closed and preserved pure type erasure because the route authority graph must model runtime dependencies without retaining compile-time-only modules.
  - Replaced manual local-module candidate ordering with `ts.resolveModuleName(...)` and parsed project compiler options because Bundler resolution can legitimately prefer JavaScript over `.mts` and the boundary must inspect the same source the application bundles.
  - Kept lexical project paths for trace diagnostics and repository-data symlink inspection, but used the canonical project root for physical containment, dependency trust ancestry, categorization, and canonical reporting.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
  - TypeScript AST string-literal text is already decoded, avoiding regex blind spots for escaped quotes and no-substitution templates containing dollar characters.
  - CommonJS graph extraction must unwrap transparent TypeScript expressions and track `require.resolve`, `module.require`, and `createRequire` aliases; every remaining computed loader expression fails closed.
  - Local compiler resolution failures and symlink escapes are boundary violations, not optional unresolved dependencies.
  - Canonicalizing only traced targets is insufficient when the project root itself is a symlink; trusted ancestor dependency roots must originate from the canonical project root.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
  - `US-037` remains `passes: false`; no commit was created because medium findings remain after the single targeted re-review.
  - Track `createRequire` from both `node:module` and `module`, namespace imports, and CommonJS destructuring, with literal and computed-loader regressions.
  - Reject declaration-only executable resolutions or resolve and contain the corresponding runtime implementation; add a `.d.ts` plus `.js` collision regression.
- Next iteration start:
  - Resume US-037 test-first with complete `createRequire` alias provenance and runtime implementation resolution, rerun checks, and begin a fresh staged review loop.
---

## 2026-08-07 20:38:29 MST - US-037
- Resumed the blocked deployment-contract candidate; completed `createRequire` provenance across `node:module` and `module` named, namespace, CommonJS, destructured, direct-member, and assigned bindings, and made executable local imports follow adjacent runtime JavaScript instead of declaration-only TypeScript resolutions.
- Files changed: `next.config.ts`, `lib/AGENTS.md`, `lib/architecture/pvpokeRuntimeBoundary.ts`, `lib/architecture/pvpokeRuntimeBoundary.test.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/data/moves.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/ordered.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/pokemon-list/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js.nft.json`, `lib/genetic/moveset.ts`, `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `progress.txt`
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "createRequire factories|runtime JavaScript"` before implementation (fail as expected: four aliased loaders were omitted and the graph followed only `runtimeAuthority.d.ts`)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "createRequire factories"` during implementation (fail as expected: direct CommonJS, namespace-destructured, and assigned member factories remained omitted)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "createRequire factories|computed reassigned"` during review remediation (fail as expected: reassignment, indirect factory, and `module.require` aliases were omitted and computed reassigned loaders did not fail closed)
  - `npx vitest run lib/build/functionTraceAnalyzer.test.ts -t "shared runtime snapshot|distinct aggregate"` during review remediation (pass, proving the existing production deduplication with discriminating fixtures)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` after review remediation (pass, 118 tests)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` after review remediation (pass, 920 tests)
  - Changed-file `npx prettier --check` (pass)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; generate-team 122 files and 19,058,698 bytes; both runtime route plans and budgets validated; 8 unique snapshots total 12,019,584 bytes)
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: skipped in this resumed iteration because both recommended agents already advised the existing US-037 candidate and the remaining work consisted of exact prior review findings with focused regressions
- Specialist review:
  - Agents used: initial `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `code-reviewer`, `qa-expert`, and `security-engineer` re-review
  - Findings: fixed two initial deduplicated medium areas by following assignment expressions, indirect factories, and `module.require` aliases with literal/computed regressions, and by adding discriminating shared-versus-distinct snapshot budget tests; targeted QA and security found none, but targeted code review found one remaining medium chained-assignment provenance bypass
- Decisions (why):
  - Extended the existing TypeScript AST provenance pass instead of adding another textual scanner so ESM and CommonJS `createRequire` forms share one fail-closed graph.
  - Mapped local `.d.ts`, `.d.mts`, and `.d.cts` resolutions only to adjacent JavaScript runtime forms and rejected declaration-only local modules, preventing compile-time declarations from hiding executable dependencies.
  - Kept the snapshot byte implementation unchanged because it already canonicalizes by path and rejects inconsistent duplicate sizes; the review gap was test discrimination, not production behavior.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
  - `createRequire` is a loader factory rather than a loader itself; graph extraction must retain its source-module namespace and factory provenance until the produced require function is invoked.
  - Loader provenance can cross both declaration initializers and later assignment expressions; sequence expressions use their rightmost value, while aliases of `module.require` remain executable loaders.
  - `ts.resolveModuleName(...)` can select a declaration file for an executable local import, so architecture walkers must inspect the corresponding runtime implementation rather than treating the declaration as executable authority.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
  - `US-037` remains `passes: false`; no commit was created because a medium finding remains after the single targeted re-review.
  - Chained assignments such as `a = b = nodeModule.createRequire` leave the outer identifier bound to an unhandled assignment expression; unwrap simple assignment expressions to their right-hand value and add literal and computed chained-assignment regressions.
- Next iteration start:
  - Resume US-037 test-first with chained-assignment loader provenance, rerun all checks, and begin a fresh staged review loop.
---

## 2026-08-07 21:07:03 MST - US-037
- Resumed the blocked deployment-contract candidate and closed the final chained-assignment provenance bypass: outer factory and loader aliases now inherit the right-hand runtime value, literal chained aliases enter the dependency graph, and computed chained loads fail closed.
- Files changed: `next.config.ts`, `lib/AGENTS.md`, `lib/architecture/pvpokeRuntimeBoundary.ts`, `lib/architecture/pvpokeRuntimeBoundary.test.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/data/moves.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/ordered.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/pokemon-list/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js.nft.json`, `lib/genetic/moveset.ts`, `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `progress.txt`
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "createRequire factories|computed chained"` before implementation (fail as expected: literal and computed outer chained aliases were omitted)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "createRequire factories|computed chained"` (pass, 2 tests)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts -t "createRequire factories|computed call|computed apply|computed createRequire member|exported createRequire|symbolic-link entrypoints|missing snapshots"` during review remediation (fail as expected: 7 tests exposed indirect loaders, unresolved loader provenance, symlinked entrypoints, and off-directory evidence)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` after review remediation (pass, 124 tests)
  - `npx prettier --check docs/pokemon-go-team-optimization.md docs/team-optimization/data-inputs.md lib/AGENTS.md` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` after review remediation (pass, 926 tests)
  - `npm run build` after review remediation (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` after review remediation (pass; generate-team 122 files and 19,058,698 bytes; both route plans and 100 MiB function budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: skipped in this resumed iteration because both recommended agents had already advised the existing US-037 candidate and the remaining remediation was one exact prior review finding with focused regressions
- Specialist review:
  - Agents used: fresh staged `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `code-reviewer` and `security-engineer` re-review
  - Findings: QA found none; fixed the initial four medium findings, but targeted re-review found three deduplicated medium blockers: computed loader-member aliases and computed `call`/`apply` members still fail open, export assignments can expose tracked factories/loaders, and the source-graph authority predicate still allows recognizable evidence outside `data/`
- Decisions (why):
  - Extended the existing transparent loader-expression unwrapping to simple assignment expressions instead of adding another provenance pass, so both inner and outer identifiers converge through the same fixed-point binding analysis.
  - Kept the fix scoped to plain `=` and comma expressions; compound assignments do not propagate an equivalent loader value and must not be treated as aliases.
  - Rejected loader factories exported across local module boundaries instead of guessing inter-module runtime values; the architecture gate must fail closed when provenance cannot be established exactly.
  - Classified project-local CSVs plus canonical manifest, index, and snapshot filenames as route data authorities regardless of directory while leaving ordinary application JSON and external dependency assets unaffected.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` and `test-driven-development` skills plus `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
  - In `a = b = value`, both assignment nodes are collected, but the outer binding's initializer is itself an assignment expression and must be unwrapped to the right-hand runtime value before provenance classification.
  - CommonJS `call` uses its second argument as the module specifier; `apply` requires a literal argument array whose first element is the module specifier, and all other forms fail closed.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
  - `US-037` remains `passes: false`; no commit was created because medium findings remain after the single targeted re-review.
  - Reject computed element-access bindings on tracked module namespaces and computed member calls on tracked loaders; reject tracked factory/loader export assignments including default, export-equals, `module.exports`, and `exports` forms; and apply recognizable CSV/manifest/index/snapshot classification to the source-graph authority predicate.
- Next iteration start:
  - Resume US-037 test-first with the three targeted-review blocker areas, rerun all checks, and begin a fresh staged review loop.
---

## 2026-08-07 21:30:22 MST - US-037
- Resumed the blocked deployment-contract candidate and closed the final three review areas: computed tracked loader and module-namespace members now fail closed, default/CommonJS/export-equals assignments cannot expose tracked `createRequire` provenance across module boundaries, and recognizable CSV/manifest/index/snapshot evidence outside `data/` must match the exact route authority plan.
- Files changed: `next.config.ts`, `lib/AGENTS.md`, `lib/architecture/pvpokeRuntimeBoundary.ts`, `lib/architecture/pvpokeRuntimeBoundary.test.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/data/moves.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/ordered.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/pokemon-list/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js.nft.json`, `lib/genetic/moveset.ts`, `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `progress.txt`
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "team-details authority|unresolved executable module load|tracked loader provenance"` before implementation (fail as expected: 9 tests exposed all three blocker areas)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "team-details authority|unresolved executable module load|tracked loader provenance"` (pass, 21 selected tests)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` (pass, 132 tests)
  - `npx prettier --check` for all changed TypeScript, AGENTS, and optimizer documentation files (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 934 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; generate-team 122 files and 19,058,698 bytes; both route plans and 100 MiB budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
  - Review regressions for supported process bindings, exported builtin-loader provenance, and canonical dependency roots containing the project before remediation (fail as expected: 3 tests exposed all merged review findings)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` after review remediation (pass, 207 tests)
  - Changed-file `npx prettier --check`, `npx tsc --noEmit`, and `npm run lint` after review remediation (pass)
  - `npm test` after review remediation (pass, 1009 tests)
  - `npm run build` after review remediation (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` after review remediation (pass; unchanged 122 files and 19,058,698 bytes; both route plans and budgets validated; 8 unique snapshots total 12,019,584 bytes)
  - `git diff --check && git diff --cached --check` (pass)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "createRequire factories|class property initializer|parameter initializer|binding default|compound assignment|loop binding|mixed named module namespace"` during review remediation (fail as expected: named namespace imports, binding defaults, loop containers, and namespace re-exports exposed four gaps)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts` during complete review-fixture validation (fail as expected: class/parameter initializers and compound assignments exposed three additional gaps)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` after review remediation (pass, 168 tests)
  - Changed-file `npx prettier --check`, `npx tsc --noEmit`, and `npm run lint` after review remediation (pass)
  - `npm test` after review remediation (pass, 970 tests)
  - `npm run build` after review remediation (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` after review remediation (pass; generate-team 122 files and 19,058,698 bytes; both route plans and 100 MiB budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts -t "team-details authority|createRequire factories|unresolved executable module load|tracked loader provenance|missing snapshots"` during review remediation (fail as expected: 12 tests exposed computed destructuring, import-equals, static indirection, export-shape, dependency-authority, and case-sensitivity gaps)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` after review remediation (pass, 141 tests)
  - Changed-file `npx prettier --check`, `npx tsc --noEmit`, and `npm run lint` after review remediation (pass)
  - `npm test` after review remediation (pass, 943 tests)
  - `npm run build` after review remediation (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` after review remediation (pass; generate-team 122 files and 19,058,698 bytes; both route plans and 100 MiB budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-037, exhausting the two-agent story budget, and the remaining changes were exact prior review findings with focused regressions
- Specialist review:
  - Agents used: initial staged `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `code-reviewer`, `qa-expert`, and `security-engineer` re-review
  - Findings: fixed six initial deduplicated medium areas, but targeted re-review found three remaining medium blockers: unsupported tracked destructuring/container assignments, module namespace or wrapped aggregate exports, and nested static factory/loader indirection can still erase provenance
- Decisions (why):
  - Rejected computed member access on any tracked factory, loader, or Node module namespace instead of guessing a runtime property value, preserving a complete fail-closed graph.
  - Rejected tracked provenance in default exports, TypeScript export assignments, and CommonJS export targets because the route-rooted walker intentionally does not perform inter-module value-flow analysis.
  - Applied exact route authority to recognizable evidence filenames regardless of directory so moving source CSVs, manifests, tooling indexes, or snapshots cannot disguise them as application code.
  - Rejected unsupported static factory/loader `call`, `apply`, and `bind` indirection instead of attempting incomplete function-value modeling; supported direct loader `call`/`apply` remains covered.
  - Applied artifact and PvPoke classification to canonical ancestor dependencies because trusted filesystem containment does not make their data assets valid route authorities.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, `code-formatter`, and `test-runner` skills plus `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
  - Computed element access must be checked as its own AST node because a surrounding call or variable binding can otherwise hide tracked loader provenance before module-specifier extraction.
  - Runtime data authority follows recognizable artifact semantics, not only the lexical `data/` directory.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
  - `US-037` remains `passes: false`; no commit was created because blocking medium findings remain after the single targeted re-review.
  - Fail closed when tracked namespace/factory/loader values enter destructuring assignments, nested/rest binding patterns, or aggregate containers.
  - Reject module namespaces and recursively wrapped tracked provenance in every ESM, TypeScript, and CommonJS export form.
  - Reject nested static chains such as `nodeModule.createRequire.call(...)` and `runtimeRequire.resolve.bind(...)` when their runtime value cannot be followed exactly.
- Next iteration start:
  - Resume US-037 test-first with the three targeted-review blocker areas, rerun all checks, and begin a fresh staged review loop.
---

## 2026-08-07 22:00:02 MST - US-037
- Resumed the blocked deployment-contract candidate and closed the remaining provenance gaps: tracked Node module namespaces, `createRequire` factories, and loader aliases now fail closed when hidden in unsupported destructuring or aggregate containers; recursively wrapped ESM, TypeScript, and CommonJS exports are rejected; and nested static factory/loader `call`, `apply`, or `bind` indirection cannot erase provenance.
- Files changed: `next.config.ts`, `lib/AGENTS.md`, `lib/architecture/pvpokeRuntimeBoundary.ts`, `lib/architecture/pvpokeRuntimeBoundary.test.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/data/moves.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/ordered.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/pokemon-list/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js.nft.json`, `lib/genetic/moveset.ts`, `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `progress.txt`
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "unresolved executable module load|tracked loader provenance"` before implementation (fail as expected: 13 tests exposed unsupported destructuring, aggregate containers, recursive exports, and nested static indirection)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts` after implementation (pass, 53 tests)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts -t "erased Node module|createRequire factories|unresolved executable module load|tracked loader provenance|team-details authority|missing snapshots"` during review remediation (fail as expected: 11 tests exposed all five merged review areas)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` after review remediation (pass, 162 tests)
  - `npx prettier --check lib/architecture/pvpokeRuntimeBoundary.ts lib/architecture/pvpokeRuntimeBoundary.test.ts docs/pokemon-go-team-optimization.md docs/team-optimization/data-inputs.md lib/AGENTS.md` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` after review remediation (pass, 964 tests)
  - `npm run build` after review remediation (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` after review remediation (pass; generate-team 122 files and 19,058,698 bytes; both route plans and 100 MiB budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
  - `git diff --check && git diff --cached --check` (pass)
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: `build-engineer`, `documentation-engineer`; both provided complementary confirmation of the exact route assets, independent function and aggregate snapshot budgets, boundary risks, existing documentation scope, and required validation commands
- Specialist review:
  - Agents used: initial staged `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `code-reviewer`, `qa-expert`, and `security-engineer` re-review
  - Findings: fixed five initial deduplicated medium areas; targeted QA found none, but targeted code and security review found two remaining medium blockers: supported loader `call`/`apply` results do not retain Node module namespace provenance, and mixed named exports still classify a `type createRequire` specifier as executable provenance
- Decisions (why):
  - Centralized tracked-provenance recognition over TypeScript AST expressions so assignment, export, and nested-member checks share one fail-closed definition instead of accumulating independent textual special cases.
  - Rejected unsupported containers and indirection rather than modeling arbitrary runtime value flow; the route-rooted architecture gate must not omit a dependency when provenance becomes ambiguous.
  - Did not classify the return value of an ordinary literal `require(...)` call as loader provenance, preserving dependency extraction for loaded modules while still tracking calls that create a new require function.
  - Classified canonical Pokemon, move, and type-effectiveness basenames as runtime data authority outside `data/` because moving those files must not evade the exact route plan.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, `code-formatter`, and `test-runner` skills plus `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
  - Loader provenance hidden inside object, array, nested, rest, or destructuring flows is not safely recoverable without interprocedural value analysis and must fail closed.
  - Export checks must inspect nested initializer values and module namespaces, not only the exported top-level identifier.
  - Node module namespace provenance can originate from `await import(...)`, `module.require(...)`, or a supported loader alias, not only a literal `require(...)` callee.
  - Functions and unknown wrappers must not return, yield, receive, construct, or tag tracked loader provenance unless the walker explicitly models that flow.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
  - `US-037` remains `passes: false`; no commit was created because blocking medium findings remain after the single targeted re-review.
  - Namespace classification must read the module specifier from the supported loader `call` second argument and `apply` literal argument array before a returned Node module namespace can expose `createRequire`.
  - Mixed named exports must ignore each erased `ExportSpecifier.isTypeOnly` binding while preserving executable sibling exports.
- Next iteration start:
  - Resume US-037 test-first with `call`/`apply` namespace provenance and mixed type-only export regressions, rerun all checks, and begin a fresh staged review loop.
---

## 2026-08-07 22:32:46 MST - US-037
- Resumed the staged deployment-contract candidate and closed the final two review blockers: supported CommonJS loader `call`/`apply` results now retain Node module namespace provenance, and mixed named exports ignore only type-erased specifiers while preserving executable siblings.
- Files changed: `next.config.ts`, `lib/AGENTS.md`, `lib/architecture/pvpokeRuntimeBoundary.ts`, `lib/architecture/pvpokeRuntimeBoundary.test.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/data/moves.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/ordered.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/pokemon-list/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js.nft.json`, `lib/genetic/moveset.ts`, `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `progress.txt`
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "erased Node module|createRequire factories"` before implementation (fail as expected: the mixed type-only export was rejected and `call`/`apply` namespace loads omitted two runtime dependencies)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "erased Node module"` for direct mixed re-export coverage (fail as expected: `type createRequire` was treated as executable provenance)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "erased Node module|createRequire factories|exported createRequire"` (pass, 3 selected tests)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` (pass, 162 tests)
  - Changed-file `npx prettier --check` (pass after formatting the architecture regression fixture)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 964 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; generate-team 122 files and 19,058,698 bytes; both route plans and 100 MiB budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
  - `git diff --check && git diff --cached --check` (pass)
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: `build-engineer`, `documentation-engineer`; both confirmed the exact route assets, independent function and aggregate snapshot budgets, architecture risks, existing documentation sections, and required validation commands
- Specialist review:
  - Agents used: initial staged `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `code-reviewer`, `qa-expert`, and `security-engineer` re-review
  - Findings: fixed two initial deduplicated medium areas; targeted QA and security found none, but targeted code review found one remaining medium blocker because shorthand destructuring assignment defaults omit `objectAssignmentInitializer` provenance
- Decisions (why):
  - Centralized direct and supported indirect CommonJS module-specifier extraction so dependency discovery and Node namespace provenance interpret `call` and `apply` identically.
  - Evaluated `ExportSpecifier.isTypeOnly` per named export instead of erasing or rejecting an entire mixed declaration, preserving executable siblings while preventing type-only `createRequire` from becoming loader provenance.
  - Rejected unsupported loader propagation rather than modeling class, parameter, binding-default, loop-container, or compound-assignment value flow; the boundary must fail closed when it cannot prove the exact runtime dependency.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` and `test-driven-development` skills plus `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
  - A supported `require.call` or `module.require.apply` can return the `node:module` namespace; provenance analysis must read the call's second argument or the apply array's first element before later `createRequire` access.
  - Named export declarations can mix erased and executable specifiers, so type-only status belongs to each `ExportSpecifier`, not only the declaration.
  - Node's named `default` and `Module` ESM bindings expose `createRequire` and therefore carry module namespace provenance just like namespace imports.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
  - `US-037` remains `passes: false`; no commit was created because one medium finding remains after the single targeted re-review.
  - Reject or track shorthand destructuring assignment defaults such as `({ runtimeRequire = require } = {})` by inspecting `ShorthandPropertyAssignment.objectAssignmentInitializer`, and add a focused literal/computed regression.
- Next iteration start:
  - Resume US-037 test-first with shorthand destructuring assignment-default provenance, rerun all checks, and begin a fresh staged review loop.
---

## 2026-08-07 22:59:41 MST - US-037
- Resumed the staged deployment-contract candidate and closed the remaining shorthand destructuring assignment-default provenance gap so object assignment patterns cannot hide a tracked CommonJS loader; retained the existing computed-property fail-closed behavior.
- Files changed: `next.config.ts`, `lib/AGENTS.md`, `lib/architecture/pvpokeRuntimeBoundary.ts`, `lib/architecture/pvpokeRuntimeBoundary.test.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/data/moves.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/ordered.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/pokemon-list/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js.nft.json`, `lib/genetic/moveset.ts`, `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `progress.txt`
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "unresolved executable module load"` before implementation (fail as expected: shorthand assignment-default provenance was not rejected; computed-property coverage already passed)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "unresolved executable module load"` after implementation (pass, 41 selected tests)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` (pass, 170 tests)
  - `npx prettier --check "lib/architecture/pvpokeRuntimeBoundary.ts" "lib/architecture/pvpokeRuntimeBoundary.test.ts" "docs/pokemon-go-team-optimization.md" "docs/team-optimization/data-inputs.md" "lib/AGENTS.md"` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 972 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; generate-team 122 files and 19,058,698 bytes; both route plans and 100 MiB budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
  - `git diff --check` (pass)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "createRequire factories|unresolved executable module load"` during review remediation (fail as expected: four cases exposed omitted `module.require` destructuring, global aliases, and computed module access)
  - `npx vitest run lib/build/functionTraceAnalyzer.test.ts -t "package identity"` during review remediation (fail as expected: a symlinked trusted dependency root erased `node_modules/pvpoke` identity)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "createRequire factories|unresolved executable module load"` after review remediation (pass, 45 selected tests)
  - `npx vitest run lib/build/functionTraceAnalyzer.test.ts -t "package identity"` after review remediation (pass, 1 selected test)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` after review remediation (pass, 174 tests)
  - Changed-file `npx prettier --check`, `npx tsc --noEmit`, and `npm run lint` after review remediation (pass)
  - `npm test` after review remediation (pass, 976 tests)
  - `npm run build` after review remediation (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` after review remediation (pass; unchanged 122 files and 19,058,698 bytes; both route plans and 100 MiB budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: skipped in this resumed iteration because both recommended agents already advised the existing US-037 candidate, exhausting the two-agent story budget, and the remaining change was one exact prior review finding with focused regressions
- Specialist review:
  - Agents used: fresh staged `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `code-reviewer`, `qa-expert`, and `security-engineer` re-review
  - Findings: fixed three initial merged medium blocker areas; targeted QA found none, but targeted code and security review found two remaining medium blocker areas: destructuring assignments/parameters can still hide CommonJS loader provenance, and package-level symlinks inside a trusted dependency root can erase the `pvpoke` package identity
- Decisions (why):
  - Rejected shorthand object assignment defaults carrying loader provenance instead of attempting to model whether the default executes at runtime; the architecture boundary must fail closed when assignment flow can hide a dependency.
  - Kept the computed-property regression even though it already passed because it distinguishes the existing generic binary traversal from the missing shorthand-specific AST field.
  - Unified loader alias recognition through `isCommonJsLoaderExpression(...)` so direct, member, global, and factory-produced loaders use the same fixed-point provenance path.
  - Reported trusted dependencies through their lexical `node_modules` root plus canonical root-relative package path after containment validation, preserving package authority without trusting lexical targets for filesystem access.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, `code-formatter`, and `test-runner` skills plus `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
  - TypeScript represents shorthand object assignment defaults through `ShorthandPropertyAssignment.objectAssignmentInitializer`; checking only the assignment expression's right side omits that provenance.
  - Canonical dependency paths can lose the `node_modules/<package>` identity when the dependency root itself is a symlink; retain a validated lexical-root mapping for reporting and policy classification.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
  - `US-037` remains `passes: false`; no commit was created because blocking medium findings remain after the single targeted re-review.
  - Track or reject supported static CommonJS loader destructuring in assignment and parameter forms, including literal and computed-property regressions.
  - Preserve validated lexical package identity through package-level symlinks such as `node_modules/pvpoke -> node_modules/.store/runtime` while retaining canonical containment and byte accounting.
- Next iteration start:
  - Resume US-037 test-first with CommonJS loader destructuring assignment/parameter coverage and package-level dependency symlink identity, rerun all checks, and begin a fresh staged review loop.
---

## 2026-08-07 23:27:14 MST - US-037
- Resumed the staged deployment-contract candidate and closed the two remaining review blockers: CommonJS `module.require` provenance now fails closed in destructuring assignment and parameter forms, and validated lexical package identity survives package-level dependency symlinks while canonical targets remain authoritative for containment, deduplication, and byte accounting.
- Files changed: `next.config.ts`, `lib/AGENTS.md`, `lib/architecture/pvpokeRuntimeBoundary.ts`, `lib/architecture/pvpokeRuntimeBoundary.test.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/data/moves.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/ordered.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/pokemon-list/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js.nft.json`, `lib/genetic/moveset.ts`, `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `progress.txt`
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "module loader in|computed module member in"` (fail as expected before implementation: six destructuring assignment/parameter forms were accepted)
  - `npx vitest run lib/build/functionTraceAnalyzer.test.ts -t "package-level dependency symlinks"` (fail as expected before implementation: `node_modules/pvpoke` was reported as its `.store` target)
  - Focused red regressions after implementation (pass, 9 selected tests)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` (pass, 182 tests before initial review)
  - Changed-file `npx prettier --check` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 984 tests before initial review)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; generate-team 122 files and 19,058,698 bytes; both route plans and 100 MiB budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
  - `git diff --check && git diff --cached --check` (pass before final staging)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "follows createRequire factories|computed aliased module"` during review remediation (fail as expected: module-owner aliases omitted two dependencies and two computed forms failed open)
  - `npx vitest run lib/build/functionTraceAnalyzer.test.ts -t "canonical duplicate validation"` during review remediation (fail as expected: canonical deduplication discarded the PvPoke package authority alias)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` after review remediation (pass, 185 tests)
  - Changed-file `npx prettier --check`, `npx tsc --noEmit`, and `npm run lint` after review remediation (pass)
  - `npm test` after review remediation (pass, 987 tests)
  - `npm run build` after review remediation (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` after review remediation (pass; unchanged 122 files and 19,058,698 bytes; both route plans and 100 MiB budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-037, exhausting the two-agent story budget, and the remaining work consisted of exact prior review findings with focused regressions
- Specialist review:
  - Agents used: fresh staged `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `code-reviewer`, `qa-expert`, and `security-engineer` re-review
  - Findings: fixed two initial deduplicated blocking areas, but the single targeted re-review found three remaining medium blockers: nested loader arguments can bypass dependency traversal, cross-root or canonical dependency aliases can hide lexical/canonical vendor and data authority, and the duplicate-alias regression does not directly assert single-count canonical byte accounting
- Decisions (why):
  - Rejected CommonJS loader destructuring in assignment and parameter flows instead of attempting interprocedural value tracking; supported variable-declaration destructuring remains explicit, while ambiguous execution-boundary flows fail closed.
  - Preserved a traced path beneath a trusted lexical dependency root only after its canonical target passed containment; this retains `node_modules/<package>` policy identity without weakening canonical deduplication or byte accounting.
  - Separated policy authority paths from canonical file reports so every package alias reaches route/vendor validation while unique file counts, categories, and bytes remain canonical and deterministic.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` and `test-driven-development` skills plus `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
  - CommonJS object destructuring has different AST and execution boundaries in declarations, assignments, and parameters; only explicitly modeled forms should propagate loader provenance.
  - Canonical paths establish trust and byte identity, but package policy must retain the validated lexical `node_modules/<package>` path when a package symlink targets an internal store.
  - CommonJS `module` is a loader-owning object whose identifier aliases must be tracked before `.require` access or destructuring can be interpreted safely.
  - Canonical trace deduplication and policy identity are separate concerns: retain all validated lexical aliases for policy and count each canonical target once for deployment size.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
  - `US-037` remains `passes: false`; no commit was created because blocking medium findings remain after the single targeted re-review.
  - Traverse or reject non-specifier arguments to recognized loader calls so nested computed loads cannot be omitted.
  - Preserve both every validated lexical alias and the canonical dependency target for policy across cross-root/package symlinks while continuing to deduplicate canonical file bytes.
  - Strengthen the canonical duplicate fixture to assert exactly one canonical file and a 10-byte total.
- Next iteration start:
  - Resume US-037 test-first with nested loader-argument traversal, cross-root lexical plus canonical dependency authority, and explicit duplicate-byte assertions; rerun all checks and begin a fresh staged review loop.
---

## 2026-08-10 17:55:37 MST - US-037
- Resumed the staged deployment-contract candidate and closed the three recorded prior-review blockers plus four fresh-review findings: recognized loader calls traverse nested argument and loader-factory loads; global loader-owner aliases fail closed; external packages are excluded consistently from the project-local graph; and trusted dependency roots retain canonical and every lexical authority identity across root, package, and project-targeting symlinks while canonical duplicate tests prove one dependency file and 10-byte accounting.
- Files changed: `next.config.ts`, `lib/AGENTS.md`, `lib/architecture/pvpokeRuntimeBoundary.ts`, `lib/architecture/pvpokeRuntimeBoundary.test.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/data/moves.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/ordered.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/pokemon-list/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js.nft.json`, `lib/genetic/moveset.ts`, `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `progress.txt`
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "computed nested.*loader argument"` before implementation (fail as expected: 3 nested computed loads were omitted)
  - `npx vitest run lib/build/functionTraceAnalyzer.test.ts -t "trusted dependency root aliases|canonical duplicate validation"` before implementation (fail as expected: the canonical trusted-root identity was omitted; the duplicate-byte assertion was corrected to exclude the emitted route entrypoint)
  - Focused red regressions after implementation (pass, 5 selected tests)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` (pass, 189 tests)
  - Changed-file `npx prettier --check` (pass after formatting `lib/build/functionTraceAnalyzer.ts`)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 991 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; generate-team 122 files and 19,058,698 bytes; both route plans and 100 MiB budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
  - `git diff --check && git diff --cached --check` (pass before final staging)
  - Fresh-review regressions for loader-factory arguments, global loader owners, external package layouts, and project-targeting package symlinks before remediation (fail as expected: 7 architecture cases and 1 trace-authority case exposed all four merged findings)
  - Fresh-review focused regressions after remediation (pass, 8 selected tests)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` after review fixes (pass, 196 tests)
  - Changed-file `npx prettier --check`, `npx tsc --noEmit`, and `npm run lint` after review fixes (pass)
  - `npm test` after review fixes (pass, 998 tests)
  - `npm run build` after review fixes (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` after review fixes (pass; unchanged 122 files and 19,058,698 bytes; both route plans and 100 MiB budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-037, exhausting the two-agent story budget, and the remaining work consisted of exact prior review findings with focused regressions
- Specialist review:
  - Agents used: fresh staged `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `code-reviewer`, `qa-expert`, and `security-engineer` re-review
  - Findings: fixed four initial deduplicated medium findings; targeted code review found none, but targeted QA and security review found three remaining medium blockers: derived global-owner aliases are not tracked, sequence/assignment-wrapped factory callees can skip argument traversal, and global loader owners can escape through unsupported aggregates or wrappers
- Decisions (why):
  - Traversed every recognized loader call argument after extracting the call's own module specifier because loader options, `thisArg`, and trailing arrays can contain independent executable loads.
  - Retained all trusted dependency roots by canonical-plus-lexical identity because canonical-root deduplication can erase a security-relevant `node_modules/<package>` alias; canonical file identity remains the sole source for counts and bytes.
  - Asserted dependency-category counts rather than complete trace counts because Next.js route traces also include the emitted route entrypoint by contract.
  - Used TypeScript's `isExternalLibraryImport` classification to keep the project-local source graph independent of package hoisting, while exact NFT trace validation remains responsible for deployed dependency assets.
  - Tracked global loader owners separately from CommonJS `module` owners so static or computed require aliases are recognized without treating ordinary `globalThis` property access as loader provenance.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, `code-formatter`, and `test-runner` skills plus `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
  - A recognized loader call's own literal specifier does not make its other arguments safe; continue AST traversal inside every argument subtree.
  - Trusted dependency roots need both canonical filesystem identity and every validated lexical root identity; package policy consumes all aliases, while deployment size consumes each canonical target once.
  - A package symlink can target ordinary in-project code outside `node_modules`; preserve its lexical package identity for policy even when canonical classification treats the target as application code.
  - Loader-factory calls can contain executable loads inside the callee expression, not only the outer call arguments.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
  - `US-037` remains `passes: false`; no commit was created because blocking medium findings remain after the single targeted re-review.
  - Track or reject derived global-owner aliases such as `globalThis.global` and `{ global: runtimeGlobal } = globalThis`.
  - Traverse loader-factory callees after the same sequence/assignment unwrapping used for loader recognition.
  - Treat global loader owners as tracked provenance so aggregate, wrapper, return, and unsupported assignment flows fail closed.
- Next iteration start:
  - Resume US-037 test-first with the three targeted-review blocker areas, rerun all checks, and begin a fresh staged review loop.
---

## 2026-08-10 18:21:19 MST - US-037
- Resumed the staged deployment-contract candidate and closed the three remaining boundary blockers: static `.global` and supported object-destructured global loader-owner aliases now retain provenance, global owners fail closed through aggregate/wrapper/return/unsupported assignment flows, and assignment/sequence-wrapped loader-factory callees are traversed for nested executable loads.
- Files changed: `next.config.ts`, `lib/AGENTS.md`, `lib/architecture/pvpokeRuntimeBoundary.ts`, `lib/architecture/pvpokeRuntimeBoundary.test.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/data/moves.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/ordered.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/pokemon-list/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js.nft.json`, `lib/genetic/moveset.ts`, `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `progress.txt`
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "createRequire factories|loader-factory arguments|global loader owner"` before implementation (fail as expected: 6 regressions exposed derived global-owner aliases, wrapped factory-callee traversal, and unsupported global-owner flows)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "createRequire factories|loader-factory arguments|global loader owner"` after implementation (pass, 6 selected tests)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` before review (pass, 200 tests)
  - Focused review regressions for builtin-module lookup, throw/catch provenance, discarded loader-callee operands, lexical trace aliases, and project-to-dependency package authority before remediation (fail as expected: 5 architecture assertions and 2 trace assertions exposed all four merged areas)
  - Focused review regressions after remediation (pass, 7 selected tests)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` after review fixes (pass, 204 tests)
  - Changed-file `npx prettier --check` and `git diff --check && git diff --cached --check` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` after review fixes (pass, 1006 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; generate-team 122 files and 19,058,698 bytes; both route plans and 100 MiB budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-037, exhausting the two-agent story budget, and the remaining work consisted of exact prior-review findings with focused regressions
- Specialist review:
  - Agents used: fresh staged `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `code-reviewer`, `qa-expert`, and `security-engineer` re-review
  - Findings: fixed four initial merged medium areas; targeted QA found none, but targeted code and security review found three remaining merged medium blockers: inline/aliased/`call`/`apply` `process.getBuiltinModule` forms do not retain or reject namespace provenance completely, assignment/comma operands inside loader `call`/`apply` receivers are not traversed, and a package symlink targeting the project can still erase its `node_modules/pvpoke` identity
- Decisions (why):
  - Reused one global-owner predicate in alias discovery and fail-closed provenance checks so static `.global` chains, supported object destructuring, loader extraction, and unsupported-flow rejection cannot drift.
  - Traversed the assignment/sequence-unwrapped factory callee instead of adding special cases for individual wrappers, matching the existing loader-binding semantics while preserving nested argument discovery.
  - Treated `process.getBuiltinModule(...)` as an explicit builtin lookup boundary: literal Node module authorities are modeled, while computed IDs fail closed rather than guessing provenance.
  - Preserved ordinary in-project symlink aliases for policy but rejected project-to-dependency symlinks when the traced path cannot retain the dependency package identity; canonical targets remain the only source for file counts and bytes.
  - Stopped without further remediation or commit because Ralph permits only one targeted re-review after substantive fixes and unresolved medium findings after that pass must block the story.
  - Testing decision: classified the change as a security-boundary bug fix, required test-first coverage, observed the focused regressions fail for the expected omissions, then implemented the minimal provenance and traversal changes needed for them to pass.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
  - Global loader-owner provenance includes derived static `.global` references and supported variable object destructuring; it is not limited to direct `globalThis` or `global` identifier aliases.
  - Loader-factory argument traversal must unwrap the callee through the same comma and plain-assignment expressions used for loader binding or nested executable loads can be omitted.
  - `process.getBuiltinModule('module')` provides the same `createRequire` namespace authority as an import or CommonJS load; computed builtin-module IDs are unresolved executable loads.
  - Loader provenance can escape through `throw`/`catch` just as through return/yield, so thrown tracked values must fail closed when catch-value flow is not modeled.
  - Route policy consumes canonical targets plus every in-project lexical alias; a project symlink into dependencies must be rejected when its package authority cannot be proven.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
  - `US-037` remains `passes: false`; no commit was created because blocking medium findings remain after the single targeted re-review.
  - Model or reject direct inline, aliased, destructured, and supported `call`/`apply` `process.getBuiltinModule` namespace sources while preserving computed-ID fail-closed behavior.
  - Traverse assignment/comma operands inside recognized loader `call`/`apply` receivers before returning from indirect-loader handling.
  - Preserve or reject `node_modules/<package>` identity when an in-project trace alias passes through a package symlink whose canonical target is also inside the project.
- Next iteration start:
  - Resume US-037 test-first with the three targeted-review blocker areas, rerun all checks, and begin a fresh staged review loop.
---

## 2026-08-10 18:49:41 MST - US-037
- Resumed the staged deployment-contract candidate and closed the three recorded review blockers: direct, aliased, destructured, and supported `call`/`apply` `process.getBuiltinModule` sources now retain Node module namespace provenance; indirect loader receivers traverse assignment/comma operands; and project-local trace aliases retain validated intermediate package identity when a package symlink targets the project.
- Files changed: `next.config.ts`, `lib/AGENTS.md`, `lib/architecture/pvpokeRuntimeBoundary.ts`, `lib/architecture/pvpokeRuntimeBoundary.test.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/data/moves.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/ordered.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/pokemon-list/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js.nft.json`, `lib/genetic/moveset.ts`, `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `progress.txt`
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "follows createRequire factories|traverses executable loads inside loader-factory arguments"` and `npx vitest run lib/build/functionTraceAnalyzer.test.ts -t "dependency symlink targets the project"` before implementation (fail as expected: five builtin-module variants and two receiver dependencies were omitted, and the project alias erased `node_modules/pvpoke` authority)
  - The same focused regression commands after implementation (pass, 3 selected tests)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` (pass, 204 tests)
  - Changed-file `npx prettier --write` and `npx prettier --check` (pass; unchanged)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 1006 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; generate-team 122 files and 19,058,698 bytes; both route plans and 100 MiB budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-037, exhausting the two-agent story budget, and the remaining work consisted of exact prior-review findings with focused regressions
- Specialist review:
  - Agents used: fresh staged `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `code-reviewer`, `qa-expert`, and `security-engineer` re-review
  - Findings: fixed four initial medium findings across two merged blocker areas, but the single targeted re-review found two remaining merged medium blockers: tracked CommonJS variants do not establish `node:process` owner provenance, and computed access on an inline builtin-module namespace is not rejected
- Decisions (why):
  - Extended the existing AST provenance sets to model supported builtin-module factory aliases instead of introducing a parallel parser, keeping namespace, loader, and fail-closed handling consistent.
  - Traversed only assignment/comma operands and nested calls inside recognized indirect-loader receivers so direct loader identifiers are not incorrectly treated as unsupported wrapper escapes.
  - Retained validated intermediate package paths as policy authority while continuing to report and count the canonical file once, preserving both vendor detection and exact deployment bytes.
  - Modeled supported process-owner forms rather than rejecting ordinary `process` APIs, while limiting tracked property provenance to `getBuiltinModule` and unknown computed members so `process.cwd()` and similar runtime code remain valid.
  - Rejected a trusted dependency root when its canonical target is the filesystem root or contains the canonical project because such a root can authorize arbitrary external files and misclassify route code as dependencies.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, `code-formatter`, and `test-runner` skills plus `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
  - `process.getBuiltinModule` is a loader factory authority, so aliases and destructured bindings must participate in the same fixed-point provenance analysis as `createRequire`.
  - An NFT trace can name a project alias whose symlink target enters `node_modules/<package>` and then returns to project code through a package symlink; preserve that validated intermediate package path for policy checks.
  - `node:process` default, namespace, named, dynamic, import-equals, and CommonJS bindings can all expose `getBuiltinModule`; process-owner and factory provenance must be fixed-point data and must not cross exports.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
  - `US-037` remains `passes: false`; no commit was created because blocking medium findings remain after the single targeted re-review.
  - Resolve `node:process` namespace loads through the existing tracked CommonJS loader machinery, including `module.require`, loader aliases, destructuring, and supported `call`/`apply`, then add inline and exported producer/consumer regressions.
  - Reject computed members whenever the receiver satisfies the complete module-namespace predicate, including inline `process.getBuiltinModule('module')[member]` expressions.
- Next iteration start:
  - Resume US-037 test-first with the two targeted-review blocker areas, rerun all checks, and begin a fresh staged review loop.
---

## 2026-08-10 19:18:37 MST - US-037
- Resumed the staged deployment-contract candidate and closed the final two recorded boundary blockers plus two fresh-review findings: `node:process` namespaces retain owner provenance through CommonJS loaders and literal `process.getBuiltinModule('process')` results, computed members are rejected against complete inline namespace predicates, and chained dependency symlinks retain every intermediate package authority identity.
- Files changed: `next.config.ts`, `lib/AGENTS.md`, `lib/architecture/pvpokeRuntimeBoundary.ts`, `lib/architecture/pvpokeRuntimeBoundary.test.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/data/moves.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/ordered.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/pokemon-list/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js.nft.json`, `lib/genetic/moveset.ts`, `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `progress.txt`
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "supported process bindings|exported CommonJS process namespace"` before implementation (fail as expected: inline and destructured tracked CommonJS process loads omitted dependencies)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "unresolved executable module load"` before implementation (fail as expected: the computed inline namespace member was accepted)
  - The same focused regressions after implementation (pass, 71 selected tests)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` (pass, 209 tests)
  - Changed-file `npx prettier --write` and `npx prettier --check` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 1011 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; generate-team 122 files and 19,058,698 bytes; both route plans and 100 MiB budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
  - `git diff --check && git diff --cached --check` (pass before final staging)
  - Fresh-review regressions for builtin process namespaces and chained dependency symlinks before remediation (fail as expected: two process-owner cases omitted dependencies, one exported namespace escaped, and the intermediate `node_modules/pvpoke` authority was absent)
  - The same focused review regressions after remediation (pass, 3 selected tests)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` after review fixes (pass, 211 tests)
  - Changed-file `npx prettier --check`, `npx tsc --noEmit`, and `npm run lint` after review fixes (pass)
  - `npm test` after review fixes (pass, 1013 tests)
  - `npm run build` after review fixes (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` after review fixes (pass; unchanged 122 files and 19,058,698 bytes; both route plans and 100 MiB budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-037, exhausting the two-agent story budget, and the remaining work consisted of two exact prior-review findings with focused regressions
- Specialist review:
  - Agents used: fresh staged `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `code-reviewer` and `security-engineer` re-review
  - Findings: QA found none; fixed two initial merged medium blocker areas, but the single targeted re-review found three remaining medium blockers: fully inline recursive process chains are rejected instead of extracted, lexical dependency-root aliases can hide chained package identities, and dot-segment symlink targets can erase intermediate package authority
- Decisions (why):
  - Extended the existing process-namespace predicate chain instead of introducing a parallel parser so CommonJS aliases, `module.require`, destructuring, and indirect invocation retain one fixed-point source of truth.
  - Rejected computed members against the complete Node module namespace predicate rather than only identifier-backed namespaces, closing inline builtin-module access without rejecting ordinary static members.
  - Classified literal `process.getBuiltinModule('process')` results as process owners through the same recursive predicate, preserving chained `getBuiltinModule('module')` dependency extraction and exported-provenance rejection.
  - Recursively followed only symlink-derived authority paths that remain under trusted lexical dependency roots and resolve to the same canonical traced file, retaining intermediate package identities without weakening containment or byte deduplication.
  - Testing decision: classified the change as a security-boundary bug fix, required test-first coverage, observed the focused regressions fail for the expected omissions, then implemented the minimal shared-predicate changes needed for them to pass.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, `code-formatter`, and `test-runner` skills plus `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
  - A `node:process` value returned by any recognized CommonJS loader is process-owner provenance even when consumed inline or destructured before assignment.
  - A literal `process.getBuiltinModule('process')` result is also process-owner provenance and may expose another `getBuiltinModule` call.
  - Computed-member rejection must evaluate the complete namespace expression, not only identifiers already present in the namespace set.
  - Dependency package identity can occur at any hop in a chained symlink; retain each trusted intermediate authority path while counting the final canonical file once.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
  - `US-037` remains `passes: false`; no commit was created because blocking medium findings remain after the single targeted re-review.
  - Thread the complete recursive process-namespace predicate through CommonJS loader recognition and add a fully inline `process.getBuiltinModule('process').getBuiltinModule('module').createRequire(...)` dependency regression, including supported `call`/`apply` forms.
  - Project canonical dependency paths into every matching lexical trusted-root alias before recursive symlink traversal so intermediate package identities survive a symlinked `node_modules` root.
  - Preserve symlink target components through physical resolution instead of lexically collapsing `..` before preceding symlinks resolve; add an `alias -> pvpoke/../runtime` regression that must retain `node_modules/pvpoke` authority.
- Next iteration start:
  - Resume US-037 test-first with the three targeted-review blocker areas, rerun all checks, and begin a fresh staged review loop.
---

## 2026-08-10 19:45:18 MST - US-037
- Resumed the staged deployment-contract candidate and closed the three recorded boundary blockers: fully inline recursive builtin process/module loader chains now preserve dependency extraction, canonical dependency paths are projected through every lexical trusted-root alias before symlink traversal, and symlink target dot segments resolve physically without erasing intermediate package authority.
- Files changed: `next.config.ts`, `lib/AGENTS.md`, `lib/architecture/pvpokeRuntimeBoundary.ts`, `lib/architecture/pvpokeRuntimeBoundary.test.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/data/moves.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/ordered.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/pokemon-list/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js.nft.json`, `lib/genetic/moveset.ts`, `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `progress.txt`
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "fully inline recursive process namespace loaders"` before implementation (fail as expected: the inline literal dependency was rejected as an unresolved load)
  - `npx vitest run lib/build/functionTraceAnalyzer.test.ts -t "projects chained package identities|symlink-sensitive dot segments"` before implementation (fail as expected: the lexical `node_modules/pvpoke` authority was absent and lexical normalization resolved the dot-segment target to the wrong path)
  - The same focused regression commands after implementation (pass, 3 selected tests)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` (pass, 214 tests)
  - Changed-file `npx prettier --write` and `npx prettier --check` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 1016 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; generate-team 122 files and 19,058,698 bytes; both route plans and 100 MiB budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
  - `git diff --check && git diff --cached --check` (pass before final staging)
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-037 in prior iterations, exhausting the two-agent story budget, and the remaining work consisted of three exact prior-review findings with focused regressions
- Specialist review:
  - Agents used: pending staged `code-reviewer`, `qa-expert`, and `security-engineer` high-risk review
  - Findings: pending
- Decisions (why):
  - Passed one recursive process-namespace predicate through existing CommonJS loader recognition instead of adding syntax-specific inline exceptions, keeping direct and supported indirect builtin-module forms consistent.
  - Projected both traced and canonical dependency identities through all matching lexical trusted roots before traversal so a symlinked `node_modules` root cannot hide package aliases that exist in its canonical tree.
  - Resolved link-target components with a cycle-safe physical walk so `..` is applied after preceding symlinks; retained intermediate package markers for vendor policy while canonical files remain the sole source for counts and bytes.
  - Testing decision: classified the changes as security-boundary bug fixes, required test-first coverage, observed all three regressions fail for the expected omissions, then implemented the minimal shared predicate and physical traversal needed for them to pass.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` and `test-driven-development` skills plus `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
  - A recursive `process.getBuiltinModule('process')` result must participate in loader recognition, not only provenance checks, or a later inline `createRequire` call is rejected before its literal dependency can be extracted.
  - Lexical path normalization is not a substitute for filesystem resolution: `pvpoke/../runtime` can reach a different directory when `pvpoke` is itself a symlink.
  - Canonical trusted-root identity and every lexical root alias must seed the same symlink authority traversal so package policy cannot depend on which equivalent NFT path Next.js emits.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
- Next iteration start:
  - Complete the fresh staged high-risk specialist review, address any blocking findings within the one allowed targeted re-review, then mark US-037 passed and commit.
---

## 2026-08-10 22:38:05 MST - US-037
- Completed the fresh staged high-risk review and fixed its only blocking finding by failing closed on static `process.mainModule` loader provenance, preventing `process.mainModule.require(...)` from escaping the route-rooted runtime dependency graph.
- Files changed: `next.config.ts`, `lib/AGENTS.md`, `lib/architecture/pvpokeRuntimeBoundary.ts`, `lib/architecture/pvpokeRuntimeBoundary.test.ts`, `lib/build/runtimeFunctionAssets.ts`, `lib/build/runtimeFunctionAssets.test.ts`, `lib/build/functionTraceAnalyzer.ts`, `lib/build/functionTraceAnalyzer.test.ts`, `lib/build/fixtures/functionTrace/valid/data/moves.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/ordered.js.nft.json`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/generate-team/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/pokemon-list/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js`, `lib/build/fixtures/functionTrace/valid/.next/server/app/api/team-details/route.js.nft.json`, `lib/genetic/moveset.ts`, `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/data-inputs.md`, `progress.txt`
- Commit message: `feat: US-037 - Guard assignment authority deployment and documentation contracts`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts -t "process mainModule loader provenance"` before remediation (fail as expected: `process.mainModule.require(...)` was omitted)
  - The same focused regression after remediation (pass, 1 selected test)
  - `npx vitest run lib/architecture/pvpokeRuntimeBoundary.test.ts lib/build/runtimeFunctionAssets.test.ts lib/build/functionTraceAnalyzer.test.ts lib/genetic/moveset.test.ts app/api/team-details/route.test.ts` (pass, 215 tests)
  - Changed-file `npx prettier --check` (pass)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 1017 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `npm run analyze:generate-team-trace` (pass; generate-team 122 files and 19,058,698 bytes; both route plans and 100 MiB budgets validated; 8 unique snapshots total 12,019,584 bytes under the 50 MiB budget)
  - `git diff --check && git diff --cached --check` (pass before final restaging)
- Implementation agents:
  - Recommended: `build-engineer`, `documentation-engineer`
  - Used: skipped in this resumed iteration because both recommended agents already advised US-037 in prior iterations, exhausting the two-agent story budget, and this iteration resumed at the pending review gate
- Specialist review:
  - Agents used: fresh staged `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `security-engineer` re-review
  - Findings: code and QA review found none; fixed one medium security finding by rejecting static `process.mainModule` loader provenance; targeted security re-review found none
- Decisions (why):
  - Failed closed on `process.mainModule` rather than modeling its deprecated mutable module object because exact inter-module loader provenance cannot be established safely and the runtime boundary must not omit dependencies.
  - Added the regression to the existing unresolved executable-load matrix so it exercises the public route-graph boundary rather than a private helper.
  - Testing decision: classified the change as a security-boundary bug fix, required test-first coverage, observed the focused regression fail for the expected omission, then implemented the minimal tracked-provenance rule needed for it to pass.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer` and `test-driven-development` skills plus `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
  - A tracked process namespace exposes loader provenance through deprecated `mainModule` as well as `getBuiltinModule`; unsupported loader-bearing process properties must fail closed.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
- Next iteration start:
  - All PRD stories are complete and passing.
---

## 2026-08-11 18:52:53 MST - US-038
- Added an explicit opt-in moveset variant sync flag, normalized omitted sync options to ranked-default-only simulation mode, introduced testable fail-closed CLI parsing, preserved combined resume and variant operation, and made read-only projection report and apply the selected variant mode.
- Files changed: `lib/AGENTS.md`, `lib/scripts/sync.ts`, `lib/scripts/syncCommand.ts`, `lib/scripts/sync.test.ts`, `lib/sync/types.ts`, `lib/sync/index.ts`, `lib/sync/index.test.ts`, `lib/sync/simulationProjection.ts`, `lib/sync/simulationProjection.test.ts`, `prd.json`, `progress.txt`
- Commit message: `feat: US-038 - Make moveset variant sync explicitly opt-in`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/scripts/sync.test.ts lib/sync/index.test.ts lib/sync/simulationProjection.test.ts` before implementation (fail as expected: missing parser module, omitted false option propagation, and missing projection mode/filtering)
  - `npx vitest run lib/scripts/sync.test.ts lib/sync/index.test.ts lib/sync/simulationProjection.test.ts` after implementation (pass, 24 tests)
  - `npx vitest run lib/scripts/sync.test.ts lib/sync/index.test.ts lib/sync/simulationProjection.test.ts lib/sync/simulations.test.ts` (pass, 35 tests)
  - `npx prettier --write "lib/scripts/sync.ts" "lib/scripts/syncCommand.ts" "lib/scripts/sync.test.ts" "lib/sync/types.ts" "lib/sync/index.ts" "lib/sync/index.test.ts" "lib/sync/simulationProjection.ts" "lib/sync/simulationProjection.test.ts"` (pass; unchanged)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 1031 tests)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
  - `bun run lib/scripts/sync.ts --resuem` (fail as expected with non-zero actionable unknown-option error before sync work)
  - `npx prettier --check "lib/scripts/sync.ts"`, `npx tsc --noEmit`, `npm run lint`, and focused 24 tests after review fix (pass)
- Implementation agents:
  - Recommended: `cli-developer`, `test-automator`
  - Used: `cli-developer`; skipped `test-automator` because the standard-mode one-agent budget and focused pure parser/orchestration tests made a second overlapping advisor unnecessary
- Specialist review:
  - Agents used: initial staged `code-reviewer` and `qa-expert`; targeted `code-reviewer` re-review
  - Findings: QA found none; fixed one medium code-review finding by documenting npm's required `--` argument separator; targeted re-review found none
- Decisions (why):
  - Extracted a pure parser and injected command dispatcher instead of importing the executable script or mutating `process.argv`, allowing CLI behavior and destructive boundaries to be tested directly.
  - Normalized `includeMovesetVariants` at the `completeSimulationManifestSync(...)` boundary so every normal programmatic sync also defaults false, not only the package script.
  - Made default projection suppress alternate estimates and report existing recognized variant files as stale, while opt-in projection preserves the existing bounded candidate estimate; both paths retain read-only dependencies.
  - Deferred default-only candidate simulation, manifest publication, snapshot generation, and cleanup behavior to US-039 rather than changing atomic publication semantics in the CLI-plumbing story.
  - Testing decision: classified the change as behavioral, required test-first coverage, observed focused failures for every missing contract, then implemented the minimal parser, option propagation, and projection behavior needed for them to pass.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `code-formatter` skills plus `docs/pokemon-go-team-optimization.md`, `docs/team-optimization/scoring-model.md`, `docs/team-optimization/lineup-structures.md`, `docs/team-optimization/coverage-threat-pools.md`, `docs/team-optimization/safety-consistency-bulk.md`, `docs/team-optimization/type-effectiveness.md`, `docs/team-optimization/role-scoring.md`, `docs/team-optimization/data-inputs.md`, and `docs/team-optimization/validation.md`.
  - CLI parsing belongs in an import-safe pure module; the executable wrapper should only dispatch parsed `process.argv.slice(2)` and translate failures to a non-zero exit.
  - Projection mode should express both the selected variant policy and counts consistent with that policy while retaining strict no-write dependencies.
- Known issues / follow-ups:
  - The existing multiple-lockfile workspace-root warning remains during production builds.
  - The pre-existing `tasks/prd-deterministic-moveset-variants.md` revision remains unstaged because Ralph did not create or need to modify it for this story.
  - US-039 must make `generateSimulations(...)`, manifest/snapshot publication, and stale cleanup honor the new default-only option while preserving the opt-in variant path.
- Next iteration start:
  - US-039 - Publish default-only simulation authorities
---

## 2026-08-11 19:24:35 MST - US-039
- Implemented one shared default-only candidate authority for simulation and manifest preparation; default sync now evaluates exactly each synchronized species' ranked default while explicit variant sync preserves the full bounded candidate path. Added fail-closed synchronized-species coverage, manifest-declared CSV publication validation, and an immediate no-follow regular-file check before stale variant deletion. Existing manifest-derived compact snapshot and runtime asset index preparation now receive default-only authority inputs without changing their schemas.
- Files changed: `lib/AGENTS.md`, `lib/sync/index.ts`, `lib/sync/index.test.ts`, `lib/sync/simulations.ts`, `lib/sync/simulations.test.ts`, `lib/sync/movesetVariantManifest.ts`, `lib/sync/movesetVariantManifest.test.ts`, `lib/sync/simulationCleanup.ts`, `lib/sync/simulationCleanup.test.ts`, `prd.json`, `progress.txt`
- Commit message: `feat: US-039 - Publish default-only simulation authorities`
- Auto approval: `disabled`
- Checks:
  - `npx vitest run lib/sync/simulations.test.ts lib/sync/index.test.ts lib/sync/movesetVariantManifest.test.ts lib/sync/simulationCleanup.test.ts` (fail as expected before implementation: default mode retained alternates, publication accepted undeclared CSVs, and cleanup did not recheck file type)
  - `npx vitest run lib/sync/index.test.ts` (fail as expected before synchronized-species completeness fix: missing candidate authority reached generation)
  - `npx vitest run lib/sync/index.test.ts lib/sync/simulations.test.ts lib/sync/movesetVariantManifest.test.ts lib/sync/runtimeSimulationSnapshots.test.ts lib/sync/runtimeSimulationAssetIndex.test.ts lib/sync/simulationCleanup.test.ts` (pass, 63 tests after review fix)
  - `npx vitest run lib/sync/simulations.test.ts lib/sync/index.test.ts lib/sync/movesetVariantManifest.test.ts lib/sync/runtimeSimulationSnapshots.test.ts lib/sync/runtimeSimulationAssetIndex.test.ts lib/sync/simulationCleanup.test.ts lib/architecture/pvpokeRuntimeBoundary.test.ts` (pass, 166 tests before the final completeness regression)
  - `npx tsc --noEmit` (pass)
  - `npm run lint` (pass)
  - `npm test` (pass, 1,037 tests after review fix)
  - `npm run build` (pass; existing multiple-lockfile workspace-root warning only)
  - `git diff --check` (pass)
- Implementation agents:
  - Recommended: `data-engineer`, `security-engineer`
  - Used: `data-engineer`, `security-engineer`; both were within the standard-mode high-risk budget and covered non-overlapping data-authority and destructive-cleanup risks
- Specialist review:
  - Agents used: initial `code-reviewer`, `qa-expert`, and `security-engineer`; targeted `code-reviewer` re-review
  - Findings: fixed one high code-review finding by filtering candidate authority to the synchronized species pool before enforcing a default, preserving evidence-only rejected ranking sets; QA found none; targeted re-review found none. A security report about parent-directory replacement was treated as a pre-existing residual risk because the staged change adds a final-component `lstat` check but does not weaken the existing physical directory checks, and Node's path APIs do not expose dirfd-relative unlink.
- Decisions (why):
  - Projected candidate sets once in `completeSimulationManifestSync(...)` and defensively inside direct simulation generation so manifests, snapshots, the runtime index, and generated CSVs cannot disagree about default-only authority.
  - Kept snapshot and runtime asset index implementations mode-agnostic because validated manifests already define their exact active inputs.
  - Added publication validation against every manifest candidate storage key, not active keys only, so resume or opt-in generation can still atomically publish inactive evidence while default-only publication rejects undeclared alternates.
  - Added `lstat`-backed pre-unlink validation instead of broad cleanup changes so canonical parser, catalog directory, physical path, and regular-file confinement remain layered and minimal.
  - Filtered ranking evidence to `simulationSpeciesIdsByFormatId` before default projection because candidate derivation intentionally retains rejected evidence-only sets for species omitted from Overall simulation.
- **Learnings for future iterations:**
  - Read the `gbl-optimizer`, `test-driven-development`, and `test-runner` skills plus `docs/pokemon-go-team-optimization.md` and all eight required `docs/team-optimization/` references.
  - Default-only authority should be expressed by narrowing candidate inputs, not by adding mode conditionals to manifest, snapshot, or runtime index serializers.
  - Publication must bind prepared source artifacts to the authority being installed; filename and directory validation alone do not prove a generated CSV belongs to the prepared manifest.
- Known issues / follow-ups:
  - The unrelated existing `tasks/prd-deterministic-moveset-variants.md` modification remains unstaged and is not part of US-039.
  - US-041 remains responsible for running default sync against checked-in data and removing prior variant-qualified CSVs through this tested cleanup boundary.
- Next iteration start:
  - US-040 - Hide moveset variation from team configuration
---
