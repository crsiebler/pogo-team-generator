# Path to the local pvpoke checkout used for data sync.
# Default: vendor/pvpoke (bundled git submodule)
# NOTE: After cloning, run `git submodule update --init --recursive`
#       to ensure vendor/pvpoke is checked out.
# Set a different path only if you keep pvpoke elsewhere.
PVPOKE_PATH=vendor/pvpoke
